# DEPRECATED — do not use for local tap-hoa.
# Local database is Supabase CLI only (project_id=tap-hoa, port 54422).
# See docs/ops/local-dev.md and scripts/dev-up.ps1.
#
# Kept as a stub so accidental `docker compose -f docker-compose.dev.yml up`
# does not start a second Postgres. Use: .\scripts\dev-up.ps1
name: tap-hoa-deprecated

services:
  deprecated-notice:
    image: alpine:3.20
    container_name: tap-hoa-deprecated-notice
    command:
      [
        "sh",
        "-c",
        "echo 'tap-hoa local DB is Supabase only. Use scripts/dev-up.ps1' && exit 1",
      ]
