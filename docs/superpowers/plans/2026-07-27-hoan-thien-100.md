# Hoàn thiện 100% — Kế hoạch đóng gap spec §4–§6

> **For agentic workers:** Subagent-Driven Development — **một Task = một subagent**, review giữa các task, không mở task tiếp theo khi task trước chưa PASS review.
> **Nguồn:** audit code-vs-spec 2026-07-27 (memory `global/architecture/trang-thai-hoan-thanh-vs-spec`). Đối chiếu `docs/superpowers/specs/2026-07-23-tap-hoa-pos-ke-toan-design.md` §4–§6 với `main` @ `2c982e1`.
> **Branch:** `feat/hoan-thien-100`.

**Goal:** Từ **~84% spec** → **100%**, ưu tiên các lỗ chặn "bán thật" trước, rồi tới đa điểm bán, rồi nợ kỹ thuật.

**Architecture:** Không đổi monorepo. Offline-first giữ nguyên: chứng từ mới → Drift + outbox + sync DTO + e2e. Bút toán mới bám `LedgerService` / `journal-builders` + period lock + replay.

**Tech Stack:** NestJS 10 + Prisma + PostgreSQL (`apps/api`); Flutter 3 + Drift (`apps/pos_app`).

## Global Constraints

- Thay đổi sổ/sync → **bắt buộc e2e**; Flutter → `flutter analyze` sạch trên path đụng.
- Không mở YAGNI §7 (website, loyalty, HR, chuỗi > ~10 điểm, nộp CQT).
- Seed `0900000001` / `123456` chỉ dev/test.
- Không `npm audit fix --force`.
- Mọi task phải chạy được `npm run build` + `npm run test:unit` (API) trước khi review.

---

## Trạng thái

| Task | Mô tả | Ưu tiên | Status |
|------|-------|---------|--------|
| **P0.1** | UsersModule — tạo/sửa tài khoản nhân viên, gán điểm bán, cờ quyền | P0 | **Done** — e2e xanh qua P1.4 (`users.e2e-spec.ts`) |
| **P0.2** | Hoàn tác số dư nợ local khi server từ chối thu nợ | P0 | **Done** — 136/136 flutter test pass |
| **P0.3** | Thống nhất tiền mặt: `cashFundSummary` ↔ `expected-cash` | P0 | **Done** — e2e xanh qua P1.4 (`cash-fund-consistency.e2e-spec.ts`) |
| **P0.4** | Guard `/reports/period/*` + phủ e2e ma trận quyền | P0 | **Done** — e2e xanh qua P1.4 (`permissions.e2e-spec.ts`, `reports-permissions.e2e-spec.ts`) |
| **P1.1** | Pull tự động trong `SyncScheduler` | P1 | **Done** — 142/142 flutter test pass, `flutter analyze` sạch |
| **P1.2** | `allowNegativeStock` thành cấu hình thật (server → pull → UI) | P1 | **Done** — e2e xanh qua P1.4 (`stores.e2e-spec.ts`) |
| **P1.3** | UI kho nhiều dòng + chọn lý do hủy + ghi chú kiểm kê | P1 | **Done** — 142/142 flutter test pass, `flutter analyze` sạch |
| **P1.4** | CI chạy e2e với Postgres service container | P1 | **Done** — 37/37 suite, 122/122 test pass (verified local qua container, xem ghi chú) |
| **P2.1** | Backoff + dead-letter cho outbox | P2 | **Done** — 146/146 flutter test pass (142 baseline + 4 mới), `flutter analyze` sạch |
| **P2.2** | Sổ quỹ tổng + bút toán bank recon + map danh mục chi → TK | P2 | **Done** — 38/38 suite, 129/129 test pass (verified qua Postgres container, 3 lần chạy full-suite DB-sạch khác nhau, xem ghi chú) |
| **P2.3** | Báo cáo nhập–xuất–tồn theo kỳ/điểm | P2 | **Done** — 39/39 suite, 136/136 test pass (verified qua Postgres container, 3 lần chạy full-suite DB-sạch khác nhau, xem ghi chú); flutter 151/151 pass (146 baseline + 5 mới), `flutter analyze` sạch |
| **P2.4** | HĐĐT: XML/PDF thật + endpoint tải + audit vòng đời | P2 | **Done** — 39/39 suite, 140/140 test pass (verified qua Postgres container, 3 lần chạy full-suite DB-sạch khác nhau, xem ghi chú); flutter 151/151 pass (baseline không đổi, chỉ thêm nhãn), `flutter analyze` sạch |
| **P2.5** | Quét mã xem tồn (§4.8) + restore backup bằng code | P2 | **Done** — 159/159 flutter test pass (151 baseline + 8 mới), `flutter analyze` sạch |
| **P2.6** | Docs hygiene: README, checkbox plans, dọn `flutter_*.log` | P2 | **Done** — 17/17 DoD box đã tick, README xác nhận không có gì sai, `flutter_*.log` xác nhận non-issue |
| **P1.5** | `store_forbidden` của thu nợ ném ngoài try → cả batch 403 + retry vô hạn | P1 | **Done** — e2e xanh qua P1.4 (`debt-payments.e2e-spec.ts`, `sync-push.e2e-spec.ts`) |
| **P1.6** | Đổi mật khẩu của chính mình phải nhập mật khẩu hiện tại | P1 | **Done** — e2e xanh qua P1.4 (`users.e2e-spec.ts`) |
| **P1.7** | Audit log cho quản lý nhân viên (tạo/đổi vai trò/reset mật khẩu) | P1 | **Done** — e2e xanh qua P1.4 (`users.e2e-spec.ts`) |
| **P1.8** | `reports.e2e-spec.ts` — test *"breaks revenue down by shift"* tạo 2 `Shift` đang mở (`closedAt=null`) cùng `storeId+userId`, vi phạm chính partial unique index `Shift_storeId_userId_open_key` mà migration `20260723203000_phase1_review_fixes` định nghĩa | P1 | **Done** — phát hiện độc lập bởi 2 nhánh song song (PR #26 ghi nhận là task mới; nhánh này tự bắt và sửa luôn trong P1.4 bằng cách thêm `closedAt` cho `shiftA`) — xem "Ghi chú review P1.4" |

### Chặn môi trường (2026-07-27)

Không chạy được e2e trên máy này: Windows đã chiếm dải TCP **55325–55424**, mà toàn bộ cổng Supabase của `tap-hoa` (55420–55429) nằm trong đó → Docker báo
`bind: An attempt was made to access a socket in a way forbidden by its access permissions`.
Container `supabase_db_tap-hoa` chạy healthy nhưng **không publish port ra host**.
Cần: (a) admin chạy `net stop winnat && net start winnat` + đặt reservation cố định, hoặc (b) dời dải cổng dự án sang vùng trống (vd. 54420–54429), đụng `tap-hoa.identity.ts` + `config.toml` + scripts + docs.

**Đã gỡ (2026-07-27):** chọn phương án (b) — bỏ qua (a) vì `net stop/start winnat` sẽ làm gián đoạn NAT của các container Docker khác đang chạy trên máy (project không liên quan `omni-commerce`, 11 container). Đổi toàn bộ dải cổng Supabase local của tap-hoa từ `5542x` sang `5442x` (`config.toml`, `tap-hoa.identity.ts` + spec, `.env.example`, `write-tap-hoa-env.ps1`, `dev-up.ps1`, README/docs). `5442x` nằm trong khoảng trống thật (đối chiếu `netsh interface ipv4 show excludedportrange protocol=tcp`), không đụng cổng của `omni-commerce`.

### Ghi chú review P0.1

- Trùng `phone` trả **409** trong khi `POST /stores` trùng `code` trả **400** → repo đang không nhất quán mã lỗi giữa các module.
- `PATCH /users/:id/password` không thu hồi JWT đang có của người bị đổi mật khẩu. Giảm nhẹ: `jwt.strategy.ts` query DB mỗi request và chặn `active=false`, nên **vô hiệu hóa nhân viên có hiệu lực tức thì**.

### Ghi chú review P0.3

- Tìm thêm **bug thứ hai**: `voucherInVnd`/`voucherOutVnd` trước đây cộng **mọi kênh**, nên phiếu thu/chi **chuyển khoản** bị tính nhầm vào tiền mặt. Đã tách sang `voucherTransferInVnd/OutVnd`; tên field cũ giữ nguyên nhưng **đổi ngữ nghĩa** (chỉ còn tiền mặt).
- Quyết định kiến trúc: trả **cả hai** con số — `netCashVnd` (từ chứng từ) và `ledgerNetCashVnd` (Nợ−Có TK 111 thật) kèm `ledgerDiffVnd`. Không thay `netCashVnd` bằng số sổ cái, vì chứng từ tiền mặt có thật nhưng chưa lên sổ (khóa kỳ, `safePost` nuốt lỗi) sẽ biến mất khỏi sổ quỹ — sai với ngăn kéo tiền thật. Lệch được **phơi ra** thay vì bị giấu.
- Còn nợ (tách task): `shifts.service.ts::loadShiftCashInputsWithClient` bỏ sót **hoàn tiền mặt của trả hàng bán** → đóng ca báo lệch âm giả. `SupplierPayment` không có `shiftId` nên không quy được về ca.
- Chạy e2e thật lần đầu (sau khi gỡ chặn cổng) bắt được bug trong chính `cash-fund-consistency.e2e-spec.ts`: test lấy mốc `clientCreatedAt` **trước** khi gọi `/shifts/open`, nên `sync.service.ts` từ chối bán hàng với `shift_not_open_at_sale` (`clientCreatedAt < shift.openedAt`). Đã sửa: lấy mốc thời gian sau khi mở ca.

### Ghi chú review P0.4

- Chọn **method-level guard** thay vì tách controller: chỉ thêm decorator, không di chuyển code, URL bất biến. 20 route kế toán có guard; 5 route vận hành (`day`, `top-skus`, `stock-on-hand`, `debt-aging`, `ar.csv`) cố ý KHÔNG guard.
- Phát hiện kèm: POS đang để `store_manager` **không** `canLedger` mở được Sổ quỹ / Đối chiếu NH / Đối chiếu NCC → sau khi khóa sẽ ăn 403. Đã gate lại UI theo `canLedger` (fail-closed).
- `phase3-hardening-accounting.e2e-spec.ts` có fixture **dựa vào chính lỗ hổng này** (manager không set `canLedger` mà expect 200) → đã sửa fixture thành `canLedger: true`, giữ nguyên ý định test (kiểm tra scope cửa hàng).
- Chưa làm (defense-in-depth): `resolvePeriodStoreFilter` trong service vẫn chỉ lọc theo role. Guard ở controller đủ cho biên HTTP vì hiện không có caller nào khác.

### Ghi chú review P0.2

- Nhãn `customer_not_found` phải trung tính: reason này dùng chung cho cả **đơn bán ghi nợ** (`sync.service.ts:907/913`) lẫn phiếu thu nợ, mà chỉ phiếu thu nợ mới được hoàn tác.
- Hoàn tác nhận diện qua **sự có mặt trong `rejectedDebtPayments`**, không qua nội dung `reason` — mọi lỗi hạ tầng đều ném ra thành `DioException` và giữ entry `pending`.

### Ghi chú review P1.6

- `setPassword` giờ chia hai nhánh theo `actor.userId === userId` (tự đổi, bất kể vai trò kể cả owner) vs owner đổi người khác (giữ nguyên, không cần `currentPassword` — đây là đường hồi phục khi quên mật khẩu). Nhánh tự đổi truy vấn `passwordHash` riêng (không qua `userSelect` vì select đó cố tình không kèm hash) rồi `bcrypt.compare`.
- Mã lỗi khi `currentPassword` sai hoặc thiếu: **400 Bad Request** với message `current_password_incorrect` (theo house style snake_case reason đã thấy ở `debt_adjust_negative_balance`, `supplier_not_found`, `bank_recon_locked`...). Không tái dùng 403 `'Only owner can change other passwords'` vì lỗi đó là sai *đối tượng* (cross-user), còn đây là sai *thông tin xác thực* của chính chủ — hai nguyên nhân khác nhau nên tách mã.
- `parsePasswordBody` đổi thành `parseSetPasswordBody`, trả `{ password, currentPassword? }` thay vì chỉ `string`; `currentPassword` chỉ được parse/validate khi có mặt trong body (không ép buộc ở tầng parse vì owner-reset-người-khác không cần trường này) — ép buộc thật sự nằm ở `UsersService.setPassword` dựa theo actor/target.
- e2e: sửa case tự đổi mật khẩu hiện có để gửi kèm `currentPassword` đúng; thêm case sai/thiếu `currentPassword` (kèm xác nhận mật khẩu cũ vẫn đăng nhập được — không có gì bị đổi); thêm case owner tự đổi mật khẩu của chính mình cũng phải qua currentPassword (dùng `try/finally` ghi thẳng `passwordHash` qua Prisma trong `finally` để trả owner seed về `123456` không phụ thuộc endpoint đang test).
- Client: đã hoàn thành, không cần refactor lớn — `PosPage` đã có sẵn `widget.user.id` (từ `AuthUser`, gán lúc đăng nhập), nên chỉ thêm 1 constructor param bắt buộc `currentUserId` cho `UserManagementPage` và truyền `widget.user.id` tại call site (`pos_page.dart` dòng ~778). Dialog đổi mật khẩu (`_changePassword`) so `user['id']` với `currentUserId`: nếu trùng (tự đổi) hiện thêm ô "Mật khẩu hiện tại" và gửi `currentPassword`; nếu khác (owner reset người khác) giữ nguyên 1 ô như cũ, không gửi `currentPassword`. `flutter analyze lib` sạch.

### Ghi chú review P1.5

- Gốc lỗi: `processDebtPayment` gọi `this.assertStoreAccess(user, payment.storeId)` ngoài mọi `try/catch`, nên `ForbiddenException('store_forbidden')` thoát thẳng ra `pushDebtPayments` → `push` → controller, biến thành HTTP 403 cho **cả request** `/sync/push`, kéo theo mọi entity khác (sales, cash voucher, stock ops...) bị kẹt `pending` và outbox retry vô hạn.
- Sửa bằng đúng pattern đã có ở `processCashVoucher` (dòng ~517-521): bọc `assertStoreAccess` trong `try/catch`, trả `{ accepted: false, reason: 'store_forbidden' }` cho riêng entry đó — batch còn lại vẫn 2xx.
- Không đụng `assertStoreAccess` hay caller nào khác; client (`outbox_worker.dart`, `outbox_reason_labels.dart`) đã hoàn tác tự động qua sự có mặt trong `rejectedDebtPayments` từ P0.2, không cần sửa thêm.

### Ghi chú review P1.7

- `listAudit` (`ledger.service.ts::assertLedgerAccess`) chặn cashier **tại tầng guard** — `LedgerPermissionGuard` trên `@Controller('ledger')` gọi `hasLedgerPermission(user)`, trả `false` tuyệt đối cho `role !== owner && role !== store_manager`, nên cashier nhận 403 trước khi vào tới service, không phụ thuộc `canLedger`. store_manager cần `canLedger=true` mới qua được guard; owner luôn qua. Không có lỗ hổng kiểu "cashier đọc được audit" — không sửa gì ở tầng này.
- Tìm ra một chỗ khớp nối cần sửa để 3 action mới thực sự hiện ra: `listAudit` mặc định lọc `action: { in: defaultAuditActions }` khi client không truyền `action` — mà `LedgerRepository.listAudit()` (Dart, `ledger_page.dart` dòng ~82) gọi `/ledger/audit` **không kèm** `action`. Nếu không thêm `user_create`/`user_role_change`/`user_password_reset` vào `defaultAuditActions` (`ledger.service.ts`), các dòng audit nhân viên sẽ nằm im trong DB nhưng không bao giờ lên UI — trái với kỳ vọng "ghi là thấy ngay ở tab Sổ kế toán" của task. Đã thêm 3 action vào mảng đó (không đổi cấu trúc filter, không tạo endpoint mới).
- Hệ quả phụ đáng lưu ý (không sửa, chỉ ghi nhận): `listAudit` lọc theo store cho store_manager bằng cách match chuỗi `"storeId":"<id>"` trong `detailJson`. Ba action mới **không** có `storeId` trong detail (theo đúng shape task yêu cầu: `{phone, role, storeIds}` / `{fromRole, toRole}` / `{selfChange}`), vì quản lý nhân viên không gắn với 1 điểm bán cụ thể. Kết quả: store_manager (kể cả có `canLedger`) gọi `listAudit()` mặc định sẽ **không bao giờ thấy** 3 loại audit này — chỉ owner thấy. Chấp nhận được vì `create`/role-change/reset-người-khác trong `UsersService` vốn đã owner-only; tự đổi mật khẩu của chính store_manager thì họ không cần xem lại audit của chính hành động đó qua tab Sổ kế toán. Nếu sau này muốn store_manager thấy, cần thêm `storeIds` (mảng) vào detail và đổi cách match ở `ledgerAuditStoreIds`/`auditStoreIdFragment` — ngoài scope task này.
- Action string & detail shape theo đúng yêu cầu trong task: `user_create` `{phone, role, storeIds}` (không có password/passwordHash); `user_role_change` chỉ ghi khi `data.role !== undefined && data.role !== target.role` (thực sự đổi), detail `{fromRole, toRole}`; `user_password_reset` cho cả 2 nhánh (tự đổi và owner reset người khác), detail `{selfChange: actor.userId === userId}`.
- Atomicity: `create` trước đây không dùng transaction — bọc thêm `$transaction` quanh `user.create` + `auditLog.create` (giữ nguyên catch `P2002` → 409 ở ngoài). `update` đã có sẵn transaction cho `userStore` + `user.update` — audit ghi bằng `tx` trong cùng transaction đó, không mở transaction thứ hai. `setPassword` trước đây không có transaction — bọc `$transaction` quanh `user.update` + `auditLog.create` (giữ nguyên catch `P2025` → 404 ở ngoài).
- Client: `_auditActionLabel` thêm 3 nhãn (`Tạo nhân viên` / `Đổi vai trò NV` / `Đổi mật khẩu NV`); `_auditDetailLines` thêm nhánh render `fromRole -> toRole` cho `user_role_change` (đúng pattern `oldPriceVnd -> newPriceVnd` của `product_price_change`) và tiện thể thêm dòng vai trò/SĐT cho `user_create`. `flutter analyze lib/features/ledger/ledger_page.dart` sạch.
- Test: mở rộng 3 `it` block sẵn có trong `users.e2e-spec.ts` (tạo nhân viên, PATCH đổi role, đổi mật khẩu) để assert trực tiếp qua `prisma.auditLog.findFirst/findMany` — theo đúng convention của `customers-debt-adjust.e2e-spec.ts`. Có test khẳng định sửa name/storeIds/active **không kèm role** thì không sinh `user_role_change` (chống nhiễu), và đổi role thật thì đúng 1 dòng.

### Ghi chú review P1.3

- Wastage (`_createWastage`) tái dùng thẳng `_showPurchaseLinesDialog` sẵn có thay vì viết dialog mới — một dòng xuất hủy có hình dạng giống hệt một dòng PO (productId + qty, không cần cost), nên chỉ cần gọi với `includeCost` mặc định `false` và `canAddLines: true`. `reasonCode`/`note` là tham số HEADER-level của `recordWastage` (không phải per-line), nên đưa vào qua slot `header:` sẵn có — một `Column` chứa `DropdownButtonFormField<String>` (`spoilage`/`damage`/`other` → "Hư hỏng"/"Hỏng do va chạm"/"Khác") và một `TextField` ghi chú. Chọn cách đơn giản nhất trong các phương án được gợi ý: **biến ngoài (`var reasonCode`) được đóng (closure) trực tiếp bởi `onChanged` của dropdown**, không bọc thêm `StatefulBuilder` quanh header — vì `DropdownButtonFormField` dùng `initialValue` (uncontrolled) nên tự quản lý hiển thị lựa chọn nội bộ, không cần `setState` để cập nhật UI của chính nó, và không có widget nào khác trong dialog phụ thuộc hiển thị vào `reasonCode` để cần rebuild ăn theo. Khác với `toStoreId` trong `_createTransfer` (dùng `setLocal` vì đó là `StatefulBuilder` bọc trực tiếp `AlertDialog`, không có lớp dialog dùng chung để tái sử dụng) — ở đây chỉ cần biến ngoài đọc lại sau khi `showDialog` đóng, đúng idiom `supplierCtrl` đã dùng cho `_createPurchaseOrder`. `note` rỗng → gửi `null` (không gửi chuỗi rỗng) để khớp `String?` optional của `recordWastage`.
- Stocktake: line-semantics khác PO (đếm thực tế so với tồn hệ thống, không phải số lượng nhập/nhận cộng dồn) nên viết dialog mới `_showStocktakeLinesDialog`, dựng theo đúng khung xương của `_showPurchaseLinesDialog` (cùng `AlertDialog` + `StatefulBuilder(setDialogState)` + `Card`-mỗi-dòng + nút "Thêm dòng" + `header:` slot + `finally` dispose) nhưng thay ô "Số lượng"/"Giá nhập dự kiến" bằng: nhãn "Hệ thống: X" (tra `productById[drafts[i].productId]?.qty`, tự cập nhật khi đổi sản phẩm trong dropdown vì đọc lại mỗi `setDialogState`) + ô "Đếm thực tế" (`TextField` số). Không ép buộc chọn `reason`/`reasonNote` theo dòng — đúng như task cho phép bỏ qua phần nice-to-have này để tập trung vào ghi chú kiểm kê ở header (`note` header-level, giống pattern header của wastage, truyền thẳng vào `recordStocktake(note: ...)`); `reasonCode` per-line vẫn được service tự suy ra từ `countedQty` so với `systemQty` (increase/decrease/match) như code cũ, không đổi hành vi đó.
- Khác biệt validate so với `_showPurchaseLinesDialog`: **0 là giá trị đếm hợp lệ** (đếm ra tồn kho = 0 là kết quả kiểm kê thật, không phải "dòng trống" cần bỏ qua kiểu `allowZeroQty`) — chỉ chặn ô trống hoặc số âm (`invalid_qty`), không chặn `== 0`. Giữ nguyên các validate còn lại (trùng sản phẩm trong cùng phiếu → `duplicate_product`, danh sách dòng rỗng → `empty_lines`, bọc `try/catch` trả về snack "Dòng hàng không hợp lệ" giống hệt style cũ).
- `_createWastage`/`_createStocktake` giờ tự fetch `productRepository.listWithStock(storeId)` để có danh sách đầy đủ cho dropdown nhiều dòng, thay vì gọi `_pickProduct()` (chỉ chọn 1 sản phẩm, dùng cho dialog đơn dòng) — dòng đầu tiên của mỗi phiếu khởi tạo bằng `products.first`, người dùng đổi qua dropdown hoặc bấm "Thêm dòng" để thêm sản phẩm khác.
- Không đụng `_createPurchase` (Nhập NCC ngoài PO) và `_createTransfer` — đúng scope task; `git diff` xác nhận chỉ 3 vùng thay đổi: thêm class `_StocktakeLineDraft`, thêm method `_showStocktakeLinesDialog`, và viết lại toàn bộ thân `_createWastage`/`_createStocktake`. Không đụng `inventory_service.dart`, Drift schema, hay `apps/api`.
- Test: không thêm widget test mới cho các dialog (đúng convention hiện có của file — `_createWastage`/`_createStocktake`/`_createTransfer`/`_createPurchaseOrder` đều chưa có widget test, và không có nơi nào dựng `InventoryHubPage` trực tiếp trong `test/`). `flutter analyze` sạch trên `inventory_hub_page.dart` và sạch toàn dự án; `flutter test` 142/142 pass, không đổi so với baseline trước task (không thêm/bớt test nào).

### Ghi chú review P1.1

- Thiết kế wiring: `SyncScheduler` bọc toàn app ở gốc (trên cả login/chọn điểm/mở ca) nên không tự biết "cửa hàng hiện tại". Thêm `GlobalKey<SyncSchedulerState>` tạo ở `main.dart`, gắn vào `SyncScheduler` qua `key:`, rồi truyền xuống như một constructor param bình thường (giống hệt cách `pullCatalog`/`outboxWorker` đã chảy) qua `PosApp` → `LoginPage` → `OpenShiftPage` → `PosPage`. `PosPage.initState()` gọi `syncSchedulerKey?.currentState?.setActiveStore(widget.storeId)` — đây là nơi duy nhất `storeId` chắc chắn đã biết. Field này **nullable, không `required`** ở mọi tầng để không phải sửa các test dựng `PosPage`/`PosApp` trực tiếp mà không bọc `SyncScheduler` (`widget_test.dart`, `pos_page_weighted_dialog_test.dart`...) — 0 file test cũ nào phải đổi.
- Phát hiện thêm 1 đường quay lại `PosPage` mà task ban đầu không nhắc: đóng ca (`close_shift_page.dart`) push lại `OpenShiftPage` (`pushAndRemoveUntil`, cùng app instance, không phải app restart) → có thể mở ca mới cho **cửa hàng khác** trên cùng phiên máy. Nếu không xâu `syncSchedulerKey` qua `CloseShiftPage` → `OpenShiftPage` thì `PosPage` thứ hai không đăng ký được store mới, còn `SyncSchedulerState._activeStoreId` vẫn giữ store cũ → pull nền âm thầm kéo nhầm dữ liệu cửa hàng đã đóng ca. Đã xâu thêm field qua `close_shift_page.dart` (không có trong scope gốc nhưng cùng convention truyền tay hiện có, rủi ro thấp) để tránh bug này. `setActiveStore` được thiết kế idempotent-safe với việc gọi lại nhiều lần: hủy timer cũ, đặt lại `_activeStoreId`, pull ngay + khởi động lại chu kỳ cho store mới.
- Chu kỳ pull nền: `catalogPullInterval = 60s` (hằng số export trong `sync_scheduler.dart`), tách biệt khỏi timer push 15s hiện có — danh mục (giá, tồn, khách hàng...) đổi chậm hơn nhiều so với tốc độ outbox rút hàng đợi, 60s đủ nhanh để thay đổi ở máy khác hiện trong vòng ~1 phút mà không cạnh tranh băng thông với push hay dội server. Cũng pull ngay lập tức khi `setActiveStore` được gọi lần đầu (mirror pattern `unawaited(_tick())` sẵn có trong `initState`), và pull thêm khi app resume nếu đã có store active (mở rộng `didChangeAppLifecycleState` sẵn có).
- An toàn lỗi: `_pullTick()` bọc `try/catch` nuốt mọi lỗi (mirror hệt `_backupTick()`) — pull hỏng (mất mạng, server sập) không bao giờ crash app hay chặn thao tác bán hàng, tự thử lại ở tick kế tiếp.
- Không đụng 3 điểm gọi `pullCatalog.pullCatalog(storeId)` thủ công hiện có (`cash_ledger_page.dart:61`, `pos_page.dart:582` trong nút "Đồng bộ", `product_list_page.dart:48`) — pull nền là **thêm**, không thay thế các lượt pull do người dùng chủ động kích hoạt.
- Test: `SyncSchedulerState` dùng `Timer.periodic` thật + `WidgetsBindingObserver` thật nên chọn widget test (`test/sync_scheduler_test.dart`, file test mới — pattern trước đó không có), tận dụng việc `testWidgets` tự chạy trong fake-clock zone của `flutter_test`: `tester.pump(catalogPullInterval)` để advance timer thay vì cần thêm gói `fake_async` (đã có sẵn transitive qua `flutter_test` nhưng không cần dùng trực tiếp). Gọi thẳng `key.currentState!.setActiveStore(...)` và `key.currentState!.didChangeAppLifecycleState(AppLifecycleState.resumed)` qua `GlobalKey` thay vì giả lập kênh lifecycle của OS — đơn giản hơn và đúng cái cần kiểm (logic trong `SyncSchedulerState`, không phải kênh platform channel). Dùng danh sách `pullCalls` tự ghi nhận trong `thenAnswer` thay vì chuỗi `verify(...).called(n)` lặp lại của mocktail (mocktail không "reset" số đếm giữa các lần `verify` như có thể lầm tưởng — số đếm là cộng dồn từ đầu mock) để tránh sai số đếm khi so sánh nhiều mốc thời gian khác nhau trong cùng 1 test. 5 test: pull ngay + định kỳ, không pull khi chưa có store active, resume chỉ pull khi có store active, đổi store thì nhắm lại đúng store mới, và lỗi pull bị nuốt mà tick kế tiếp vẫn chạy.
- Không đụng `apps/api` — `GET /sync/pull` không đổi, task thuần Flutter nên bar là `flutter test` + `flutter analyze`, không cần e2e NestJS mới.

### Ghi chú review P1.2

- Endpoint: chọn **dedicated `PATCH :id/allow-negative-stock`** (không gộp vào `PATCH :id` chung) — nhất quán với tiền lệ `setDebtOverdueDays` (single-field-dedicated-endpoint, cũng dùng `assertStoreManage`), chứ không theo `setVat` (2-field, dùng khi nhiều field liên quan cùng đổi 1 lượt). `allowNegativeStock` chỉ có 1 field nên endpoint riêng đơn giản hơn là nhét thêm nhánh vào `validateStoreBody` của `PATCH :id` (vốn đang owner-only qua `assertOwner`, sai quyền cần cho task này). Thêm `allowNegativeStock: true` vào `storeSelect` nên `GET /stores` trả kèm field này cho mọi route hiện có (không cần sửa gì thêm ở đó).
- Migration: `apps/api/prisma/migrations/20260727120000_p1_2_allow_negative_stock/migration.sql` — copy nguyên idiom từ `20260725120000_phase3_vat` (`ALTER TABLE "Store" ADD COLUMN IF NOT EXISTS "allowNegativeStock" BOOLEAN NOT NULL DEFAULT false;`), chạy `npx prisma generate` để build `@prisma/client` (không chạy `migrate dev` được — DB vẫn unreachable từ máy này, xem "Chặn môi trường"). `npm run build` + `npm run test:unit` (9 suite/69 test) đều pass.
- Client — vị trí lưu: **không dùng `metaValue`** (đúng yêu cầu task) mà thêm cột thật vào `StoresLocal` (Drift), vì đây là cấu hình theo **từng cửa hàng** (không phải theo máy) và phải có sẵn offline cho quyết định checkout/trừ kho — giống hệt cách `debtOverdueDays`/`largeDebtThresholdVnd` đã làm trước đó. `schemaVersion` 10 → 11, `if (from < 11) { await migrator.addColumn(storesLocal, storesLocal.allowNegativeStock); }`. Codegen bằng `flutter pub run build_runner build` (cờ `--delete-conflicting-outputs` bị bản `build_runner` hiện tại báo "removed and ignored" nhưng build vẫn ghi đè file cũ bình thường — không cần cờ đó nữa) — `database.g.dart` đã có `allowNegativeStock` ở `$StoresLocalTable`, `StoresLocalData`, `StoresLocalCompanion`.
- Hai điểm đọc (`checkout_service.dart` dòng ~104, `inventory_service.dart` dòng ~79) đổi từ `(await _db.metaValue('allowNegativeStock')) == 'true'` (luôn `false` vĩnh viễn vì không ai từng gọi `setMetaValue` cho key này) sang truy vấn `storesLocal` theo `storeId` đã có sẵn trong scope, mặc định `false` khi thiếu dòng (giữ hành vi an toàn cũ khi chưa pull lần nào). Không đụng logic `allowNegative`/`requireNonNegative` ở tầng dưới, không đụng `metaValue`/`setMetaValue` cho các key khác (`fcmToken`, `currentStoreId`...).
- UI: sheet mới `apps/pos_app/lib/features/inventory/allow_negative_stock_sheet.dart`, dựng theo mẫu `debt_settings_sheet.dart` (đọc/ghi trực tiếp `storesLocal` theo `storeId` hiện tại + PATCH server rồi ghi đè local ngay — write-through) chứ không theo `vat_settings_sheet.dart` (load cả danh sách store qua `GET /stores` sống, hợp với màn kế toán đa điểm bán nhưng thừa cho 1 công tắc theo cửa hàng đang đứng, và không cần thiết vì giá trị phải có ngay offline không đợi vòng pull kế tiếp). Gắn vào `IconButton` trong `AppBar.actions` của `InventoryHubPage` (mục "Kho"), gate theo `canTransfer` (owner/store_manager, biến đã có sẵn trong page) — cùng vị trí logic với nút "Tồn hiện tại". `InventoryHubPage` cần thêm `dio` (trước đây không có Dio) để sheet gọi PATCH được; lấy từ `widget.dayReportRepository.dio` tại call site duy nhất (`pos_page.dart` dòng ~751, phần khởi tạo `InventoryHubPage` khi bấm icon "Kho" — không phải đoạn dialog mật khẩu/initState của P1.6/P1.1). Không có test dựng `InventoryHubPage` trực tiếp nên thêm param bắt buộc không phá test nào.
- Test client: sửa 1 test cũ dựa vào stub chết — `checkout_service_test.dart` ("checkout allows negative stock...") trước đây gọi `db.setMetaValue('allowNegativeStock', 'true')`, đổi thành insert dòng `storesLocal` cho `store-1` với `allowNegativeStock: true` (đúng cơ chế mới). `inventory_service_test.dart` đã insert sẵn `storesLocal` cho mỗi test (không set `allowNegativeStock` → mặc định `false` qua `withDefault`) nên không cần sửa. `flutter test` 142/142 pass, `flutter analyze` sạch trên toàn bộ file đụng tới.
- Test server: mở rộng `stores.e2e-spec.ts` — thêm assertion `allowNegativeStock: false` mặc định vào test tạo/list store sẵn có; thêm test owner PATCH bất kỳ store nào (kèm case body sai kiểu → 400); thêm test store_manager (tạo qua `POST /users` theo đúng convention `users.e2e-spec.ts`, scoped 1 store) — PATCH cửa hàng khác bị 403 (và DB không đổi), PATCH đúng cửa hàng mình được 200. `afterEach` dọn user test theo prefix `09502` và trả CH1/CH2 về `allowNegativeStock: false` để không rò rỉ state sang spec khác. Không chạy được thật (DB unreachable — xem "Chặn môi trường"); đã xác nhận file **type-check sạch** qua `npx jest --config ./test/jest-e2e.json -t "zzz_never_matches_zzz" test/stores.e2e-spec.ts` (exit 0, ts-jest transpile+type-check chạy ở bước load module, không cần kết nối DB vì `-t` không khớp gì nên Jest bỏ qua toàn bộ `beforeAll`/`it` trong suite).

### Ghi chú review P1.4

- **Cách xác minh e2e cục bộ (lần đầu trong session này, không phụ thuộc "Chặn môi trường")**: dựng một `postgres:15` container Docker tay, publish ra cổng host **15432** thay vì 55422 (dải cổng dev-machine bị Windows chiếm — xem "Chặn môi trường" ở trên) — cổng 15432 nằm ngoài dải bị chiếm nên bind thành công:
  ```
  docker run -d --name tap-hoa-ci-smoke -p 15432:5432 -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=tap_hoa postgres:15
  ```
  rồi trỏ `DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:15432/tap_hoa?schema=public` và `TAP_HOA_SKIP_LOCAL_IDENTITY=1` (bỏ qua khóa `tap-hoa.identity.ts` chỉ trói cổng 55422 — đúng mục đích thiết kế của cờ này cho DB tạm/CI, không phải lách luật). Chạy `npx prisma migrate deploy` (áp hết 26 migration sạch) → `npx prisma db seed` (owner `0900000001`/`123456` + CH1/CH2 + chart of accounts) → `npm run test:e2e`. Đây chính xác là setup mà GitHub Actions Ubuntu runner sẽ làm với service container — cổng khác nhau (15432 cục bộ vs 5432 trên CI) không đổi hành vi vì app không hard-code cổng, chỉ đọc `DATABASE_URL`. Đã lặp lại **7 lần** trên DB tạo mới hoàn toàn mỗi lần (`DROP DATABASE` + `CREATE DATABASE` + migrate + seed) để loại trừ nhiễu từ state cũ và bắt lỗi phụ thuộc thứ tự file — Jest không đảm bảo thứ tự chạy file ổn định giữa các lần (xác nhận thực tế: nhiều thứ tự file khác nhau giữa các lần chạy), cộng thêm vài lần chạy có kiểm soát thứ tự (ép 2 file chạy tách rời theo đúng thứ tự nghi vấn) để xác minh trực tiếp từng bug rò rỉ state — xem chi tiết bug #3/#4 bên dưới.
  - Lưu ý môi trường: `npx prisma db seed` bị hook `rtk` (Rust Token Killer, cấu hình global của máy — không liên quan repo) chặn nhầm ở bước resolve binary `prisma`; vòng qua bằng `rtk proxy npx prisma db seed`. Không liên quan gì đến CI (GitHub Actions runner không có hook này).
- **2 bug test đã sửa (đúng root cause orchestrator đã tìm)**:
  1. `test/reports.e2e-spec.ts` — `'GET /reports/day breaks revenue down by shift for a store date'`: thêm `closedAt: new Date('2026-07-25T07:00:00.000Z')` vào `shiftA` (trước đó thiếu, khiến 2 ca cùng `storeId`+`userId` cùng mở → vi phạm unique index partial `Shift_storeId_userId_open_key` — "chỉ 1 ca mở/user/điểm bán"). Fixture giờ thực tế hơn: 2 ca kế tiếp nhau trong ngày thay vì 2 ca mở đồng thời (điều không thể xảy ra thật vì `/shifts/open` đi qua đúng constraint này).
  2. `test/cash-fund-consistency.e2e-spec.ts` — `'netCashVnd của /reports/cash-fund bằng số dư TK 111...'`: dời `const at = new Date(); const nowIso = ...; const { periodYm, from, to } = ictPeriodRange(at);` xuống **sau** lệnh `POST /shifts/open` thay vì trước. Trước đây `nowIso` chụp mốc thời gian sớm hơn `shift.openedAt` (server tự gán) một khoảng bằng độ trễ round-trip HTTP, khiến 13 chứng từ dùng `nowIso` làm `clientCreatedAt` bị `sync.service.ts` (~dòng 900-905) từ chối với `shift_not_open_at_sale`. Không đụng test thứ hai trong cùng file (đã tự có `at`/`nowIso` riêng, đã pass từ trước).
- **Bug thứ 3 tự phát hiện trong lúc xác minh (không nằm trong 3 bug orchestrator liệt kê ban đầu) — cùng lớp lỗi "rò rỉ state kỳ hiện tại giữa các file test"**: `test/ledger.e2e-spec.ts` tạo `PeriodLock` cho **kỳ thật của tháng hiện tại** (`periodYm` suy từ `new Date()`) qua `POST /ledger/period-locks` (dòng ~137-141) nhưng **không** dọn lại — không unlock, không `afterAll`, không `deleteMany` sau đó. Vì `--runInBand` chạy tuần tự cùng 1 DB, khóa này rò rỉ sang **mọi file chạy sau nó** trong cùng lượt test có chứng từ "now" trong kỳ thật đó → bút toán của chúng bị `LedgerService.safePost` nuốt lỗi âm thầm (ghi log + audit `journal_post_failed`, không throw ra ngoài). Bắt được qua lần chạy full-suite trên DB sạch: `test/supplier-return.e2e-spec.ts` fail vì `journalEntry` tra theo `sourceType_sourceId: {supplier_return, ...}` trả `null` — root cause xác nhận bằng cách đọc `ledger.service.ts::safePost` (nuốt lỗi, không rethrow) và thấy `ledger.e2e-spec.ts` là file DUY NHẤT tạo period-lock cho kỳ "now" mà thiếu dòng dọn dẹp (so với 12 file khác cũng đụng `periodLock` đều có `deleteMany`/unlock tương ứng). Sửa: thêm `await prisma.periodLock.deleteMany({ where: { periodYm } });` vào cuối test, đúng convention đã dùng ở `cash-fund-consistency.e2e-spec.ts`, `wastage-journal.e2e-spec.ts`, `phase3-hardening.e2e-spec.ts`... — không đụng gì khác trong file.
- **Điều tra `bank-recon.e2e-spec.ts` — TÌM RA root cause thật (không phải bug #3 ở trên)**: 4 lần chạy full-suite đầu (DB sạch, thứ tự file khác nhau) đều pass `bank-recon.e2e-spec.ts`, nhưng lần chạy full-suite **thứ 5** (dùng làm "final check" theo DoD #3) tái hiện đúng lỗi gốc của orchestrator: `bookTotalVnd: 50000 expected, 10000 got`, với thứ tự file lần đó có `ap-recon.e2e-spec.ts` chạy ngay trước `bank-recon.e2e-spec.ts`. Đã kiểm tra và loại bỏ giả thuyết "cùng nguyên nhân với bug #3" (khóa kỳ) bằng cách đọc `reports.service.ts::loadBankBook`: hàm nguồn `bookTotalVnd` đọc thẳng bảng `Sale`/`CashVoucher`/`SupplierPayment`, **không** qua `JournalEntry`, nên khóa kỳ rò rỉ (chỉ chặn *post sổ cái* qua `safePost`, không chặn tạo dòng gốc) không thể là nguyên nhân. Root cause thật: `test/ap-recon.e2e-spec.ts` tạo thẳng qua `prisma.supplierPayment.create()` (dòng ~70-80) một `SupplierPayment` **channel `'transfer'`, `amountVnd: 40_000`, `storeId` = CH1, `clientCreatedAt` = ngày 15 của kỳ thật hiện tại** — và không dọn lại (không `afterAll`, không `deleteMany` cuối test). `loadBankBook` cộng supplier payment kênh transfer với dấu **âm** (`amountVnd: -p.amountVnd`, dòng 1098 `reports.service.ts`) vào `bookTotalVnd`. Khi `ap-recon.e2e-spec.ts` chạy trước `bank-recon.e2e-spec.ts` trong cùng lượt `--runInBand` (cùng DB, cùng CH1, cùng tháng thật): `50.000` (sale chuyển khoản của chính bank-recon) `− 40.000` (supplier payment rò rỉ từ ap-recon) `= 10.000` — khớp **chính xác** với cả số orchestrator báo cáo lẫn số tôi tái hiện được. Xác minh trực tiếp: chạy `ap-recon.e2e-spec.ts` rồi `bank-recon.e2e-spec.ts` như 2 lệnh Jest **tách rời, cùng 1 DB** (ép đúng thứ tự lỗi, không phụ thuộc sequencer của Jest) — trước khi sửa tái hiện lỗi `Received: 10000`; sau khi thêm `prisma.supplierPayment.deleteMany({ where: { supplierId: supplier.id } })` + `prisma.supplierPayable.deleteMany(...)` vào cuối test của `ap-recon.e2e-spec.ts`, chạy lại đúng cặp lệnh tách rời đó **pass sạch**, và xác nhận bằng SQL trực tiếp `SELECT count(*) FROM "SupplierPayment" WHERE channel='transfer'` → `0` sau khi `ap-recon.e2e-spec.ts` chạy xong. Đã rà thêm các nơi khác tạo thẳng `Sale`/`CashVoucher`/`SupplierPayment` qua Prisma trong `test/*.e2e-spec.ts` (`phase1-polish`, `einvoice-http`, `cash-vouchers`) — không có leak tương tự (đều `transferAmount: 0` hoặc được `beforeEach`/test kế tiếp trong cùng file dọn sạch trước khi file khác chạy).
- **Xác nhận đóng vòng cho các task "chờ e2e (DB blocked)" trước đó trong session**: Đây là lần ĐẦU TIÊN trong session này e2e chạy thật với DB sống (không phải type-check suông như ghi chú P1.2). Sau khi sửa đủ **4 bug test** (2 bug gốc orchestrator tìm + bug #3 `ledger.e2e-spec.ts` period-lock rò rỉ + bug #4 `ap-recon.e2e-spec.ts` SupplierPayment rò rỉ — root cause thật của mismatch bank-recon), full suite **37/37 suite, 122/122 test pass** trên **7 lần chạy full-suite DB-sạch khác nhau** (thứ tự file mỗi lần một khác, xác nhận qua thứ tự PASS in ra) — bao gồm mọi spec chạm code của P0.1 (`users.e2e-spec.ts`), P0.3 (`cash-fund-consistency.e2e-spec.ts`), P0.4 (`reports-permissions.e2e-spec.ts`, `permissions.e2e-spec.ts`), P1.5 (`debt-payments.e2e-spec.ts`, `sync-push.e2e-spec.ts`), P1.6 (đổi mật khẩu trong `users.e2e-spec.ts`), P1.7 (audit log trong `users.e2e-spec.ts`), P1.2 (`stores.e2e-spec.ts`). **Không** phát hiện lỗi nào khác liên quan đến code mới của các task đó — cả 4 bug tìm thấy đều là bug trong chính file test (dữ liệu test không tự dọn, rò rỉ sang file khác), không phải bug ứng dụng, và không nằm trong phạm vi các task P0.1/P0.3/P0.4/P1.5/P1.6/P1.7/P1.2. Có thể nói dứt khoát: **mọi claim "Code xong — chờ e2e" của các task trên nay đã được xác minh thật, xanh toàn bộ.**
- **CI**: `.github/workflows/ci.yml` job `api` thêm block `services.postgres` (image `postgres:15`, `ports: ['5432:5432']`, health-check `pg_isready`) + `env` cấp job (`DATABASE_URL` trỏ `localhost:5432`, `TAP_HOA_SKIP_LOCAL_IDENTITY: '1'`) + 3 step mới sau "Unit tests": "Migrate database" (`npx prisma migrate deploy`), "Seed database" (`npx prisma db seed`), "E2E tests" (`npm run test:e2e`). Không đụng job `flutter`, không đụng thứ tự/nội dung step "Build"/"Unit tests" sẵn có. Không thể chạy thật GitHub Actions để xác nhận (ngoài khả năng của agent), nhưng cú pháp `services:`/`ports:`/`options:` đúng idiom chuẩn GH Actions cho Postgres service container (không dùng container ở cấp job nên step chạy trực tiếp trên runner host, kết nối qua `localhost` — đúng, không phải `postgres` hostname vốn chỉ dùng khi job cũng chạy trong `container:`).
- Container Docker `tap-hoa-ci-smoke` dùng để xác minh đã được dừng + xoá sau khi hoàn tất task (không để lại tài nguyên chạy nền).

### Ghi chú review P2.1

- **Phạm vi lỗi thật**: `OutboxEntries` (`tables.dart`) trước đây chỉ có `status`/`lastError`, không có gì đếm số lần thử hay giữ thời điểm retry tiếp theo. Nhánh `on DioException` trong `outbox_worker.dart::tick()` (lỗi HẠ TẦNG — mất mạng, server sập, timeout) trước đây không làm gì (`// stay pending; do not throw to UI`) → tick 15s sau lại gửi y hệt batch đó, dội server vô thời hạn nếu outage kéo dài hàng giờ. Nhánh từ chối NGHIỆP VỤ (`rejected*` → `markOutboxError` → `status='error'`) đã đúng từ trước và **không đụng tới** trong task này — đây là ranh giới quan trọng nhất phải giữ: `error` = cần người sửa dữ liệu; `dead_letter` (mới) = hạ tầng hỏng tạm thời/kéo dài, không có gì sai trong payload.
- **Công thức backoff** (`outbox_worker.dart`, hằng số `outboxBackoffBase`/`outboxBackoffCap`/`outboxMaxRetries` + hàm `outboxBackoffDuration`): cấp số nhân, base **15s** (khớp đúng chu kỳ `Timer.periodic(Duration(seconds: 15))` đẩy `tick()` trong `sync_scheduler.dart` — lần backoff đầu tiên sau 1 lỗi hạ tầng không khác gì hành vi cũ, chỉ giãn ra từ lần lỗi liên tiếp thứ hai), nhân đôi mỗi lần, trần **8 phút** (đạt trần đúng ở lần thử thứ 6: `15×2^5 = 480s`). 8 phút nằm giữa khoảng 5-10 phút hợp lý task gợi ý: đủ dài để giảm >97% số request/giờ so với 15s cố định trước đây trong một outage kéo dài, đủ ngắn để máy không "im lặng" quá lâu khi mạng/server hồi phục.
- **Ngưỡng dead-letter**: `outboxMaxRetries = 10`. Với công thức trên, tổng thời gian chờ cộng dồn qua 10 lần retry ≈ `15+30+60+120+240 + 480×5 = 2865s` (~48 phút) trước khi lần thử thứ 11 (nếu cũng lỗi) mới thực sự chuyển `dead_letter` — dưới 1 giờ tính từ lỗi đầu tiên. Chọn 10 (giữa khoảng 8-15 task gợi ý): thấp hơn sẽ dead-letter nhầm một đợt mất mạng vài-chục-phút (thường gặp với mạng cửa hàng nhỏ) dù nó tự khỏi; cao hơn sẽ để một entry hỏng THẬT (vd. server đổi API breaking, lỗi không bao giờ tự hết) nằm im nhiều giờ không ai biết cần vào sửa. Ngưỡng so sánh SAU khi tăng `retryCount` (đúng yêu cầu task): lần lỗi thứ 11 làm `retryCount=11 > 10` mới dead-letter, không phải ở lần thứ 10.
- **Cơ chế backoff không cần scheduler riêng**: `pendingOutbox()` (`database.dart`) tự lọc thêm `(nextRetryAt IS NULL OR nextRetryAt <= now)` — một entry đang chờ backoff đơn giản bị loại khỏi batch của `tick()` kế tiếp cho tới khi tới hạn, dùng đúng idiom nullable-DateTime đã có trong codebase (`column.isNull() | column.isSmallerOrEqualValue(now)`, cùng kiểu với `shift_repository.dart::closedAt.isNull()`). Không cần Timer/Isolate mới, không đụng `sync_scheduler.dart` (giữ nguyên tick 15s cố định — bản thân backoff nằm hoàn toàn ở tầng lọc dữ liệu, không phải ở tầng lịch trình).
- **DB method mới `recordOutboxInfraFailure`** (`database.dart`) nhận thẳng `List<OutboxEntry>` — chính là biến `pending` mà `tick()` đã fetch ở đầu hàm (đúng batch VỪA gửi thất bại, không phải re-query lại theo entityType+payload id như `markOutboxError`/`markOutboxEntitiesDone` phải làm vì chúng chỉ nhận id thô từ response JSON của server). Vì `pending` đã có sẵn `retryCount` hiện tại của từng dòng và không có ai ghi xen vào giữa lúc fetch và lúc catch trong cùng một lượt `tick()` (không có concurrency ở đây), dùng thẳng `entry.retryCount` làm mốc mà không cần SELECT lại từng dòng.
- **Migration schema 11 → 12 — lệch so với hướng dẫn ban đầu, có lý do**: đề bài gợi ý chỉ 2 dòng `migrator.addColumn(...)` cho `retryCount`/`nextRetryAt`, theo đúng khuôn các block `if (from < N)` trước đó. Đã làm đúng 2 dòng đó, NHƯNG thêm 1 dòng `await migrator.alterTable(TableMigration(outboxEntries));` ngay sau — lý do: `addColumn` chỉ chạy `ALTER TABLE ADD COLUMN`, KHÔNG cập nhật CHECK constraint đã tồn tại trên cột `status` (SQLite không hỗ trợ sửa CHECK bằng ALTER trực tiếp). Nếu dừng ở 2 dòng addColumn, máy cài đặt từ TRƯỚC (upgrade từ schema < 12) vẫn giữ CHECK constraint cũ 3 giá trị `('pending','error','done')` — `UPDATE status='dead_letter'` sẽ ném `SqliteException` CHECK constraint failed CHỈ trên những máy đã cài trước đó (máy cài mới qua `onCreate`/`createAll()` đọc thẳng schema hiện tại trong `tables.dart` nên không sao — nghĩa là lỗi này sẽ không lộ ra khi test bằng `NativeDatabase.memory()` như toàn bộ test suite hiện có, vì test luôn đi qua `onCreate`, không bao giờ qua `onUpgrade`). `TableMigration` (không kèm `newColumns`, vì 2 cột đã tồn tại sẵn sau 2 dòng addColumn ngay phía trên) recreate bảng theo đúng "12-bước" `sqlite.org/lang_altertable.html#otheralter` mà package `drift` (`Migrator.alterTable`, `drift-2.34.2/lib/src/runtime/query_builder/migration.dart`) đã cài sẵn — giữ nguyên toàn bộ dữ liệu cũ, chỉ đổi CHECK constraint sang bản khớp `OutboxEntries` hiện tại trong `tables.dart`. Đã xác minh bằng một script thủ công (không nằm trong test suite, xoá sau khi dùng): dựng tay 1 file sqlite schema-11 thật (CHECK 3 giá trị, không có `retry_count`/`next_retry_at`) + 1 dòng `pending`, mở qua `AppDatabase` thật để chạy đúng đường `onUpgrade(11, 12)` — xác nhận dòng cũ được giữ nguyên với `retryCount=0`/`nextRetryAt=null` sau migrate, và `UPDATE status='dead_letter'` sau đó THÀNH CÔNG (không có bước `alterTable` thì bước này sẽ throw).
- **`requeueOutbox` mở rộng** để reset `retryCount: 0`, `nextRetryAt: null` (ngoài `status`/`lastError` như cũ) — không reset sẽ khiến một entry `dead_letter` được bấm "Thử lại" mà lỡ gặp đúng 1 lỗi hạ tầng nữa dead-letter lại NGAY (vì `retryCount` đã sẵn ở 11, vượt ngưỡng từ trước), rất khó hiểu với người dùng vừa chủ động thử lại. Áp dụng chung cho MỌI đường requeue (không chỉ nút "Thử lại" của conflicts page) vì tất cả đều đi qua `AppDatabase.requeueOutbox` — bao gồm cả `OutboxConflictService.saveRawJson`/`saveProductUpsertIdentity`/`saveInsufficientStockQtys`, dù các đường đó chủ yếu dùng cho `status='error'` (retryCount vốn đã 0) nên hành vi không đổi ở đó, chỉ thực sự có tác dụng mới khi entry đang là `dead_letter`.
- **`lastError` cho dead-letter dùng reason CODE, không phải câu tiếng Việt thô**: đặt `outboxSyncRetryExhaustedReason = 'sync_retry_exhausted'` (const trong `database.dart`) — đúng convention hiện có của cột này (`'insufficient_stock'`, `'sku_conflict'`...), giữ nguyên tắc "lastError chứa mã lỗi ổn định, UI tự dịch" thay vì rò rỉ câu tiếng Việt hay message `DioException` thô vào tầng dữ liệu. Thêm đúng 1 case vào `labelOutboxReason` (`outbox_reason_labels.dart`, KHÔNG đụng các case nghiệp vụ có sẵn) → "Không đồng bộ được sau nhiều lần thử — kiểm tra kết nối rồi thử lại".
- **Mở rộng UI conflicts hiện có, không tạo màn mới**: `listOutboxErrors()`/`watchOutboxErrorCount()` (`database.dart`) đổi `WHERE status = 'error'` thành `status IN ('error', 'dead_letter')` — gộp chung 1 danh sách cho `OutboxConflictsPage` thay vì tách hai màn, vì cả hai đều cần một người xem qua. `OutboxConflictService.listErrors()` không đổi gì (đã gọi thẳng `_db.listOutboxErrors()`, tự động ăn theo). `outbox_conflicts_page.dart` phân biệt trực quan bằng `entry.status == 'dead_letter'`: icon `Icons.wifi_off` + màu `colorScheme.tertiary` (thay vì `Icons.error_outline` + `colorScheme.error` như cũ) và thêm dòng "Đã tự thử lại N lần" (đọc `entry.retryCount`) dưới nhãn lý do — giữ nguyên 2 nút "Sửa"/"Thử lại" sẵn có cho mọi entry (không ẩn "Sửa" dù về mặt khái niệm không thật cần thiết cho dead-letter, vì `outbox_edit_sheet.dart` không nằm trong scope task và ẩn/hiện có điều kiện thêm một nhánh UI không bắt buộc để đạt DoD).
- **Cố tình KHÔNG đụng `watchSyncStatus()`** (`database.dart`, dùng cho banner "Lỗi:" ở đầu `PosPage` qua `sync_status_banner.dart`) dù về mặt khái niệm dead-letter cũng là một dạng "lỗi đồng bộ" đáng hiện ở đó — DoD/đề bài chỉ yêu cầu hiện ra ở "màn hình xung đột hiện có" (`OutboxConflictsPage`), không nhắc `sync_status_banner.dart`; để trong scope task, không mở rộng thêm một mặt UI khác không được yêu cầu. Nợ ghi nhận lại đây cho ai muốn làm tiếp: nếu cần dead-letter cũng đẩy banner đầu trang, chỉ cần đổi `watchSyncStatus()`'s `WHERE` tương tự.
- **Test**: mở rộng bộ test có sẵn trong `outbox_worker_test.dart` (142 → 146, đúng baseline cộng 4 test mới, `flutter analyze` sạch) — test DioException cũ được mở rộng thêm assertion `retryCount`/`nextRetryAt` (so khoảng 10-20s quanh `outboxBackoffBase`, không so mốc mili giây tuyệt đối, đúng như task yêu cầu vì `DateTime.now()` không injectable trong codebase này); thêm 4 test mới: (1) entry có `nextRetryAt` tương lai bị loại khỏi `pendingOutbox()`/batch tick kế tiếp — xác minh gián tiếp qua `dio.post` chỉ bị gọi đúng 1 lần dù `tick()` chạy 2 lần liên tiếp; (2) lỗi hạ tầng liên tiếp vượt `outboxMaxRetries` chuyển `dead_letter` — set thẳng `retryCount = outboxMaxRetries` qua DB trước khi tick (tránh phải đợi backoff thật nhiều vòng lặp trong test); (3) entry `dead_letter` biến mất khỏi `pendingOutbox()` nhưng vẫn có trong `listOutboxErrors()`; (4) `requeueOutbox` trên entry `dead_letter` đưa nó về `pendingOutbox()` với `retryCount=0`/`nextRetryAt=null`. Không thêm test migration vào bộ test chính thức (task không yêu cầu, và toàn bộ test suite hiện tại dùng `NativeDatabase.memory()` nên không bao giờ đi qua `onUpgrade` để test migration một cách tự nhiên) — xác minh migration bằng script thủ công riêng như mô tả ở trên, không phải một phần của `flutter test`.

### Ghi chú review P2.2

- **Sổ quỹ tổng — quyết định response shape**: chọn phương án "cả hai" thay vì grand-total-only. `cashFundSummary` giờ nhận `storeId?` và luôn dùng `resolveStoreIds` (sẵn có, dùng chung với `dayReport`/`debtAging`) để suy ra danh sách điểm bán theo scope role. 6 query song song đổi từ `where: { storeId, ... }` sang `where: { storeId: { in: storeIds }, ... }`, cộng thêm `storeId: true` (và với `journalLine` là `entry: { select: { storeId: true } }`) vào từng `select` — đủ để, ngoài tổng gộp `netCashVnd`/`ledgerNetCashVnd`/`ledgerDiffVnd` như cũ ở top-level, tính thêm `byStore: [{ storeId, ...totals, ledgerNetCashVnd, ledgerDiffVnd }]` bằng cách lọc lại 6 mảng đã fetch theo từng `storeId` (không query lại DB lần hai) — hoá ra không "awkward" như lo ngại ban đầu vì `computeCashFundTotals`/`sumLedgerMovement` nhận flat array, filter theo storeId rồi gọi lại là đủ. `byStore` luôn được trả (kể cả khi `storeId` cụ thể được truyền — khi đó chỉ có đúng 1 phần tử khớp top-level) để một API shape nhất quán, không rẽ nhánh theo có/không `storeId`. Đây là field CỘNG THÊM, không đổi field cũ nào — test `cash-fund-consistency.e2e-spec.ts` gốc (dùng `expect(fund.body.xxx)` rời rạc, không `toEqual` toàn object) không cần sửa.
- **Category → TK — vị trí endpoint**: đặt trong `ledger.controller.ts` (không tạo controller `apps/api/src/cash/` mới) vì `LedgerController` đã có sẵn `LedgerPermissionGuard` ở mức controller (mọi route đòi `canLedger`) và đã có tiền lệ y hệt cho "cấu hình kế toán owner-only" là period-locks (`lockPeriod`/`unlockPeriod` ném `Error('owner_required')`, `mapError` map sang 403) — `setCashCategoryAccount` theo đúng pattern đó, không cần cơ chế mới. Thêm `GET /ledger/cash-categories` (đọc, cần `canLedger` — đủ cho mọi role được vào `LedgerController`) và `POST /ledger/cash-categories/:id/account` (ghi, owner-only qua check trong service, không phải qua guard riêng). Validate TK: tồn tại (`Account.findUnique`) **và** `active: true`, thiếu 1 trong 2 → `account_not_found` → 400; danh mục không tồn tại → `category_not_found` → 404 (thêm case mới vào `mapError`). Cho phép `accountCode: null` để gỡ map (quay lại fallback cứng cũ) — không bắt buộc trong đề bài nhưng đối xứng tự nhiên với việc set, và hữu ích khi owner set nhầm TK.
- **`buildCashVoucherJournal`**: thêm tham số optional `categoryAccountCode`, `counterAcct = categoryAccountCode ?? (direction === 'in' ? '711' : '642')` — chỉ đổi TÊN tài khoản đối ứng, không đổi cấu trúc bút toán (vẫn 2 dòng, vẫn cân bằng qua `assertBalanced`). `LedgerService.postFromCashVoucher` include thêm `category: { select: { accountCode: true } }` khi load voucher rồi truyền `row.category?.accountCode ?? null` — danh mục chưa map (`accountCode = null`) giữ nguyên hành vi 711/642 cũ, không cần thay đổi gì thêm ở caller. Không thêm `@relation` Prisma cho `CashCategory.accountCode` (dù `JournalLine.accountCode` có FK thật tới `Account.code` — nghĩa là nếu lỡ map một mã không tồn tại thì `journalEntry.create` sẽ tự ném lỗi FK ở tầng DB, một lớp an toàn kép ngoài validate ở tầng service) — đúng theo đúng snippet migration được giao (`ADD COLUMN IF NOT EXISTS "accountCode" TEXT;`, không FK), giữ additive tối thiểu.
- **Bút toán bank recon — đi đúng đường CashVoucher-nullable-shiftId đã gợi ý**, không rẽ sang `postEntry` trực tiếp: `ReportsService.createBankReconEntry` suy `direction` từ dấu `amountVnd` của `BankStatementLine` (dương → `in`, âm → `out`), validate danh mục tồn tại + đúng chiều, tạo `CashVoucher` (`channel: 'transfer'`, `shiftId: null`, `amountVnd: Math.abs(...)`, `clientCreatedAt: st.bookedAt` — không phải `now()`, để khớp đúng ngày statement khi `loadBankBook` lọc theo khoảng ngày của kỳ), post qua `LedgerService.postFromCashVoucher` (tự ăn theo map TK của P2.2), rồi gọi thẳng `this.matchBankLine(user, { ..., bookRef: `voucher:${voucherId}` })` — tái dùng 100% logic khớp/khoá/check-lock sẵn có, `loadBankBook` không đổi 1 dòng nào vì nó vốn đã gom `channel: 'transfer'` CashVoucher vào "book". `ReportsModule` import `LedgerModule` (đã export `LedgerService`) để inject — không có cycle vì `LedgerModule` chỉ phụ thuộc `PrismaModule`.
- **`shiftId` nullable — đã rà những nơi lọc theo shiftId trên CashVoucher** (`grep '\.shiftId' apps/api/src`): `shifts.service.ts::loadShiftCashInputsWithClient` dùng `where: { shiftId }` (equality match với giá trị cụ thể của ca đang đóng) — SQL/Prisma equality không bao giờ khớp `NULL`, nên voucher bank-recon tự động bị loại khỏi tính `expectedCashVnd` khi đóng ca, không cần sửa gì. `reports.service.ts::cashFundSummary`/`loadBankBook` chỉ lọc theo `storeId`+khoảng ngày, không đụng `shiftId` — voucher null-shiftId lên đủ cả hai, đúng ý đồ (sổ quỹ tổng phải thấy khoản phí NH này; bank recon vốn là nơi sinh ra nó). Không có `include: { shift: true }`/inner-join ngầm nào trên CashVoucher trong toàn bộ `apps/api/src` (chỉ có field `shift Shift? @relation(...)` khai báo quan hệ optional, không ai `include` nó) nên không có nguy cơ join ngầm loại bỏ hàng null.
- **Hệ quả phụ phải xử lý ở Flutter (không nằm trong hướng dẫn gốc nhưng bắt buộc để không vỡ app)**: `/sync/pull` trả về MỌI `CashVoucher` của điểm bán (không lọc theo shiftId), nên voucher bank-recon (`shiftId: null`) sẽ được kéo về máy POS qua `cashVouchers` payload. `AppDatabase.upsertCashVoucher` (`database.dart`) trước đây `voucher['shiftId'] as String` — ép kiểu `null as String` sẽ crash (`TypeError`) ngay khi pull. Đã sửa: cột `CashVouchersLocal.shiftId` (`tables.dart`) đổi `text()()` → `text().nullable()()`, `upsertCashVoucher` đổi sang `Value(voucher['shiftId'] as String?)`; `schemaVersion` 12 → 13 kèm `migrator.alterTable(TableMigration(cashVouchersLocal))` (giống hệt cách xử lý CHECK constraint của P2.1 — SQLite không hỗ trợ `ALTER COLUMN ... DROP NOT NULL` trực tiếp, phải recreate bảng theo 12-bước mà `drift` đã cài sẵn). Cập nhật 1 call site production còn lại tạo Companion trực tiếp (`cash_voucher_service.dart::recordVoucher`, `shiftId: shift.id` → `shiftId: Value(shift.id)`). Hai nơi lọc theo shiftId cụ thể ở Flutter (`shift_repository.dart`, `cash_ledger_page.dart`, đều `v.shiftId.equals(shiftId)`) tự loại hàng null giống hệt lý do phía server, không cần sửa. `flutter analyze`/`flutter test` xác nhận sạch sau khi sửa (146/146, baseline không đổi vì không thêm/bớt test Dart nào — phần này chỉ là type-safety fix, chưa phải business logic mới).
- **Flutter UI hook**: `bank_recon_page.dart` — thêm `BankReconRepository.listCategories()` (`GET /ledger/cash-categories`) và `.createEntry(...)` (`POST /reports/bank-recon/create-entry`). Trong danh sách "Sao kê", mỗi dòng có `matchedRef == null` (chưa khớp) và kỳ chưa khoá hiện thêm nút `Icons.post_add` mở dialog chọn danh mục (lọc theo đúng chiều suy từ dấu `amountVnd` của dòng — tránh người dùng chọn sai chiều rồi vẫn bị server từ chối) + ô ghi chú tuỳ chọn, gọi `createEntry` rồi `_reload()`. Không xây màn hình mới, không đụng `record_cash_voucher_sheet.dart`/`cash_voucher_service.dart` (chỉ tham khảo pattern `DropdownButtonFormField` category picker từ đó) — đúng tinh thần "nhỏ, hỗ trợ thêm" của đề bài. `flutter analyze` ban đầu báo `use_build_context_synchronously` sau `await showDialog(...)`, sửa bằng thêm `if (!mounted) return;` trước khi dùng lại `context` — sạch sau đó.
- **e2e — 3 file/khối mới, không đụng cấu trúc test cũ**: (1) `cash-fund-consistency.e2e-spec.ts` thêm 1 `it` cho aggregate — thay vì so khớp số tuyệt đối (dễ vỡ vì phụ thuộc state để lại từ các `it` khác cùng file/kỳ thật), so sánh **tương đối**: gọi `/reports/cash-fund` riêng cho từng storeId rồi so tổng với kết quả gọi không kèm `storeId`, và so từng phần tử `byStore` với kết quả single-store tương ứng — bất biến này đúng bất kể dữ liệu nền còn sót lại từ test khác. Có thêm case store_manager (canLedger, chỉ gán CH1) gọi không `storeId` → chỉ thấy CH1, và gọi `storeId=CH2` → 403. (2) `cash-category-mapping.e2e-spec.ts` (file mới) — tạo 2 `Account` test-only (`6421` active, `6499` inactive) trong `beforeAll`, xoá lại ở `afterAll`; phủ: GET list mặc định null, POST owner-only (store_manager có `canLedger` vẫn 403), từ chối mã không tồn tại/không active, set rồi gỡ (`null`) thành công, và bút toán thực tế qua `/sync/push` — danh mục có map đi TK map, danh mục chưa map vẫn 642 (regression). Tự dọn `JournalEntry`/`JournalLine`/`CashVoucher` vừa tạo ở cuối test vì mapping không ảnh hưởng số cộng dồn của test khác nhưng dữ liệu kế toán thật thì nên dọn theo thói quen chung của repo. (3) `bank-recon.e2e-spec.ts` thêm `describe` lồng — phát hiện khi viết test: test gốc đầu file tạo 1 sale chuyển khoản 50.000đ và **không** dọn lại, nên nếu test P2.2 chỉ xoá `bankStatementLine`/`bankReconLock` như test gốc vẫn làm, sale đó sẽ hiện ra như một dòng book "chưa khớp" vĩnh viễn trong `loadBankBook` và làm mọi `lock()` trong test mới thất bại giả — thêm `resetBookSide()` (xoá journal/sale/cashVoucher/supplierPayment theo storeId, chạy ở `beforeEach` **và** `afterAll` của khối lồng) để cô lập hoàn toàn, đồng thời "dọn hộ" luôn phần rò rỉ của test gốc cho các file chạy sau. Phủ: tạo bút toán cho dòng sao kê chưa từng có ở book → khớp → khoá kỳ (trước đó bị chặn đúng vì thiếu book-side); sai chiều danh mục/dòng không tồn tại/dòng đã khớp/kỳ đã khoá đều 400; cashier bị chặn 403 bởi `LedgerPermissionGuard` (guard chặn trước khi vào service).
- **Xác minh e2e thật**: dựng `postgres:15` container tay (`tap-hoa-p22-verify`, cổng host **15434** — ngoài dải bị Windows chiếm, xem "Chặn môi trường"), `DATABASE_URL` trỏ vào đó + `TAP_HOA_SKIP_LOCAL_IDENTITY=1`, `npx prisma migrate deploy` (27 migration, gồm migration mới của task này) → `rtk proxy npx prisma db seed` (hook `rtk` global của máy chặn nhầm `prisma` binary, vòng qua như P1.4 đã ghi) → `npm run test:e2e`. Lặp lại **3 lần** trên DB tạo mới hoàn toàn mỗi lần (`DROP DATABASE`/`CREATE DATABASE` + migrate + seed), thứ tự file mỗi lần một khác (xác nhận qua log PASS) — cả 3 lần đều **38/38 suite, 129/129 test pass** (122 baseline của P1.4 + 7 test mới: 1 aggregate cash-fund + 3 category-mapping + 3 bank-recon create-entry — khớp đúng số cộng thêm). Container `tap-hoa-p22-verify` đã dừng + xoá sau khi xong, không đụng các container `supabase_*` sẵn có (`tap-hoa`/`omni-commerce`).
- **`npm run build` + `npm run test:unit`**: cả hai pass (69 test, +1 so baseline P1.2 — do sửa `reports.service.spec.ts` cần truyền thêm arg `ledger` giả cho constructor `ReportsService` sau khi thêm dependency `LedgerService`, không phải test logic mới).

### Ghi chú review P2.3

- **Endpoint**: `GET /reports/inventory-movement?storeId&periodYm` (+ `GET /reports/inventory-movement.csv` cùng tham số) — đặt trong nhóm VẬN HÀNH của `reports.controller.ts` (không `LedgerPermissionGuard`), đúng tiền lệ `stock-on-hand` vì lý do y hệt P0.4 đã lập luận: đọc tồn kho là vận hành, không phải kế toán. Cập nhật luôn dòng comment liệt kê nhóm vận hành ở đầu controller cho khớp thực tế (đã có endpoint thứ 6/7). `storeId` bắt buộc — theo đúng yêu cầu task, KHÔNG có chế độ gộp nhiều điểm bán như `cashFundSummary` (P2.2), vì tồn kho vốn là khái niệm theo từng điểm bán.
- **Base-set sản phẩm**: hợp của (a) có dòng `ProductStoreStock` tại điểm này với `product.active = true` (khớp `stockOnHand`) và (b) có ≥1 `StockMovement` trong kỳ tại điểm này (không lọc active) — để một sản phẩm bán hết/ngưng bán trong kỳ không biến mất khỏi báo cáo của chính kỳ nó còn biến động. Xác nhận bằng e2e riêng: set `product.active = false` sau khi đã có đủ nhập/bán/hủy trong kỳ, gọi lại báo cáo, sản phẩm vẫn hiện đúng số — nếu bỏ nhánh (b) đi thì test này sẽ fail (item biến mất), nên test có sức chứng minh thật.
- **Thiết kế breakdown theo docType**: KHÔNG dùng bảng ánh xạ docType→chiều cố định (`purchase` luôn nhập, `sale` luôn xuất...) vì `transfer`/`stocktake`/`sale_return`/`supplier_return` (đủ 7 giá trị enum `StockDocType`, không chỉ 5 giá trị task liệt kê mẫu) có thể là dòng TĂNG hay GIẢM tùy chứng từ cụ thể (chuyển đến vs chuyển đi, kiểm kê dư vs thiếu). Chiều nhập/xuất suy trực tiếp từ DẤU `qtyDelta` của từng dòng, rồi gom vào `inByDocType`/`outByDocType` (2 object riêng, key = docType thô, chỉ liệt kê docType có phát sinh > 0) — cùng 1 docType có thể xuất hiện ở CẢ HAI object nếu kỳ đó vừa có dòng chuyển đến vừa có dòng chuyển đi. Logic này tách thành hàm thuần `computeInventoryMovement`/`computeOpeningQty` trong `apps/api/src/reports/inventory-movement.ts` (không đụng Prisma), theo đúng khuôn `cash-fund.ts`/`debt-aging.ts` cùng thư mục — unit test riêng (`inventory-movement.spec.ts`, 7 test) phủ cả trường hợp cùng docType xuất hiện 2 chiều trong cùng kỳ, dòng `qtyDelta = 0` (kiểm kê khớp, không tính nhập/xuất), và bất biến `opening + in − out == closing`.
- **Truy vấn hiệu quả thay vì tải cả lịch sử**: tồn đầu kỳ lấy qua 1 query `distinct: ['productId']` + `orderBy: [{productId:'asc'},{clientCreatedAt:'desc'},{createdAt:'desc'}]` trên `StockMovement` với `clientCreatedAt < from` — Prisma dịch thành `DISTINCT ON` của Postgres, trả đúng 1 dòng (gần nhất) mỗi sản phẩm, KHÔNG cần tải toàn bộ lịch sử trước kỳ về rồi lọc tay trong JS. `createdAt` (thời điểm server ghi) làm tiêu chí phụ để phá vỡ hòa nếu 2 dòng cùng `clientCreatedAt` (vd. nhiều dòng cùng 1 chứng từ multi-line).
- **Biên kỳ UTC+7**: tái dùng ĐÚNG công thức đã có ở `loadBankBook`/`loadApBook` (`Date.UTC(y, m-1, 1) - 7*3600_000` .. `Date.UTC(y, m, 1) - 7*3600_000 - 1`) — 2 nơi đó tự lặp lại công thức này (không có helper dùng chung sẵn có trong `reports.service.ts` dù task gợi ý có thể có). Đã tách thành 1 private method `periodBoundsIct` mới, dùng cho endpoint này — KHÔNG sửa lại 2 nơi cũ (đã qua review P2.2/phase3) để giữ additive tối thiểu, tránh rủi ro hồi quy ở code đã pass review.
- **CSV export**: làm theo — pattern giống hệt `arExportCsv` (trả JSON `{storeId, periodYm, csv}`, không phải `StreamableFile` — đó là kiểu chỉ dùng cho `.xlsx`/`.pdf` ở nhóm kế toán). CSV chỉ gồm cột tổng (`openingQty,inQty,outQty,closingQty`), KHÔNG có breakdown theo docType trong CSV (breakdown chỉ ở JSON) — vì mỗi dòng/sản phẩm có thể có số lượng cột docType khác nhau (tuỳ có bao nhiêu loại chứng từ phát sinh), làm CSV cột động phức tạp không tương xứng lợi ích cho một export phụ; số tổng đã đủ dùng cho việc đối chiếu ngoài Excel.
- **Flutter**: `inventory_movement_repository.dart` + `inventory_movement_page.dart` (mới, `apps/pos_app/lib/features/reports/`) theo đúng khuôn `stock_on_hand_repository.dart`/`stock_on_hand_page.dart` — NHƯNG không có offline fallback (khác `StockOnHandRepository`) vì đây là báo cáo kỳ lịch sử phục vụ đối soát, không phải quyết định tại quầy lúc mất mạng; cùng lý do `bank_recon_page.dart`/`ledger_page.dart` cũng không có fallback. Ô chọn kỳ dùng lại đúng pattern `TextFormField` + `RegExp(r'^\d{4}-\d{2}$')` validate on-submit đã thấy ở `ledger_page.dart`/`bank_recon_page.dart` (không có date-picker kiểu tháng nào có sẵn trong app để tái dùng — cả 2 màn kỳ hiện có đều dùng ô nhập tay này), thay vì dựng widget chọn tháng mới. Nút xuất CSV mở dialog `SelectableText` + nút Copy clipboard, giống hệt `_exportCsv` của `ledger_page.dart` (không viết file, không cần `path_provider`/`share_plus` — đơn giản hơn `day_report_csv.dart` vì CSV đã được server generate sẵn).
- **2 điểm vào điều hướng** (không mồ côi) — thêm cả hai nơi `StockOnHandPage(` đang được gọi: (1) `inventory_hub_page.dart` (tab "Kho") — nút `TextButton` "Nhập-xuất-tồn" cạnh nút "Tồn hiện tại" sẵn có trong `AppBar.actions`, dùng thẳng `widget.dio`/`widget.storeId`/`widget.db` đã có sẵn trong scope (không cần sửa constructor, không cần xâu thêm field qua `PosPage`). (2) `day_report_page.dart` — nút text "Xem nhập-xuất-tồn theo kỳ" ngay dưới "Xem tồn hiện tại" sẵn có, dùng `widget.repository.dio` (getter public có sẵn từ P1.2, cùng pattern `widget.dayReportRepository.dio` đã dùng ở nhiều call site khác trong `pos_page.dart`) để tự dựng `InventoryMovementRepository` tại chỗ — không cần thêm field mới vào `DayReportPage`/`PosPage`, không đụng bất kỳ test nào dựng các widget đó trực tiếp.
- **Test client**: không có widget test cho trang mới — đúng convention hiện có (`stock_on_hand_page.dart`/`bank_recon_page.dart`/`ledger_page.dart`/`inventory_hub_page.dart` đều không có widget test trực tiếp trong `test/`, chỉ có test ở tầng repository). Thêm `inventory_movement_repository_test.dart` (5 test, theo khuôn `stock_on_hand_repository_test.dart` với `MockDio`/mocktail): parse JSON đúng field + map `inByDocType`/`outByDocType`, ném lỗi khi body rỗng, KHÔNG nuốt `DioException` (khác `StockOnHandRepository` — xác nhận qua test tên "no offline fallback"), `fetchCsv` trả đúng chuỗi csv và fallback `''` khi thiếu field. `flutter test` 151/151 pass (146 baseline + 5 mới), `flutter analyze` sạch toàn dự án.
- **e2e** (`apps/api/test/inventory-movement.e2e-spec.ts`, file mới): dựng 2 sản phẩm SKU ngẫu nhiên duy nhất (không đụng `STING-330` hay bất kỳ fixture chia sẻ nào) tại CH1, kỳ test cố định `2026-02` (quá khứ, không phải kỳ thật hiện tại của máy chạy — tránh mọi va chạm với các file khác suy `periodYm` từ `new Date()`). Kịch bản: nhập trước kỳ (Jan, thiết lập tồn đầu kỳ) → nhập + bán + hủy trong kỳ (đủ 3 docType task yêu cầu tối thiểu) → nhập sau kỳ (Mar, xác nhận KHÔNG lọt vào báo cáo). Phủ: tồn đầu kỳ đúng, breakdown theo docType đúng, bất biến `opening+in−out=closing` (đối chiếu chéo với `balanceAfter` của dòng `StockMovement` cuối kỳ đọc thẳng qua Prisma), sản phẩm không biến động trong kỳ vẫn hiện đúng (opening=closing, nhập/xuất=0, không bị bỏ sót), sản phẩm ngưng bán vẫn hiện (test base-set), chặn store ngoài scope (403), validate `periodYm`/`storeId`, CSV export, và không đòi `canLedger` (nhóm vận hành). Cũng thêm 1 dòng vào `operationalCases` sẵn có trong `test/reports-permissions.e2e-spec.ts` (file P0.4) để endpoint mới nằm trong ma trận quyền 4-vai-trò đã kiểm tồn tại — additive thuần, không đụng logic cũ của file đó. Bug tự phát hiện lúc viết test (không phải bug service): quên track id của chứng từ "sau kỳ" nên `afterAll` không dọn được (FK chặn xoá `Product`), và query đối chiếu `balanceAfter` dùng nhầm biên UTC ngây thơ thay vì biên ICT — cả hai chỉ ở trong test, sửa xong không đổi gì ở `reports.service.ts`.
- **Xác minh e2e thật**: dựng `postgres:15` container tay (`tap-hoa-p23-verify`, cổng host **15437**), `DATABASE_URL` trỏ vào đó + `TAP_HOA_SKIP_LOCAL_IDENTITY=1`, `npx prisma migrate deploy` (27 migration, không có migration mới — task này thuần đọc, không đụng schema) → `rtk proxy npx prisma db seed` → `npm run test:e2e`. Lặp lại **3 lần** trên DB tạo mới hoàn toàn mỗi lần (`DROP DATABASE`/`CREATE DATABASE` + migrate + seed), thứ tự file mỗi lần một khác (xác nhận qua log PASS) — cả 3 lần đều **39/39 suite, 136/136 test pass** (129 baseline của P2.2 + 7 test mới của `inventory-movement.e2e-spec.ts`). Container `tap-hoa-p23-verify` đã dừng + xoá sau khi xong.
- **`npm run build` + `npm run test:unit`**: cả hai pass (76 test, +7 so baseline P2.2 — toàn bộ 7 test mới thuộc `inventory-movement.spec.ts`, không đụng test logic nào khác).
- **Không đụng** `GET /reports/stock-on-hand`, bất kỳ `_writeMovement`/`stockMovement.create` caller nào (`sync/stock-ops.service.ts`, `sync/sale-returns.service.ts`, `suppliers/suppliers.service.ts`), hay dữ liệu P2.2 (`cash-fund.ts`, bank-recon) — task thuần đọc/report, xác nhận qua `git diff --stat` chỉ chạm `reports.controller.ts`/`reports.service.ts` (thêm method mới, không sửa method cũ) + 2 điểm điều hướng Flutter + 1 dòng thêm vào test P0.4.

### Ghi chú review P2.4

- **Quyết định lưu trữ `xmlContent`/`pdfContent`**: 2 cột `Bytes?` mới trên `EInvoice` (migration `20260728150000_p2_4_einvoice_stub_content_audit`, `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ... BYTEA`) chứa PDF/XML nội dung thật do `StubEInvoiceAdapter` sinh ra — DB thay vì đĩa cục bộ, đúng lý do task nêu (VPS redeploy không xoá mất, cùng đường backup/restore với mọi dữ liệu khác, dọn đường cho P2.5). **Ngữ nghĩa `xmlPath`/`pdfPath` được giữ nguyên kiểu (`String?`) nhưng tách theo provider**: stub-issued để `null` (không còn chuỗi giả `stub://STUB-XXXX.xml` — không có URL ngoài nào thật sự tồn tại phía sau nó), provider-hosted (`http` adapter) tiếp tục dùng như cũ (URL `https://` thật đã được `HttpEInvoiceAdapter.parseIssueBody` validate). Endpoint tải (`downloadDocument` trong `einvoice.service.ts`) chọn nhánh theo **dữ liệu có mặt** (`xmlContent`/`pdfContent` khác null → phục vụ thẳng từ DB; không có nhưng có `xmlPath`/`pdfPath` → proxy-fetch), không theo tên provider — tự nhiên đúng cho cả 2 chế độ mà không cần nhánh `if (provider === 'stub')` nào trong controller/service.
- **Sinh PDF/XML stub thật**: file mới `einvoice-stub-document.ts` — PDF dùng `pdfkit` + font NotoSans (đúng constant/candidate-path pattern copy từ `reports.service.ts::resolvePeriodPdfFontPath`/`usePeriodPdfFont`, không sửa `reports.service.ts` để không đụng code đã qua review) hiển thị số HĐ, ngày, người mua (tên từ `Customer.name` nếu có — thêm field `buyerName` mới vào `IssueEInvoiceInput`/`AdjustEInvoiceInput`), MST, từng dòng hàng (tên SP — thêm `productName` vào line item, cần `include: { lines: { include: { product: true } } }` ở cả `loadSalesForIssue` lẫn `adjust`'s sale include), tổng cộng, và một dòng chú thích rõ ràng **"không phải hóa đơn điện tử hợp lệ theo Nghị định 123/2020/NĐ-CP và Thông tư 78/2021/TT-BTC"** in ngay trên PDF — tự nhắc ngay tại điểm sử dụng, không chỉ trong docs. XML là export có cấu trúc đơn giản tự mô tả (`<Invoice><Number>/<Lines><Line>.../<TotalVnd>`), khai báo rõ trong comment đầu file là **KHÔNG PHẢI** schema CQT chính thức — đúng ranh giới scope đã xác nhận, không nghiên cứu/động tới Thông tư 78 format thật.
- **Type friction Prisma 6 `Bytes` vs Node `Buffer`**: `@prisma/client` 6.1 generate kiểu `Bytes` thành `Uint8Array<ArrayBuffer>` (generic chặt), trong khi `Buffer` của Node (qua `@types/node` hiện tại) là `Uint8Array<ArrayBufferLike>` (bao gồm cả `SharedArrayBuffer` về mặt lý thuyết) → TS chặn gán trực tiếp. Thêm helper `EInvoiceService.toBytes()` cast qua `new Uint8Array(buf) as Uint8Array<ArrayBuffer>` tại đúng 2 điểm ghi (`persistIssue` update, `adjust`'s `tx.eInvoice.create`) — an toàn vì buffer tự dựng từ `pdfkit`/`Buffer.from(string)` không bao giờ là `SharedArrayBuffer`.
- **Proxy download cho provider `http`**: thêm `HttpEInvoiceAdapter.fetchDocument(url, logPrefix)` (method public mới, cùng file `http-einvoice.adapter.ts`) — tái dùng **nguyên `validateEndpoint` private method đã có** (chặn non-https ngoài localhost/`EINVOICE_HTTP_ALLOW_INSECURE`, chặn host `metadata`/`*.internal`) làm bước re-check phòng thủ kép trước khi fetch thật (dù `xmlPath`/`pdfPath` đã được validate 1 lần lúc lưu qua `parseIssueBody`, tái-check tại thời điểm đọc rẻ và an toàn hơn), cộng timeout (`AbortController` + `this.timeoutMs`) và header `Authorization: Bearer <EINVOICE_HTTP_API_KEY>` nếu có cấu hình — đúng 2 thứ task yêu cầu tái dùng. `EInvoiceService` inject thẳng `HttpEInvoiceAdapter` (không qua token `EINVOICE_ADAPTER`, vì token đó trỏ theo `EINVOICE_PROVIDER` runtime còn `HttpEInvoiceAdapter` luôn là 1 provider hợp lệ trong `EInvoiceModule` bất kể đang chạy stub hay http) để `downloadDocument` gọi được `fetchDocument` cho cả 2 trường hợp cấu hình.
- **Endpoint tải**: `GET /einvoices/:id/xml` và `GET /einvoices/:id/pdf` trong `einvoice.controller.ts`, dùng `StreamableFile` (đúng pattern `reports.controller.ts::periodExportXlsx/Pdf`) — `Content-Type` lấy từ `downloadDocument()` (mặc định `application/xml`/`application/pdf` cho nội dung DB; với proxy, ưu tiên `content-type` vendor trả về, fallback về mặc định nếu vendor thiếu/trả `application/octet-stream`), `Content-Disposition: attachment; filename="<invoiceNumber-hoặc-id>.<ext>"`. Gate quyền: `JwtAuthGuard` + `EInvoicePermissionGuard` sẵn có ở mức controller (không đổi), cộng `EInvoiceService.assertStoreAccess` bên trong `downloadDocument` (so `sale.storeId` của hoá đơn với `user.storeIds`) — chặn được store_manager điểm bán khác dù có `canEinvoice`, đã có e2e xác nhận 403.
- **Audit vòng đời**: 3 action string `einvoice_issue`/`einvoice_cancel`/`einvoice_adjust`, `entityType: 'einvoice'`, `entityId` = id dòng `EInvoice` liên quan (với adjust là dòng **mới** tạo ra, không phải hoá đơn gốc). `detailJson` gồm `storeId`, `invoiceNumber`, `provider`, `saleIds`, và `reason`/`originalInvoiceId`/`originalInvoiceNumber` khi có — **cố tình KHÔNG** đưa `buyerTaxCode` hay bất kỳ PII người mua nào vào audit (có test khẳng định `detailJson` không chứa MST), theo đúng mức "không PII" của tiền lệ P1.7. Khác với 3 action `user_*` của P1.7 (không có `storeId` trong detail vì quản lý NV không gắn 1 điểm bán) — 3 action HĐĐT NÀY luôn có `storeId` vì hoá đơn vốn thuộc về đúng 1 điểm bán, nên `LedgerService.ledgerAuditStoreIds`/`auditStoreIdFragment` (match chuỗi con `"storeId":"<id>"`) cũng cho **store_manager** (có `canLedger`) thấy audit HĐĐT của điểm bán mình, không chỉ owner như hạn chế đã ghi nhận ở P1.7.
  - **Vị trí ghi 2 backend allowlist**: đã thêm cả 3 action vào `defaultAuditActions` trong `apps/api/src/ledger/ledger.service.ts` (đúng gotcha P1.7 — thiếu bước này thì audit ghi vào DB nhưng không bao giờ lên `GET /ledger/audit` khi client gọi không kèm `action`, đúng cách `ledger_page.dart::listAudit()` luôn gọi). Cả 3 e2e mới đều gọi thẳng `GET /ledger/audit` (không kèm `action`) để xác nhận trực tiếp việc này, không chỉ kiểm qua `prisma.auditLog.findFirst`.
  - **Vị trí ghi nhãn Flutter**: `apps/pos_app/lib/features/ledger/ledger_page.dart::_auditActionLabel` thêm 3 case (`einvoice_issue` → "Xuất HĐĐT", `einvoice_cancel` → "Hủy HĐĐT", `einvoice_adjust` → "Điều chỉnh HĐĐT"); `_auditDetailLines` thêm nhánh hiện `Số HĐ:`/`NCC:` cho cả 3 action (nhánh `Lý do:` sẵn có tự động áp dụng vì đọc `detail['reason']` không phân biệt action).
  - **Điểm ghi trong service**: `issue`/`issueBatch` (dùng chung `persistIssue`) ghi audit **sau khi** update trạng thái cuối cùng thành công, không transaction với call adapter (vốn service này chưa từng transaction hoá bước gọi adapter external — giữ nguyên rủi ro hiện có, không mở rộng scope); short-circuit "đã issue trước đó, trả về invoice cũ" **không** ghi audit mới (không có gì mới xảy ra). `cancel` tương tự — double-cancel (đã `cancelled` từ trước) không ghi thêm dòng audit. `adjust` ghi audit **trong cùng** `$transaction` với `tx.eInvoice.create` (khác `issue`, vì ở đây adapter call đã xảy ra xong trước khi vào transaction, nên transaction chỉ bọc phần ghi DB thuần — atomic thật giữa tạo hoá đơn điều chỉnh và audit).
- **e2e**: mở rộng `einvoice.e2e-spec.ts` (+3 test, 1 `describe` lồng mới) — (1) issue sinh PDF/XML thật tải được (`%PDF` header, `<Invoice>`/`<Number>`/`<TotalVnd>` trong XML), `xmlPath`/`pdfPath` không còn `stub://` giả, store_manager điểm bán khác (CH2) bị 403 khi tải, audit `einvoice_issue` đúng shape + hiện qua `GET /ledger/audit`, không rò rỉ MST vào `detailJson`; (2) cancel/adjust ghi đúng audit action + hiện qua API, hoá đơn điều chỉnh có nội dung tải riêng; (3) hoá đơn không có cả content lẫn path (case biên) → 404 đúng 2 route. Mở rộng `einvoice-http.e2e-spec.ts` (+1 test) — sửa mock server trả `xmlPath`/`pdfPath` trỏ **về chính nó** (`http://127.0.0.1:<port>/inv.xml`, trước đây là chuỗi giả `http://mock/...` không resolve được, chỉ dùng để so khớp field chứ chưa từng bị fetch thật) thay vì hostname giả, thêm nhánh xử lý `GET` cho 4 path tài liệu (`/inv.pdf`, `/inv.xml`, `/adj.pdf`, `/adj.xml`) trả bytes thật + ghi lại header `Authorization` nhận được; test mới xác nhận: tải qua endpoint trả đúng bytes/`content-type` vendor, không có `EINVOICE_HTTP_API_KEY` thì vendor không thấy header auth, đặt biến env đó thì vendor thấy đúng `Bearer <key>` (chứng minh forward auth header hoạt động), cộng audit issue/cancel qua `GET /ledger/audit`. Response body binary đọc qua `.buffer(true).parse(...)` — copy đúng pattern đã dùng ở `vat-summary.e2e-spec.ts` cho `/reports/period/export.pdf` (client Node của supertest/superagent không có parser mặc định cho `application/pdf`).
- **Xác minh e2e thật**: dựng `postgres:15` container tay (`tap-hoa-p24-verify`, cổng host **15439**), `DATABASE_URL` trỏ vào đó + `TAP_HOA_SKIP_LOCAL_IDENTITY=1`, `rtk proxy npx prisma migrate deploy` (28 migration, gồm migration mới của task này) → `rtk proxy npx prisma db seed` → `rtk proxy npm run test:e2e` (lệnh `prisma`/`npm run test:e2e` phải qua `rtk proxy` — hook `rtk` global của máy tóm tắt/nuốt output thật của các lệnh này thành dòng tóm tắt gây hiểu lầm, vd. `migrate status` không qua proxy in "0 applied, 0 pending" dù DB đã migrate đầy đủ; qua `rtk proxy` mới thấy output thật). Lặp lại **3 lần** trên DB tạo mới hoàn toàn mỗi lần (`DROP DATABASE`/`CREATE DATABASE` + migrate + seed), thứ tự file mỗi lần một khác (xác nhận qua log PASS) — cả 3 lần đều **39/39 suite, 140/140 test pass** (136 baseline của P2.3 + 4 test mới: 3 trong `einvoice.e2e-spec.ts` + 1 trong `einvoice-http.e2e-spec.ts`). Container `tap-hoa-p24-verify` đã dừng + xoá sau khi xong.
- **`npm run build` + `npm run test:unit`**: cả hai pass (76 test, không đổi so baseline P2.3 — task này không thêm unit test mới, toàn bộ coverage mới nằm ở e2e vì logic phụ thuộc DB/HTTP thật).
- **Flutter**: chỉ đụng `ledger_page.dart` (3 case nhãn + 1 nhánh chi tiết) — không có test Dart trực tiếp cho các nhánh này (đúng convention hiện có của file, không có widget test dựng `LedgerPage`/gọi `_auditActionLabel` trực tiếp). `flutter analyze` sạch toàn dự án, `flutter test` 151/151 pass (baseline không đổi — không thêm/bớt test Dart nào, đúng như kỳ vọng cho một thay đổi chỉ đổi nhãn hiển thị).
- **Không đụng** `apps/pos_app/lib/features/einvoice/einvoice_page.dart` (đọc `pdfPath`/`xmlPath` để hiện text — nay `null` với hoá đơn stub nên dòng "PDF: .../XML: ..." đơn giản không hiện nữa, không crash, không lỗi lint) — thêm nút tải PDF/XML vào màn hình này nằm ngoài scope DoD đã xác nhận (chỉ yêu cầu nhãn audit ở tab Sổ · Nhật ký), cân nhắc cho task riêng nếu cần UX tải trực tiếp trong app.

### Ghi chú review P2.5

- **Quét mã xem tồn — tái dùng thật, không chỉ "giống hệt"**: `openCameraBarcodeScanner`/`posCameraBarcodeScannerAvailable` trước đây là top-level function riêng của `pos_page.dart` — vì `inventory_hub_page.dart` đã bị `pos_page.dart` import (dòng 20), import ngược lại sẽ tạo circular import. Đã tách 2 hàm này sang `barcode_scanner_page.dart` (file đã sẵn định nghĩa `PosBarcodeScannerPage` mà chúng bọc quanh — vị trí tự nhiên hơn `pos_page.dart`), cả `pos_page.dart` lẫn `inventory_hub_page.dart` cùng import từ đó — không còn vòng lặp, không nhân đôi logic camera/scanner. Riêng phần MATCHING (chuẩn hoá + khớp barcode duy nhất) thì **replicate** thay vì import thẳng: `posExactBarcodeMatch`/`_normalizeBarcodeQuery` gắn chặt với `pos_page.dart` (đã có test riêng `pos_page_barcode_test.dart`), nên viết bản sao ngữ nghĩa y hệt `inventoryScanExactBarcodeMatch`/`_normalizeStockScanBarcode` trong `inventory_hub_page.dart` (cùng: trim+lowercase, khớp duy nhất — 0 hoặc >1 kết quả đều trả `null`) thay vì đẩy thêm một export/import chéo cho một hàm ~20 dòng. Có test riêng (`test/inventory_hub_barcode_test.dart`, 4 case) xác nhận hành vi khớp hệt `pos_page_barcode_test.dart`.
- **Vị trí action**: `IconButton` "Quét mã xem tồn" trong `AppBar.actions` của `InventoryHubPage`, ngay sau "Nhập-xuất-tồn" — cùng khu vực với "Tồn hiện tại"/"Nhập-xuất-tồn" như đề bài yêu cầu. Gate theo `posCameraBarcodeScannerAvailable(defaultTargetPlatform)` giống hệt nút quét ở `pos_page.dart` (ẩn trên desktop/web, `mobile_scanner` chỉ chạy Android/iOS) — không gate theo role: đây là lookup chỉ-đọc, không sửa dữ liệu, nhất quán với "Tồn hiện tại"/"Nhập-xuất-tồn" (không role-gate) chứ không phải "Cho phép âm tồn"/"Tạo chuyển kho" (role-gate vì có ghi). Kết quả hiện qua `AlertDialog` đơn giản (tên, SKU, tồn hiện tại + đơn vị) hoặc thông báo "Không tìm thấy" — không có `minQty` vì `ProductWithStock` (model dùng chung toàn app, kể cả `pos_page.dart`) hiện chỉ có `qty`, thêm `minQty` sẽ phải sửa `product_repository.dart::watchByStore` và lan ra mọi call site — không cần thiết cho một quick-check 2-chạm.
- **Restore — thứ tự thao tác (đúng root cause "SQLite correctness detail" đề bài nêu)**: `LocalBackupService.restoreBackup(backupDirectoryName)` làm đúng 5 bước theo thứ tự bắt buộc (chi tiết trong docstring tại chỗ): (1) validate tên + file backup tồn tại, ném `ArgumentError`/`FileSystemException` rõ ràng, KHÔNG đụng gì tới `db`/file sống nếu bước này fail; (2) `createBackup()` tự động (auto-backup-before-restore) **trong khi `db` còn mở** — bắt buộc vì `VACUUM INTO`/checkpoint cần kết nối sống; (3) `PRAGMA wal_checkpoint(TRUNCATE)` (best-effort, bọc try/catch) rồi `db.close()`; (4) xoá `-wal`/`-shm` còn sót cạnh file database sống; (5) ghi đè file sống bằng file backup (`File.copy`, tự overwrite). Thứ tự 3→4→5 là mấu chốt: nếu xoá sidecar TRƯỚC khi đóng `db`, kết nối cũ có thể ghi tiếp vào `-wal` sau khi xoá; nếu ghi đè file chính TRƯỚC khi xoá sidecar, lần mở kế tiếp sẽ thấy `-wal` ứng với dữ liệu CŨ nhưng file chính đã là dữ liệu MỚI từ backup → SQLite replay WAL sai khớp, hỏng database vừa khôi phục.
- **Quyết định rotation cho auto-backup-before-restore**: dùng CHUNG cơ chế xoay vòng 7-bản với backup thường (gọi thẳng `createBackup()` không đổi tham số, không đặt tên riêng để "miễn nhiễm" rotation). Lý do chọn phương án đơn giản thay vì thêm sơ đồ đặt tên/ngoại lệ riêng: bản mới nhất luôn đứng đầu trong `localBackupNamesToDelete` (sort desc, giữ `keepCount` đầu tiên), nên bản auto-backup-before-restore chỉ bị xoá sau khi có thêm **7 lượt backup kế tiếp** (lịch tự động mỗi 24h hoặc bấm tay "Sao lưu ngay") — tối thiểu vài ngày, đủ để người dùng phát hiện lỡ khôi phục nhầm. Đổi lại tránh được rủi ro sửa `rotateLocalBackups`/`localBackupNamesToDelete` (code đã hoạt động, không có test riêng cho một sơ đồ đặt tên thứ hai) chỉ để phòng một tình huống hiếm (khôi phục nhầm NGAY TRƯỚC KHI kịp phát hiện, rồi chờ đúng 7 lượt backup nữa trôi qua mà không ai nhận ra).
- **Xác nhận WAL-safe swap bằng round-trip thật** (không phải mock file-copy): `test/local_backup_service_test.dart`, group `restoreBackup (real WAL round-trip)` — dùng `AppDatabase(NativeDatabase(dbFile))` với file thật trong thư mục temp (`Directory.systemTemp.createTemp`, qua `DirectoryProvider` injection sẵn có của `LocalBackupService`) thay vì `NativeDatabase.memory()` (database in-memory KHÔNG BAO GIỜ sinh ra `-wal`/`-shm`, nên không thể dùng để test đúng cái cần test). Ép `PRAGMA journal_mode=WAL` sau khi mở, ghi dữ liệu thật (`setMetaValue`), backup, ghi dữ liệu thật khác (mutate), rồi khẳng định file `-wal` THẬT SỰ tồn tại và có nội dung trước khi restore (sanity check chống test giả-xanh) — restore xong, mở lại bằng một kết nối `AppDatabase` MỚI (vì `db` gốc đã bị `restoreBackup` đóng), đọc lại giá trị: đúng bằng giá trị TRƯỚC lúc mutate (dữ liệu mutate biến mất), và xác nhận cả `-wal` lẫn `-shm` cạnh file sống đã bị xoá sạch. 2 test khác trong cùng group: (a) auto-backup-before-restore thực sự chạy — đếm `listBackups()` tăng đúng 1, tên khớp `result.preRestoreBackupDirectoryName`, và mở TRỰC TIẾP file backup đó (không gọi `restoreBackup` lần 2, vì 2 lần restore liên tiếp có thể trùng tên thư mục do độ phân giải giây — xem bên dưới) để xác nhận nó chứa đúng dữ liệu MUTATED (trạng thái ngay-trước-khi-ghi-đè, không phải dữ liệu gốc); (b) tên backup sai định dạng / không tồn tại ném lỗi rõ ràng (`ArgumentError`/`FileSystemException`) mà không đụng gì tới `db`/file sống (đọc lại `db.metaValue` sau 2 lần gọi lỗi, xác nhận không đổi).
- **Giới hạn độ phân giải tên thư mục (1 giây) — rủi ro đã nhận diện, không sửa**: `localBackupDirectoryName` chỉ có độ phân giải giây; nếu 2 lệnh `createBackup()` xảy ra trong CÙNG một giây (vd. gọi `restoreBackup()` hai lần liên tiếp rất nhanh, hoặc lịch tự động trùng khớp một lượt tạo tay), lệnh sau sẽ ném `FileSystemException('Backup target already exists', ...)` — NHƯNG throw này xảy ra ở bước 2 (auto-backup), TRƯỚC KHI `db.close()`/ghi đè file, nên fail an toàn (không đụng dữ liệu sống, không đóng kết nối, người dùng thấy lỗi rõ ràng và thử lại). Không sửa (không thêm disambiguator vào tên thư mục) vì: (a) bước xác nhận gõ đúng "KHÔI PHỤC" tạo độ trễ vài giây tự nhiên giữa các lần khôi phục thật của người dùng, xác suất va chạm giây gần như bằng 0 trong sử dụng thực tế; (b) sửa `localBackupDirectoryName`/`isLocalBackupDirectoryName` sẽ đụng vào format đã có test riêng (`localBackupNamesToDelete`, rotation) — rủi ro hồi quy không tương xứng với một race-condition cực hẹp. Test round-trip ở trên tránh va chạm này bằng cách truyền `now:` là một mốc quá khứ xa (1 ngày trước) cho backup ban đầu, không phải bằng cách sửa source.
- **Gate owner-only**: theo đúng house style `allow_negative_stock_sheet.dart`/`showVatSettingsSheet` — kiểm `role` tại điểm vào hành động (không phải ẩn hoàn toàn khỏi cây widget bằng điều kiện dựng). Nút "Khôi phục từ bản sao lưu" trong `_buildBackupCard` chỉ dựng khi `widget.role == 'owner'` (ẩn với NV/quản lý điểm — không có gì để họ thấy), NHƯNG `_openRestoreFlow()` vẫn kiểm lại `role` ở đầu hàm và hiện snackbar giải thích nếu sai — phòng hờ trong trường hợp tương lai có nơi khác gọi thẳng hàm này mà bỏ qua điều kiện dựng. `SyncDiagnosticsPage` cần thêm `role` bắt buộc trong constructor — chỉ 1 call site (`pos_page.dart` dòng ~737, phần khởi tạo icon "Diagnostics sync"), truyền `widget.role`.
- **Cụm từ xác nhận gõ tay**: chọn từ cố định `'KHÔI PHỤC'` (không phải tên/timestamp của bản backup) — đơn giản hơn so với bắt gõ lại một chuỗi ngày-giờ (dễ gõ sai vì định dạng, không thêm giá trị xác nhận thực sự vì người dùng đã CHỌN bản đó qua danh sách ngay trước đó rồi). Dialog xác nhận vẫn hiện rõ nhãn thời gian của bản đã chọn (`Sẽ khôi phục dữ liệu về thời điểm: ...`) để người dùng tự đối chiếu trước khi gõ. So khớp không phân biệt hoa/thường (`toUpperCase()`) để không phạt người dùng vì bàn phím không có sẵn dấu — vẫn buộc gõ tay đủ ký tự (không phải một nút OK đơn).
- **Không tự restart app — chặn tương tác trên kết nối đã đóng**: sau khi `restoreBackup()` trả về, hiện `AlertDialog` qua `showDialog(barrierDismissible: false, ...)` bọc `PopScope(canPop: false, ...)` — không có nút đóng nào trong dialog này (cố ý), chặn cả tap-outside lẫn nút back. Không có API restart tiến trình sạch cho Flutter desktop nên không thử spawn/kill process nào — đúng yêu cầu đề bài. Rủi ro còn lại đã ghi nhận (không sửa, ngoài scope): các `StreamBuilder` khác trong cây widget phía sau dialog (vd. `watchOutboxErrorCount()` ở `PosPage`) vẫn đang lắng nghe kết nối `db` đã đóng — nếu stream đó lỗi, `StreamBuilder` chỉ nhận `snapshot.hasError` (không crash, giữ nguyên giá trị hiển thị cuối cùng) nên không có tương tác thêm thực sự khả dụng, nhưng cũng không có gì chủ động vô hiệu hoá TOÀN BỘ các widget đó — chấp nhận được vì dialog chặn đã ngăn người dùng điều hướng đi đâu khác.
- **Xoá hướng dẫn "copy tay" cũ**: bỏ đoạn text cũ trong `_buildBackupCard` ("Không tự khôi phục. Nếu cần restore: đóng ứng dụng, chọn một thư mục backup, rồi chép tap_hoa_pos.sqlite đè lên file dữ liệu hiện tại.") — đã có luồng khôi phục thật trong app, không còn cần hướng dẫn thao tác thủ công ngoài app nữa.
- **Không đụng** `apps/api`, không đụng file nào của các task P0.1–P2.4 trong session này (`git diff --stat` xác nhận chỉ chạm `local_backup_service.dart`, `inventory_hub_page.dart`, `barcode_scanner_page.dart`, `pos_page.dart` — chỉ đổi 1 call site truyền thêm `role`, `sync_diagnostics_page.dart`, cộng 2 file test — `local_backup_service_test.dart` mở rộng, `inventory_hub_barcode_test.dart` mới).
- **Kết quả**: `flutter analyze` sạch toàn dự án; `flutter test` **159/159 pass** (151 baseline + 8 mới: 4 `inventory_hub_barcode_test.dart` + 4 `local_backup_service_test.dart` — 1 test parse timestamp thuần + 3 test trong group `restoreBackup`).

### Ghi chú review P2.6

- **Checkbox plans**: tick cả 17 ô `- [ ]` (P0.1: 7, P0.2: 4, P0.3: 3, P0.4: 3) sau khi sanity-check từng dòng đối chiếu code hiện tại (không chạy lại e2e — dựa trên xác nhận thật của P1.4): `apps/api/src/users/{users.controller,users.service,user-validation}.ts` phủ đủ 4 endpoint + 3 chặn nghiệp vụ (409 trùng phone qua P2002, owner cuối cùng, cashier không gọi được update vì owner-only) của P0.1; `apps/pos_app/lib/features/pos/pos_page.dart:85` (`posShowUserManagementAction(role) => role == 'owner'`) xác nhận màn `user_management_page.dart` owner-gated; `outbox_worker.dart` (nhánh `rejectedDebtPayments` gọi `revertLocalDebtPayment`, DioException giữ nguyên pending) + `outbox_reason_labels.dart` (case `payment_exceeds_balance`) phủ P0.2; `reports.service.ts::cashFundSummary` cộng `debtPayments`/`supplierPayments` cùng `cashVoucher`/`sale` phủ P0.3; `reports.controller.ts` xác nhận đúng nhóm KẾ TOÁN (`period/*`, `cash-fund`, `bank-recon*`, `ap-recon*`) có `@UseGuards(LedgerPermissionGuard)`, nhóm VẬN HÀNH (`day`, `top-skus`, `stock-on-hand`, `debt-aging`, `ar.csv`) không guard, phủ P0.4. Cả 4 file e2e liên quan (`users`, `cash-fund-consistency`, `permissions`, `reports-permissions`) tồn tại và có nội dung khớp DoD. Đồng thời đổi 3 dòng trạng thái P0.1/P0.3/P0.4 trong bảng "Trạng thái" từ "Code xong — chờ e2e (DB blocked)" sang **Done**, dựa trên xác nhận e2e thật của P1.4 (không phải một lượt chạy DB mới trong task này) — P1.4 đã chạy full suite 7 lần trên DB sạch và liệt kê rõ 4 file e2e trên nằm trong phạm vi đã xanh.
- **README.md**: đọc toàn bộ 198 dòng, đối chiếu với 12 task đã xong trong session — **không sửa gì**. Không có tuyên bố nào về CI trong README (chỉ có "Run API tests"/"Run Flutter tests" cho dev cục bộ, không nhắc `.github/workflows/ci.yml`) nên không có gì "sai" để sửa liên quan P1.4. Danh sách tính năng P1.1–P2.5 (auto-pull, allowNegativeStock, outbox backoff, aggregate cash-fund, cash-category mapping, inventory-movement report, HĐĐT XML/PDF thật, scan-to-check-stock, restore backup) đều **không được liệt kê** trong README từ trước — đây là thiếu sót có chủ đích của README (bảng "API surface" chưa từng liệt kê đầy đủ mọi endpoint, kể cả trước session này), không phải một dòng SAI cần sửa; thêm vào sẽ là đúng kiểu "changelog theo từng tính năng" mà đề bài yêu cầu tránh. Dòng "Host choice for Wave 1: Docker Compose... live VPS execution is an operator follow-up" (dòng 189) vẫn đúng — xác nhận `apps/api/Dockerfile`/`apps/api/docker-compose.prod.yml` còn nguyên, không đổi từ trước session (`git log` gần nhất trên 2 file này đều thuộc các commit trước 2026-07-27).
- **flutter_*.log**: tự xác nhận độc lập — `git ls-files | grep -i "flutter.*\.log"` không khớp gì; tìm trên filesystem (`find . -iname "flutter_*.log"`) cũng không ra file nào; `.gitignore` gốc có `*.log` (dòng 7) và `apps/pos_app/.gitignore` cũng có `*.log` (dòng 3). Xác nhận đây là non-issue thật, không có gì cần xoá.
- **Bảng trạng thái — orchestrator mở rộng phạm vi sau khi subagent báo lệch, đã xử lý xong**: subagent P2.6 phát hiện đúng và báo lại thay vì tự suy diễn — `P1.2`, `P1.5`, `P1.6`, `P1.7` vẫn đọc "Code xong — chờ e2e (DB blocked)" dù ghi chú P1.4 (dòng ~141) đã liệt kê rõ 4 file e2e của chính các task này (`stores.e2e-spec.ts`; `debt-payments.e2e-spec.ts`/`sync-push.e2e-spec.ts`; đổi mật khẩu + audit log trong `users.e2e-spec.ts`) nằm trong 37/37 suite đã xanh, và tôn trọng đúng chỉ thị phạm vi ("Do not touch the status of any other row") mà không tự vượt quyền. Orchestrator xác nhận lý do đúng như subagent chỉ ra, tự tay đổi nốt 4 dòng này sang **Done** (cùng dạng trích dẫn "e2e xanh qua P1.4 (`...spec.ts`)" như 3 dòng P0 vừa đổi) — bảng trạng thái giờ **100% Done trên mọi hàng P0.1 → P2.6**, hoàn thành toàn bộ kế hoạch.

---

## P0.1 — UsersModule (P0)

**Vấn đề:** `app.module.ts` không có `UsersModule`; `seed.ts` + `create-owner.ts` chỉ tạo `role: owner`. Spec §3.2 định nghĩa 3 vai trò nhưng không có đường tạo Quản lý điểm / Thu ngân ngoài sửa DB tay.

**Thuận lợi:** `User` đã đủ cột (`role`, `canLedger`, `canEinvoice`, `active`) + bảng nối `UserStore` → **không cần migration**.

**DoD:**
- [x] `GET /users` — owner thấy tất cả; store_manager chỉ thấy user thuộc điểm mình; cashier bị chặn.
- [x] `POST /users` — owner tạo user (phone, name, password, role, storeIds, canLedger, canEinvoice).
- [x] `PATCH /users/:id` — sửa name/role/storeIds/cờ quyền/active.
- [x] `PATCH /users/:id/password` — owner đổi mật khẩu bất kỳ; user tự đổi mật khẩu mình.
- [x] Chặn: owner cuối cùng không tự vô hiệu hóa; phone trùng → 409; cashier không tự nâng quyền.
- [x] POS: màn hình quản lý nhân viên (owner-only) trong `features/stores` hoặc `features/users`.
- [x] e2e `users.e2e-spec.ts` phủ ma trận 3 role + các case chặn.

**Neo:** `apps/api/src/stores/*` (mẫu CRUD + role check), `apps/api/src/auth/auth.service.ts` (bcrypt), `apps/pos_app/lib/features/stores/store_management_page.dart` (mẫu UI owner-only).

---

## P0.2 — Hoàn tác nợ local (P0)

**Vấn đề:** `debt_payment_service.dart:49-56` trừ `balanceVnd` local ngay; server trả `payment_exceeds_balance` → `outbox_worker.dart:461` chỉ `markOutboxError`, không hoàn tác → **số dư nợ khách sai vĩnh viễn**. Spec §6.2 yêu cầu "máy hoàn tác local".

**DoD:**
- [x] Khi outbox entry `debtPayment` bị server từ chối (reason không-retry-được), hoàn lại `balanceVnd` + xóa `debtLedgerLocal` tương ứng, trong cùng transaction.
- [x] Phân biệt lỗi mạng (giữ pending, retry) vs lỗi nghiệp vụ (rollback + báo user).
- [x] Nhãn tiếng Việt cho `payment_exceeds_balance` trong `outbox_reason_labels.dart`.
- [x] `flutter test` phủ: reject → balance trở về đúng; lỗi mạng → KHÔNG rollback.

---

## P0.3 — Thống nhất tiền mặt (P0)

**Vấn đề:** `reports.service.ts` `cashFundSummary` chỉ cộng `cashVoucher` + `sale`, bỏ sót thu nợ tiền mặt (`DebtPayment`) và chi NCC tiền mặt (`SupplierPayment`) — cả hai đều có bút toán TK 111; `cash/expected-cash.ts` lại có cộng thu nợ. Đóng ca lệch sổ quỹ.

**DoD:**
- [x] `cashFundSummary` cộng đủ mọi nguồn tiền mặt có bút toán 111.
- [x] Có test khẳng định `netCashVnd` khớp số dư TK 111 trong trial balance cùng kỳ/điểm.
- [x] e2e: bán tiền mặt + thu nợ tiền mặt + chi NCC tiền mặt + phiếu chi → sổ quỹ == sổ cái 111.

---

## P0.4 — Guard báo cáo kỳ (P0)

**Vấn đề:** `reports.controller.ts:17` chỉ `JwtAuthGuard` → store_manager có `canLedger=false` vẫn đọc được `/reports/period/*` (CĐPS, KQKD, VAT, xlsx/pdf). Vi phạm §5.7 "bán hàng ≠ kế toán".

**DoD:**
- [x] `/reports/period/*` + `/reports/cash-fund` + `/reports/bank-recon*` + `/reports/ap-recon*` yêu cầu `canLedger`.
- [x] `/reports/day`, `/reports/stock-on-hand`, `/reports/debt-aging` **giữ nguyên** (vận hành quầy, không phải kế toán).
- [x] `permissions.e2e-spec.ts` phủ ma trận role × endpoint báo cáo.

---

## P1–P2

Chi tiết DoD từng task được viết ngay trước khi mở subagent tương ứng (tránh plan chết). Tóm tắt trong bảng trạng thái ở trên.

---

## Cách chạy (SDD)

| Bước | Cách |
|------|------|
| Mỗi task | **1 subagent** — implement + test, không đụng file ngoài scope |
| Sau mỗi task | Review inline: đọc diff, chạy build + unit test (+ e2e nếu DB sẵn), tick DoD |
| Không PASS | Sửa trong cùng task, không mở task mới |

**Không** chạy 2 task song song khi chúng đụng cùng file (P0.2 và P1.1 cùng chạm `outbox_worker.dart` / scheduler → phải tuần tự).
