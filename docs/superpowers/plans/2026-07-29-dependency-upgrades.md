# Nâng cấp dependency có lỗ hổng bảo mật (I1-I8)

> **For agentic workers:** Subagent-Driven Development — **một Task = một subagent**, review giữa các task, không mở task tiếp theo khi task trước chưa PASS review.
> **Nguồn:** `npm audit` trên `apps/api` (2026-07-28, xác nhận lại 2026-07-29): 58 lỗ hổng toàn cây (3 low/20 moderate/34 high/1 critical), lọc production (`--omit=dev`) còn 27 (14 moderate/12 high/1 critical) — chủ yếu qua `@nestjs/*`, `exceljs`, `firebase-admin`. Không có bản vá lẻ, phải nâng major. **Không dùng `npm audit fix --force`** (theo đúng Global Constraint đã áp dụng suốt các đợt G/H trước — force fix chọn version tự động, không kiểm soát được, không phù hợp khi cần verify từng bước).
> **Branch:** `chore/dependency-upgrades`.

**Goal:** Nâng từng dependency lên major mới nhất, **thấp rủi ro trước, cao rủi ro sau** — mỗi bước tự đứng độc lập, build+test+e2e xanh trước khi qua bước tiếp theo. Nếu 1 bước phát hiện rủi ro/breaking change vượt phạm vi hợp lý, dừng lại, ghi rõ lý do, không ép chạy.

**Không thuộc phạm vi:** golang webpack/teeny-request/@google-cloud/storage transitive qua `firebase-admin` — nâng `firebase-admin` (I2) tới bản mới nhất hiện có, phần dư nếu còn tồn tại là do lỗ hổng upstream chưa có bản vá, ghi nhận lại chứ không tự chế fix riêng.

**Architecture:** Không đổi monorepo, không đổi logic nghiệp vụ — đây thuần là nâng version + sửa breaking-change API nếu phát sinh khi build/test đỏ.

**Tech Stack hiện tại → mục tiêu** (xác nhận qua `npm outdated`, `npm view <pkg>@<major> peerDependencies/engines` — 2026-07-29):

| Package | Hiện tại | Mục tiêu | Rủi ro |
|---|---|---|---|
| `bcrypt` + `@types/bcrypt` | 5.1.1 / 5.0.2 | 6.x | Thấp |
| `@types/supertest` | 6.0.3 | 7.x | Thấp (chỉ dev/test) |
| `firebase-admin` | 13.10.0 | 14.x | Thấp (FCM chưa cấu hình thật, đường code ít dùng) |
| `jest` + `@types/jest` | 29.7.0 / 29.5.14 | 30.x | Thấp-Trung bình |
| `class-validator` | 0.14.4 | 0.15.x | Trung bình (dùng khắp DTO) |
| `@types/express` | 4.17.25 | 5.x | Trung bình (cần xác nhận express runtime thật có bị ảnh hưởng qua NestJS không) |
| `typescript` + `@types/node` | 5.9.3 / 22.20.1 | 7.x / 26.x | **Cao** |
| `@nestjs/*` (common/core/cli/jwt/platform-express/testing) | 10.x | 11.x | **Cao** — peer dep đòi `@nestjs/common@^11`, `@nestjs/platform-express@^11` khớp nhau; node >=20 (Dockerfile `node:20-alpine` đã đủ) |
| `@prisma/client` + `prisma` | 6.19.3 | 7.x | **Cao** — cần node `^20.19 \|\| ^22.12 \|\| >=24.0`, phải xác nhận patch version thật của `node:20-alpine` đủ `20.19+` |

## Global Constraints

- **Không `npm audit fix --force`** dưới mọi hình thức.
- Mỗi task: `npm run build` + `npm run test:unit` + full `npm run test:e2e` (container Postgres riêng) đều phải xanh trước khi coi là xong.
- Nếu 1 package cần sửa code (không chỉ bump version trong `package.json`), sửa tối thiểu — chỉ đủ để build/test xanh lại, không nhân tiện refactor thêm.
- Nếu phát hiện breaking change quá lớn/rủi ro cao hơn lợi ích rõ ràng, dừng — ghi "Ghi chú review" giải thích, để nguyên version cũ, coi đó là kết luận hợp lệ (giống cách H6 đã làm với thứ tự sync).
- Không đụng `apps/pos_app` (Flutter) — ngoài phạm vi đợt này.

---

## Trạng thái

| Task | Mô tả | Rủi ro | Status |
|------|-------|--------|--------|
| **I1** | `bcrypt` + `@types/bcrypt` 5→6, `@types/supertest` 6→7 | Thấp | **Done** — bcrypt 5.1.1→6.0.0, @types/bcrypt 5.0.2→6.0.0, @types/supertest 6.0.2→7.2.1; không sửa code (build sạch ngay); build sạch + unit (12 suite/84 test) + e2e (39 suite/146 test, 1 lần trên Postgres 15 tạm cổng 55432) xanh |
| **I2** | `firebase-admin` 13→14 | Thấp | **Done** — firebase-admin 13.10.0→14.2.0; CÓ sửa code (`src/devices/devices.service.ts`: legacy namespace API `admin.apps`/`admin.credential.cert`/`admin.messaging()` bị gỡ bỏ hoàn toàn ở v14, chuyển sang modular `firebase-admin/app` + `firebase-admin/messaging`); build sạch + unit (12 suite/84 test) + e2e (39 suite/146 test, container Postgres 15 tạm cổng 55433) xanh |
| **I3** | `jest` + `@types/jest` 29→30 | Thấp-Trung bình | **Done** — jest 29.7.0→30.4.2, @types/jest 29.5.14→30.0.0; `ts-jest` giữ nguyên `^29.2.5` (không có major 30.x, bản `latest` 29.4.12 đã hỗ trợ peer `jest@^30`, đã resolve sẵn); không sửa config/code (2 file jest config + toàn bộ test giữ nguyên nội dung); build sạch + unit (12 suite/84 test) + e2e (39 suite/146 test, chạy 3 lần trên 2 container Postgres 15 tạm cổng 55434, không lần nào gặp flaky) xanh |
| **I4** | `class-validator` 0.14→0.15 | Trung bình | **Done** — class-validator 0.14.4→0.15.1; grep xác nhận **0 file** trong `apps/api/src` dùng decorator thật (không có `ValidationPipe`, mọi "DTO" đều là `type` thuần hoặc `class` không decorator, validate 400 làm thủ công qua `BadRequestException`) → bump không đổi hành vi; không sửa code; build sạch + unit (12 suite/84 test) + e2e (39 suite/146 test, chạy **2 lần** trên Postgres 15 tạm cổng 55435, không lần nào gặp flaky) xanh |
| **I5** | `@types/express` 4→5 | Trung bình | **Done** — nâng cấp (không hoãn): xác nhận express runtime thật vẫn ghim `4.22.1` qua `@nestjs/platform-express@10.4.22` (không đổi trong task này), rủi ro type/runtime lệch pha có thật về lý thuyết nhưng **0 điểm chạm** trong `apps/api/src` (không nơi nào type bằng `express.Request/Response/NextFunction/RequestHandler` thật); `@types/express` 4.17.25→5.0.6; build sạch tuyệt đối (0 sửa code) + unit (12 suite/84 test) + e2e (39 suite/146 test, container Postgres 15 tạm cổng 55436, chạy 2 lần do gặp lại flaky `vat-summary.e2e-spec.ts` đã biết từ I2) xanh |
| **I6** | `typescript` + `@types/node` lên bản mới nhất tương thích | Cao | **Done** — `typescript` giữ nguyên `^5.7.2`→`^5.9.3` (đã là bản mới nhất dòng 5.x; **KHÔNG lên 7.0.2** dù đó là bản `latest` trên npm — `ts-jest` dự án đang dùng khai peer `typescript: ">=4.3 <7"` loại trừ thẳng 7.x, và NestJS 11 (cả bản publish lẫn `master`) tự dùng `5.9.3` nội bộ, chưa hỗ trợ 6.x/7.x — xem Ghi chú review); `@types/node` **HẠ** `^22.10.2`→`^20.19.43` (không phải nâng lên `26.x` như mục tiêu ban đầu — Node runtime thật của Docker (`node:20-alpine` → xác nhận `v20.20.2` qua `docker run`) và CI (`.github/workflows/ci.yml` ghim `node-version: 20`) đều là Node 20.x, khác máy dev sandbox `v24.18.0`; sửa luôn một lệch pha runtime tiềm ẩn đã có từ trước I6); không sửa code (`git diff` chỉ `package.json`+`package-lock.json`); build sạch + unit (12 suite/84 test) + e2e (39 suite/146 test, chạy 2 lần trên Postgres 15 tạm cổng 55437, không gặp flaky) xanh |
| **I7** | `@nestjs/*` 10→11 | Cao | **Done** — `@nestjs/common` `^10.4.15`(10.4.22)→`^11.1.28`, `@nestjs/core` `^10.4.15`(10.4.22)→`^11.1.28`, `@nestjs/platform-express` `^10.4.15`(10.4.22)→`^11.1.28`, `@nestjs/jwt` `^10.2.0`→`^11.0.2`, `@nestjs/testing` `^10.4.15`(10.4.22)→`^11.1.28`, `@nestjs/cli` `^10.4.9`→`^11.0.24` (cài đồng loạt 1 lệnh); express runtime nội bộ đổi thật `4.22.1`→`5.2.1` (qua `@nestjs/platform-express`, xác nhận qua `npm ls express`); KHÔNG sửa code nào trong `src/`/`test/` (`git diff` chỉ `package.json`+`package-lock.json`) — 0 điểm chạm sau khi rà toàn bộ breaking change chính thức; build sạch + unit (12 suite/84 test) + e2e chạy **3 lần, cả 3 lần đều dựng container Postgres 15 hoàn toàn mới từ đầu** (cổng 55438/55439/55440), mỗi lần **39/39 suite, 146/146 test pass**, không gặp lại flaky `vat-summary.e2e-spec.ts` (đã biết từ I2/I5) lần nào; `npm audit --omit=dev` giảm **24→16 lỗ hổng** (đóng hẳn chuỗi `@nestjs/core` (GHSA-36xv-jgw5-4q75) + `express`/`qs`/`multer`/`body-parser`/`file-type` qua `@nestjs/platform-express`) |
| **I8** | `@prisma/client` + `prisma` 6→7 | Cao | **Done** — `prisma` `^6.1.0`(6.19.3)→`^7.9.1`, `@prisma/client` `^6.1.0`(6.19.3)→`^7.9.1`; thêm dependency mới bắt buộc `@prisma/adapter-pg@^7.9.1` (kéo theo `pg@8.22.0`); CÓ sửa code tối thiểu theo đúng migration guide chính thức (không đoán): `prisma/schema.prisma` bỏ `datasource.url` (Prisma 7 cấm khai trong schema, lỗi P1012), thêm mới `apps/api/prisma.config.ts` (thay thế `url` + block `"prisma".seed` đã bị gỡ khỏi `package.json`), `src/prisma/prisma.service.ts`+`prisma/seed.ts`+`prisma/create-owner.ts` thêm constructor truyền `@prisma/adapter-pg` (Prisma 7 bỏ hẳn `new PrismaClient()` không tham số — xác nhận thật qua lỗi thật, không đoán), `Dockerfile` thêm `COPY prisma.config.ts` vào runtime stage + `ENV DATABASE_URL` placeholder ở build stage (generate cần resolve biến này dù không cần kết nối DB thật); GIỮ NGUYÊN generator `prisma-client-js` (không đổi sang `prisma-client`/ESM — tránh kéo theo đổi `package.json#type=module`+tsconfig ESM ảnh hưởng toàn bộ ts-jest/ts-node/nest build, ngoài phạm vi "sửa tối thiểu"); build sạch (xác nhận cả trên host lẫn trong `node:20-alpine` thật qua `docker build --target build`) + unit (12 suite/84 test) + e2e **3 lần, mỗi lần container Postgres 15 hoàn toàn mới** (cổng 55441/55442/55443) — lần 1 và 2 sạch 39/39 suite 146/146 test; lần 3 dính đúng flaky `vat-summary.e2e-spec.ts` đã biết từ I2/I5 (test-order dependent, không liên quan Prisma), reset schema + chạy lại → 39/39 sạch; xác nhận **28/28 migration** cũ áp dụng đúng dưới engine mới (WASM + driver adapter, không còn Rust engine binary) qua SQL trực tiếp cả 3 lần; phát hiện thêm 1 bug tiền tồn tại KHÔNG liên quan Prisma (`dist/main.js` vs `dist/src/main.js` do `prisma/*.ts` mở rộng TypeScript rootDir), đã xác minh có từ trước I8, để ngoài phạm vi, báo cáo riêng |

---

## I1 — `bcrypt`/`@types/bcrypt`/`@types/supertest` (Thấp)

**DoD:**
- [x] `npm install bcrypt@latest @types/bcrypt@latest @types/supertest@latest` (trong `apps/api`).
- [x] Đọc CHANGELOG/breaking-change note của `bcrypt` 6.x (native binding version, Node ABI) — xác nhận bản mới vẫn build được trên môi trường hiện tại (`node:20-alpine` cho Docker, Windows cho dev).
- [x] `npm run build` + `test:unit` + full `test:e2e` xanh (đặc biệt test liên quan login/password: `auth.e2e-spec.ts`, `users.e2e-spec.ts`).
- [x] Cập nhật bảng Trạng thái + tick DoD.

### Ghi chú review I1

- **Version thật trước→sau** (đối chiếu `git diff apps/api/package.json`, không phải số trong bảng Tech Stack ở đầu file — bảng đó ghi `@types/supertest` hiện tại là `6.0.3` nhưng `package.json` thật đang pin `^6.0.2`, lệch nhẹ so với thực tế lúc viết plan, không ảnh hưởng kết quả): `bcrypt` `^5.1.1`→`^6.0.0`, `@types/bcrypt` `^5.0.2`→`^6.0.0`, `@types/supertest` `^6.0.2`→`^7.2.1`. `supertest` runtime (không đổi, ngoài phạm vi I1) đã là `^7.0.0` từ trước — nghĩa là `@types/supertest@6` từng lệch pha với runtime `7.x`, bump lần này thực ra là sửa một type mismatch tồn đọng, không phải rủi ro mới.
- **Nghiên cứu breaking change `bcrypt` 6.x trước khi cài** (`npm view bcrypt engines` + đọc `CHANGELOG.md`/release notes chính thức trên GitHub `kelektiv/node.bcrypt.js`, không đoán): thay đổi lớn nhất là bỏ `node-pre-gyp` (tải prebuilt binary qua mạng lúc `npm install`), chuyển sang `prebuildify` (đóng gói sẵn prebuilt binary NGAY TRONG tarball npm, dùng `node-gyp-build` để chọn đúng binary lúc `require()`, không cần mạng lúc cài). Node engines đổi từ không giới hạn rõ ràng sang `>= 18` (`npm view bcrypt engines` xác nhận), CHANGELOG ghi rõ "Drop support for NodeJS <= 16" — cả hai đều nằm dưới Node 20 (base image `apps/api/Dockerfile` dùng `node:20-alpine`), không có yêu cầu Node cao hơn 20. Binary vẫn build trên N-API (`prebuildify --napi`, xác nhận qua `scripts.build` trong `package.json` của package đã cài) — ABI ổn định qua các bản Node major, giảm rủi ro "vỡ" khi Node runtime đổi version trong tương lai so với kiểu binding cũ dựa thẳng vào `NODE_MODULE_VERSION`.
- **Xác nhận thật (không chỉ đọc doc) rằng native binding chạy được trên cả 2 môi trường mục tiêu** — đọc trực tiếp nội dung package vừa cài, KHÔNG build Docker image thật (đúng phạm vi DoD, không yêu cầu build image trong task này): `node_modules/bcrypt/prebuilds/` chứa sẵn 7 target (`darwin-arm64`, `darwin-x64`, `linux-arm`, `linux-arm64`, `linux-x64`, `win32-arm64`, `win32-x64`), riêng `linux-x64` có CẢ HAI biến thể libc — `bcrypt.glibc.node` VÀ `bcrypt.musl.node` — tức Alpine (`node:20-alpine` dùng musl, không phải glibc) có sẵn prebuilt binary đóng gói sẵn, không cần toolchain biên dịch (`Dockerfile` hiện chỉ `apk add openssl`, không có `python3`/`make`/`g++`) như lo ngại ban đầu trong đề bài. Trên Windows dev: chạy trực tiếp `node -e "require('bcrypt').hashSync(...)"`/`compareSync(...)` ngay sau khi cài (dùng `win32-x64/bcrypt.node` có sẵn) → **thành công**, không cần biên dịch gì thêm.
- **Một quan sát về môi trường, không phải bug của I1**: `npm install` in ra cảnh báo `npm warn allow-scripts` — một cơ chế chặn install-script (đến từ tooling toàn cục trên máy, không phải config trong repo — đã kiểm tra không có `allowScripts`/`.npmrc` nào trong repo) — khiến install-script `node-gyp-build` của `bcrypt@6.0.0` KHÔNG chạy tự động lúc `npm install`. Đã xác minh đây không phải vấn đề thật: `require('bcrypt')` gọi `node-gyp-build` lúc RUNTIME (không phải chỉ lúc install) để tự tìm đúng prebuild trong `prebuilds/`, nên hoạt động bình thường dù install-script bị chặn (đã test `hashSync`/`compareSync` thành công ở trên, và toàn bộ e2e dùng `bcrypt.hash`/`bcrypt.compare` qua `AuthService`/`UsersService`/`prisma/seed.ts` cũng pass). Không sửa gì — ngoài phạm vi I1 (là hành vi máy cục bộ, không phải của package hay của repo).
- **Không cần sửa code**: `npm run build` sạch ngay sau khi cài, không có lỗi type nào phát sinh dù `@types/bcrypt`/`@types/supertest` nâng major — 2 điểm gọi API `bcrypt.hash(...)`/`bcrypt.compare(...)` trong `apps/api/src/auth/auth.service.ts` và `apps/api/src/users/users.service.ts` dùng đúng chữ ký promise-based không đổi giữa 5.x/6.x. `git diff` xác nhận chỉ có `apps/api/package.json` + `apps/api/package-lock.json` thay đổi, không đụng file `src/`.
- **Xác minh migration áp dụng đúng, không tin theo text log CLI** (đúng phát hiện đã ghi nhận từ đợt H2-H7 — `rtk` hook rewrite output CLI đôi khi rút gọn gây hiểu nhầm): `npx prisma migrate deploy` trên container tạm in ra `"2 migration(s) deployed"` — con số này KHÔNG khớp với 28 file migration thật có trong `apps/api/prisma/migrations/`. Đối chiếu trực tiếp bằng SQL (`SELECT count(*) FROM _prisma_migrations` → **28**, `SELECT count(*) FROM information_schema.tables WHERE table_schema='public'` → **47 bảng**) xác nhận migrate deploy áp dụng đúng, đủ 28/28 — text log CLI bị `rtk` rút gọn gây hiểu lầm, không phải migrate thật sự thiếu.
- **`npx prisma db seed` bị hook `rtk` chặn nhầm** (`rtk: Failed to resolve 'prisma' via PATH... [rtk: program not found]`, exit 127) — dùng `rtk proxy npx prisma db seed` theo đúng hướng dẫn trong đề bài, chạy thành công (seed owner `0900000001` + 2 store + cash categories + accounts).
- **Kết quả xác minh**: `npm run build` sạch. `npm run test:unit` — **12 suite/84 test pass**. `npm run test:e2e` (container `postgres:15` tạm tên `tap-hoa-i1-e2e-pg`, cổng host `55432` — xác nhận trống trước khi dùng qua `docker ps` (không trùng cổng dev `54420-54429`/`3040` hay các container khác đang chạy), `TAP_HOA_SKIP_LOCAL_IDENTITY=1` + `DATABASE_URL` trỏ container tạm) — **39/39 suite, 146/146 test pass**, gồm cả `auth.e2e-spec.ts` và `users.e2e-spec.ts` (test login/đổi mật khẩu dùng `bcrypt.compare`/`bcrypt.hash` thật qua HTTP). Đã `docker stop`/`docker rm` container tạm ngay sau khi xong, không đụng container dev/Supabase/prod đang chạy trên máy.
- **Rủi ro thật sự**: không có. Đây là bump major đúng nghĩa "chỉ đổi hạ tầng build/phân phối binary", không đổi API JS, không đổi hành vi hash/compare, không cần sửa code, không phát hiện rủi ro nào vượt phạm vi hợp lý.

## I2 — `firebase-admin` 13→14 (Thấp)

**DoD:**
- [x] `npm install firebase-admin@latest`.
- [x] Kiểm tra `apps/api/src` có nơi nào import API của `firebase-admin` mà 14.x đổi breaking (đọc changelog chính thức) — sửa nếu có.
- [x] `npm run build` + `test:unit` + full `test:e2e` xanh.
- [x] Cập nhật bảng Trạng thái + tick DoD.

### Ghi chú review I2

- **Version thật trước→sau**: `firebase-admin` `^13.10.0` → `^14.2.0` (bản mới nhất hiện có lúc thực hiện, 2026-07-29, xác nhận qua `npm view firebase-admin version`).
- **Nơi dùng thật trong `apps/api/src`** (grep `firebase-admin` toàn bộ `apps/api`, chỉ khớp `package.json`/`package-lock.json`/1 file source): đúng như đề bài mô tả — chỉ `src/devices/devices.service.ts`, dùng `require('firebase-admin')` (dynamic require, không static import, có try/catch bao ngoài) để lấy `admin.apps` / `admin.initializeApp` / `admin.credential.cert` / `admin.messaging()` phục vụ gửi FCM multicast (`sendEachForMulticast`) trong `notifyUser()`. Không có file `*.spec.ts`/`*.e2e-spec.ts` nào mock hay gọi trực tiếp firebase-admin/messaging.
- **Nghiên cứu breaking change 13→14 qua nguồn chính thức** (`gh api`/`gh pr view` trên `firebase/firebase-admin-node`, không đoán): release `v14.0.0` liệt kê breaking changes gồm "Remove Deprecated Legacy Namespace Support" (#3164), "Drop legacy messaging types" (#3157), "Drop support for Node.js 18 and 20" (#3138), "Remove deprecated Instance ID service" (#3166). Đọc trực tiếp nội dung PR #3164: root `firebase-admin` (`src/index.ts`) bị viết lại để **chỉ** export API modular App-related (`initializeApp`, `getApp`, `getApps`, `deleteApp`, `cert`, `applicationDefault`, `refreshToken`, `SDK_VERSION`...) — namespace object cũ (`admin.apps`, `admin.credential.cert`, `admin.messaging()`) bị xoá hoàn toàn, đúng 3 API mà `devices.service.ts` đang dùng. Đọc PR #3157: chỉ bỏ 4 type cũ (`DataMessagePayload`/`MessagingOptions`/`MessagingPayload`/`NotificationMessagePayload`) — không ảnh hưởng vì code tự định nghĩa type `AdminMessaging` riêng, không import type từ package. Đối chiếu lại bằng cách đọc trực tiếp `node_modules/firebase-admin/lib/index.d.ts` + `package.json#exports` sau khi cài — khớp đúng mô tả trong PR: phải `require('firebase-admin/app')` và `require('firebase-admin/messaging')` (2 subpath riêng) mới có `getApps`/`initializeApp`/`cert` và `getMessaging()`; `sendEachForMulticast(message, dryRun?)` và `BatchResponse.successCount` (kiểu trả về) không đổi.
- **Có sửa code, tối thiểu**: `src/devices/devices.service.ts`, method `getMessaging()` — đổi 1 lần `require('firebase-admin')` (namespace API) thành 2 require riêng theo đúng modular entrypoint v14 bắt buộc: `require('firebase-admin/app')` (lấy `getApps`/`initializeApp`/`cert`) và `require('firebase-admin/messaging')` (lấy `getMessaging`). Giữ nguyên 100% cấu trúc defensive try/catch + kiểu dữ liệu tối giản tự định nghĩa như bản gốc, không refactor gì thêm ngoài phần bắt buộc để khớp API mới. Lưu ý: `npm run build` KHÔNG tự phát hiện lỗi này (vì `require()` động, TypeScript chỉ check theo type tự khai báo, không check theo type thật của package) — nên đã tự viết smoke-test runtime thật (không chỉ tin doc): tạo RSA key tạm bằng `openssl genrsa`, gọi thật `cert(fakeServiceAccount)` → `initializeApp({credential})` → `getMessaging()` → xác nhận `getApps().length` tăng đúng từ 0→1 và `messaging.sendEachForMulticast` là function — chạy thành công với code MỚI, xoá file tạm ngay sau đó (`_smoke_test_tmp.js`, `_smoke_test_key_tmp.pem`, không lọt vào git).
- **Rủi ro phát hiện thêm, báo cáo lại thay vì tự bỏ qua**: `firebase-admin@14.x` đổi `engines.node` từ `>=18` (bản 13.x) sang `>=22` — đây là thay đổi CHỦ ĐÍCH, xác nhận qua đúng dòng trong release note "Drop support for Node.js 18 and 20 (v14)" (#3138), không phải suy đoán. `apps/api/Dockerfile` (cả build stage lẫn runtime stage) đang dùng `node:20-alpine` — THẤP hơn yêu cầu mới này. Repo không có `.npmrc` với `engine-strict=true`, nên `npm ci`/`npm install` trên Node 20 trong Docker build chỉ in cảnh báo EBADENGINE chứ không fail — build Docker hiện tại không bị chặn ngay. Vì FCM chưa cấu hình thật ở bất kỳ đâu (kể cả biến môi trường trong Dockerfile/compose), `require('firebase-admin/...')` không được gọi thật lúc runtime hiện tại nên không có tác động ngay lập tức. Nhưng đây là rủi ro tiềm ẩn có thật: nếu sau này ai đó cấu hình `FIREBASE_SERVICE_ACCOUNT` thật trên image `node:20-alpine` hiện tại, hành vi của `firebase-admin@14.x` trên Node 20 không được đội firebase-admin hỗ trợ/kiểm thử chính thức (nhiều khả năng vẫn chạy được vì Node 20/22 phần lớn tương thích, nhưng không có cam kết upstream). Phát hiện này CHƯA từng được liệt kê trong bảng Tech Stack/rủi ro ở đầu file — nằm ngoài phạm vi sửa của I2 (không đụng Dockerfile), cần cân nhắc bump base image Node lên >=22 ở một task riêng (có thể tự nhiên trùng với yêu cầu Node của I6/I7/I8) trước khi thật sự bật FCM trong production.
- **Side-issue phát hiện khi chạy e2e, KHÔNG thuộc phạm vi I2 (test flakiness phụ thuộc thứ tự chạy, không liên quan firebase-admin)**: `test/vat-summary.e2e-spec.ts` (dòng ~51-61) dọn dữ liệu store trước mỗi test bằng cách xoá `purchaseReceipt`/`saleLine`/`sale` nhưng THIẾU bước xoá `saleReturn` trước — nếu một suite khác đã chạy trước đó và để lại `SaleReturn` tham chiếu tới `Sale` cùng store `CH1` (nhiều suite khác có tạo sale-return, chưa dò ra suite cụ thể vì ngoài phạm vi), `prisma.sale.deleteMany()` sẽ throw `Foreign key constraint violated: SaleReturn_originalSaleId_fkey`. Gặp lỗi này ở lần chạy `test:e2e` đầu tiên (Jest default sequencer xếp `vat-summary` chạy thứ 23/39 do dùng cache thời gian chạy trước đó — không cố định thứ tự giữa các lần chạy). Đã XÁC MINH RÕ ĐÂY LÀ LỖI TIỀN TỒN TẠI, không liên quan tới bump `firebase-admin`: dừng lại, `git stash` code I2, `npm install` để phục hồi hẳn `firebase-admin@13.10.0` (baseline gốc), rebuild, reset DB sạch (drop/create lại database), chạy lại `test:e2e` — lần này `vat-summary` chạy thứ 1/39 (thứ tự đổi do cache Jest, không phải do code) → **PASS**, toàn bộ 39/39 suite 146/146 test xanh trên baseline cũ. Sau đó `git stash pop` phục hồi code I2, `npm install` lại để có `firebase-admin@14.2.0`, rebuild, reset DB sạch, chạy `test:e2e` lần cuối trên đúng code I2 thật → `vat-summary` lại chạy thứ 1/39 → **PASS**, 39/39 suite, 146/146 test xanh. Kết luận: lỗi phụ thuộc hoàn toàn vào THỨ TỰ chạy file test (dữ liệu `SaleReturn` sót từ suite chạy trước), hoàn toàn không liên quan `firebase-admin` — code `require('firebase-admin/...')` chưa từng được gọi thật trong suốt tất cả các lần chạy (`FIREBASE_SERVICE_ACCOUNT` luôn không set trong môi trường test). Không sửa `vat-summary.e2e-spec.ts` (ngoài phạm vi I2) — ghi nhận lại để I3-I8 (cũng chạy full e2e nhiều lần) không nhầm tưởng đây là regression do dependency bump của mình nếu gặp lại; gợi ý cho một task dọn dẹp test riêng sau này: thêm `await prisma.saleReturn.deleteMany({ where: { sale: { storeId } } })` trước dòng xoá `sale` trong file này.
- **Kết quả xác minh cuối cùng** (trên đúng code I2, sau khi đã hiểu rõ nguyên nhân flake ở trên): `npm run build` sạch (2 lần, trước và sau vòng đối chiếu baseline). `npm run test:unit` — **12 suite/84 test pass** (2 lần). `npm run test:e2e` (container `postgres:15` tạm tên `tap-hoa-i2-e2e-pg`, cổng host `55433` — xác nhận trống trước khi dùng qua `Get-NetTCPConnection` + `docker ps`, không trùng cổng dev/Supabase/prod đang chạy trên máy) — kết quả cuối **39/39 suite, 146/146 test pass**. Xác minh migrate deploy đúng bằng SQL trực tiếp (không tin số dòng log CLI bị `rtk` rút gọn, đúng pattern đã rút ra từ I1): `SELECT count(*) FROM _prisma_migrations` → **28**, khớp đúng 28 file migration trên đĩa; `information_schema.tables` → **47 bảng**. `npx prisma db seed` bị hook `rtk` chặn nhầm y như I1 đã ghi nhận (`rtk: program not found`, exit 127) — dùng `rtk proxy npx prisma db seed` chạy thành công. Đã `docker stop`/`docker rm` container tạm ngay sau khi xong; xác nhận các container khác (`tap-hoa-prod-api`, `tap-hoa-prod-db`, `supabase_*_omni-commerce`) không bị đụng tới.
- **`npm audit --omit=dev` sau khi nâng**: 24 lỗ hổng (12 moderate/12 high), giảm so với baseline đầu file (27: 14 moderate/12 high/1 critical) — hết critical. Phần còn lại đi qua `firebase-admin` → `@google-cloud/storage` → `teeny-request` → `uuid` (moderate, GHSA-w5hq-g745-h8pq) vẫn tồn tại ở `firebase-admin@14.2.0` (bản mới nhất hiện có) — đúng như đã loại trừ ở đầu file ("Không thuộc phạm vi... phần dư nếu còn tồn tại là do lỗ hổng upstream chưa có bản vá, ghi nhận lại chứ không tự chế fix riêng"), không có bản vá lẻ nào khác để làm thêm trong I2.

## I3 — `jest`/`@types/jest` 29→30 (Thấp-Trung bình)

**DoD:**
- [x] `npm install jest@latest @types/jest@latest ts-jest@latest` (nếu `ts-jest` cũng cần khớp version) — kiểm tra `jest-unit.json`/`test/jest-e2e.json` config còn tương thích cú pháp Jest 30 (một số option đổi tên/bỏ giữa 29→30).
- [x] `npm run build` + `test:unit` + full `test:e2e` xanh — đây chính là công cụ dùng để verify các task khác, nên phải đặc biệt chắc chắn nó tự chạy đúng trước.
- [x] Cập nhật bảng Trạng thái + tick DoD.

### Ghi chú review I3

- **Version thật trước→sau**: `jest` `^29.7.0`→`^30.4.2`, `@types/jest` `^29.5.14`→`^30.0.0`. `ts-jest` **giữ nguyên** `^29.2.5` trong `package.json` — xác nhận qua `npm view ts-jest dist-tags`: package này chưa từng phát hành major 30.x, bản `latest` hiện tại vẫn là `29.4.12` nhưng đã tự thêm peerDependency `"jest": "^29.0.0 || ^30.0.0"` từ trong dòng 29.x, và bản đang cài (`npm ls ts-jest` xác nhận `29.4.12`) đã resolve đúng theo range `^29.2.5` hiện có, không cần đổi gì trong `package.json`. `npm install` không báo `ERESOLVE`/peer conflict nào.
- **Nghiên cứu breaking change 29→30 qua nguồn chính thức** (`gh api repos/jestjs/jest/releases/tags/v30.0.0` + WebFetch migration guide `jestjs.io/docs/upgrading-to-jest30`, không đoán): breaking change thật gồm — đổi CLI flag `--testPathPattern`→`--testPathPatterns`, xoá lệnh `jest --init`, đổi default `moduleFileExtensions`/`testMatch`/`testRegex` (thêm `mjs`/`cjs`/`mts`/`cts`), xoá hẳn 10 alias matcher (`toBeCalled`, `toBeCalledWith`, `toThrowError`...), đổi format snapshot (`Error.cause`, `ArrayBuffer`/`DataView`, non-enumerable properties bị loại khỏi object matching), Node tối thiểu `^18.14.0 || ^20.0.0 || ^22.0.0 || >=24.0.0` (bỏ Node 14/16/19/21/23), TypeScript tối thiểu 5.4. Đối chiếu với dự án: `apps/api/jest-unit.json` và `apps/api/test/jest-e2e.json` chỉ dùng `moduleFileExtensions`/`rootDir`/`testEnvironment`/`testRegex`/`transform`/`setupFiles` — không option nào trong số này bị đổi tên/bỏ, và vì `moduleFileExtensions`/`testRegex` được khai báo tường minh (không dùng default) nên thay đổi default value không ảnh hưởng. Đã grep toàn bộ `apps/api` xác nhận **không dùng** alias matcher nào bị xoá, không dùng `testPathPattern`/`--init`/`SpyInstance`/`MockFunctionMetadata`/`genMockFromModule`, không có file `.snap` nào (`toMatchSnapshot`/`toMatchInlineSnapshot` không xuất hiện) nên thay đổi snapshot format không có gì để vỡ. Node: máy dev `v24.18.0` (khớp `>=24.0.0`), `node:20-alpine` trong `Dockerfile` (khớp `^20.0.0`, range này chấp nhận mọi patch/minor 20.x nên không cần xác nhận patch version cụ thể). TypeScript hiện tại `^5.7.2` > 5.4, không cần đổi.
- **Không cần sửa config lẫn code**: `git diff` xác nhận chỉ `apps/api/package.json` + `apps/api/package-lock.json` thay đổi — 2 file jest config và toàn bộ `src/`/`test/` giữ nguyên 100%, không option nào phải đổi tên, không test nào phải sửa cú pháp.
- **Phát hiện phụ đáng ghi nhận (không phải rủi ro, là cải thiện)**: đối chiếu `npm audit` (đầy đủ, gồm devDependencies) trước/sau bằng cách tạm `git checkout` về `jest@29.7.0` + `npm ci` + `npm audit`, rồi khôi phục lại `jest@30.4.2` + `npm ci` + `npm audit`, diff chính xác danh sách gói bị flag: **55→54 lỗ hổng (34 high→33 high, moderate/low không đổi)**, chênh lệch duy nhất là gói `create-jest` (phụ thuộc của lệnh `jest --init`) biến mất hoàn toàn khỏi cây phụ thuộc — đúng như dự đoán từ release note ("Remove deprecated `--init` argument"), Jest 30 không còn kéo `create-jest` về nữa nên tự động dọn theo 1 lỗ hổng high. Không có lỗ hổng mới nào phát sinh từ bump này (verify bằng `comm -13` giữa 2 danh sách gói đã sort — rỗng). `npm audit --omit=dev` không đổi (vẫn 24: 12 moderate/12 high, giống số cuối I2) vì `jest` chỉ là devDependency, không nằm trong cây production.
- **Rủi ro đã kiểm tra kỹ và loại trừ, không phải của I3**: audit đầy đủ vẫn còn hiển thị advisory `GHSA-mh99-v99m-4gvg` ("brace-expansion: DoS via unbounded expansion length") ở CẢ baseline lẫn sau khi nâng — tra `gh api advisories/GHSA-mh99-v99m-4gvg` xác nhận bản vá đầu tiên là `5.0.8`, nhưng **toàn bộ** cây phụ thuộc hiện tại (kể cả `@nestjs/cli`→`glob@10.4.5`, `exceljs`→`archiver`→`glob@7.2.3`, và `jest`→`glob@10.5.0` — cả 3 nguồn độc lập) đều kéo theo `minimatch`/`brace-expansion` phiên bản thấp hơn nhiều so với `5.0.8` vì chưa có gói nào trong hệ sinh thái (kể cả bản `glob@10.5.0` mới nhất mà Jest 30 tự dùng) publish bản phụ thuộc `brace-expansion@^5` — không có bản vá lẻ khả thi, giống hệt tình huống `firebase-admin`→`uuid` đã ghi nhận ở I2, ngoài phạm vi sửa của I3 (đổi được thì phải đổi cả `@nestjs/cli` lẫn `exceljs`, không riêng jest). Tương tự advisory `GHSA-5j98-mcp5-4vw2` (glob CLI command injection, range `>=10.2.0 <10.5.0`): bản `glob@10.5.0` mà Jest 30 dùng nằm NGOÀI range này (đã vá), advisory này chỉ còn xuất hiện trong audit vì `@nestjs/cli` kéo riêng `glob@10.4.5` (nằm trong range, dính lỗi) — không liên quan jest, thuộc phạm vi I7 (nâng `@nestjs/cli`) nếu muốn giải quyết.
- **Sự cố phát sinh trong quá trình xác minh audit, đã tự phát hiện và khắc phục, không lọt vào commit**: khi tạm `git checkout`/`npm ci` qua lại để so sánh audit trước/sau, `npm ci` làm mất Prisma Client đã generate (do `@prisma/client` có `postinstall` script bị chặn bởi cơ chế `allow-scripts` của máy — không phải config repo, giống quan sát đã ghi nhận ở I1) → `npm run build` báo hơn 200 lỗi type (`Module '"@prisma/client"' has no exported member 'Role'`...) — đã xác minh ngay đây KHÔNG PHẢI do Jest 30 mà do thiếu bước `npx prisma generate` sau `npm ci`; chạy lại `npx prisma generate` xong build sạch lại ngay, unit test lại xanh 12/84. Không có gì trong sự cố này lọt vào git diff (chỉ ảnh hưởng `node_modules` cục bộ).
- **Kết quả xác minh cuối cùng**: `npm run build` sạch. `npm run test:unit` — **12 suite/84 test pass**. `npm run test:e2e` chạy **3 lần độc lập** trên 2 container `postgres:15` tạm (`tap-hoa-i3-e2e-pg` rồi `tap-hoa-i3-e2e-pg-final`, cùng cổng host `55434` — xác nhận trống qua `docker ps`/`Get-NetTCPConnection` trước khi dùng, không trùng cổng dev/Supabase/prod đang chạy), `TAP_HOA_SKIP_LOCAL_IDENTITY=1` + `DATABASE_URL` trỏ container tạm, DB reset sạch (drop/create hoặc container mới) giữa mỗi lần — cả 3 lần đều **39/39 suite, 146/146 test pass**, không lần nào gặp lại flaky `vat-summary.e2e-spec.ts` đã ghi nhận ở I2 (nên không cần áp dụng nhánh xử lý flaky trong đề bài). Xác minh migrate deploy đúng bằng SQL trực tiếp mỗi lần (không tin số dòng log CLI bị `rtk` rút gọn thành "2 migration(s) deployed"): `SELECT count(*) FROM _prisma_migrations` → **28**, khớp đúng 28 file trên đĩa. `npx prisma db seed` bị `rtk` chặn nhầm y hệt I1/I2 (`rtk: program not found`, exit 127) — dùng `rtk proxy npx prisma db seed` chạy thành công cả 2 lần seed thật. Đã `docker stop`/`docker rm` cả 2 container tạm ngay sau khi xong; xác nhận `docker ps -a` cuối cùng chỉ còn đúng các container prod/Supabase đã chạy từ trước, không đụng gì tới chúng.
- **Rủi ro thật sự**: không có. Bump major `jest` 29→30 không đụng tới bất kỳ option nào dự án đang dùng trong 2 file jest config, không cần sửa test nào, và còn tự dọn thêm 1 lỗ hổng high-severity (`create-jest`) nhờ Jest 30 bỏ lệnh `--init`. Công cụ test (`npm run test:unit`/`npm run test:e2e`) đã xác nhận chạy đúng, ổn định qua 3 lần e2e liên tiếp — đủ tin cậy làm công cụ verify cho I4-I8 tiếp theo.

## I4 — `class-validator` 0.14→0.15 (Trung bình)

**DoD:**
- [x] `npm install class-validator@latest`.
- [x] Đọc breaking-change note 0.15 — dự án dùng validator ở đâu (DTO nào có decorator thật, phân biệt với các DTO kiểu `type` thuần không dùng class-validator như đã thấy ở nhiều chỗ trong `apps/api/src/sync/dto`).
- [x] `npm run build` + `test:unit` + full `test:e2e` xanh — chú ý các test liên quan validate input (400 Bad Request cases).
- [x] Cập nhật bảng Trạng thái + tick DoD.

### Ghi chú review I4

- **Version thật trước→sau**: `class-validator` `^0.14.1` (resolve thật `0.14.4`) → `^0.15.1` (bản mới nhất hiện có, `npm view class-validator dist-tags` xác nhận `latest: 0.15.1`, `0.15.0` cũng tồn tại nhưng bị chính tác giả tái phát hành ngay vì lỗi publish — xem mục breaking-change bên dưới). `git diff` xác nhận chỉ 2 dòng đổi trong `apps/api/package.json` + `apps/api/package-lock.json` (đúng 1 entry `class-validator`), không đụng gì khác.
- **Grep toàn bộ `apps/api/src` tìm nơi dùng THẬT (không đoán)** — dùng nhiều pattern độc lập để chắc chắn: (1) `from ['"]class-validator['"]` — 0 file; (2) `from ['"]class-transformer['"]` — 0 file; (3) liệt kê tên decorator cụ thể `IsString|IsEmail|IsOptional|IsNumber|IsBoolean|ValidateNested|IsArray|IsEnum|IsUUID|IsDate|Transform|@Type\(` — 0 file; (4) chuỗi thô `class-validator`/`class-transformer` không giới hạn ngữ cảnh trên toàn bộ `apps/api` (không riêng `src`) — chỉ khớp `package.json`/`package-lock.json`, không khớp bất kỳ file `.ts` nào; (5) `ValidationPipe` (cơ chế NestJS dùng để kích hoạt class-validator tự động) trên toàn bộ `apps/api/*.ts` — 0 file, `main.ts` không có `app.useGlobalPipes(...)`. Đối chiếu bằng `Glob` toàn bộ file DTO thật có trong dự án — chỉ đúng **3 file**: `src/auth/dto/login.dto.ts` (`class LoginDto` nhưng KHÔNG có decorator nào, chỉ 2 field `phone!: string; password!: string;`), `src/customers/dto/create-customer.dto.ts` (`type` thuần), `src/sync/dto/push-sale.dto.ts` (28 `type` thuần, không phải `class` nên về mặt kỹ thuật không thể gắn decorator được). Đọc trực tiếp cả 3 file xác nhận đúng như grep. Kết luận: **0 file trong toàn bộ `apps/api/src` dùng decorator class-validator thật** — package chỉ tồn tại trong `package.json` như một *optional peer dependency* của `@nestjs/common` (xác nhận trong `package-lock.json`: `@nestjs/common` khai `"class-validator": "*"` dưới `peerDependenciesMeta.optional: true`, phục vụ `ValidationPipe` mà dự án không dùng tới), không phải do code thật sự import. Grep thêm `BadRequestException` xác nhận cơ chế trả lỗi 400 thật của dự án là hàm parse thủ công tự viết (ví dụ `src/users/user-validation.ts`: `parsePhone`/`parseName`/`parsePassword`/`parseRole`/... mỗi hàm tự `throw new BadRequestException(...)` sau khi check `typeof`/regex/length tay), xuất hiện ở 16 file — hoàn toàn độc lập với class-validator, nên bump version này về lý thuyết không có đường nào chạm tới hành vi 400 hiện tại.
- **Nghiên cứu breaking change qua nguồn chính thức** (`gh api repos/typestack/class-validator/releases` + `/pulls/<n>` trên `typestack/class-validator`, không đoán): so với `0.14.4` đang dùng, `0.15.0` (`gh api .../releases/tags/v0.15.0`) liệt kê đúng 1 thay đổi đánh dấu breaking theo conventional-commit (`!`) — PR #2618 `feat(IsIBAN)!: Add IsIBANOptions as argument for IsIBAN decorator` (đọc trực tiếp mô tả PR: thêm tham số options cho decorator `@IsIBAN()`/hàm `isIBAN()`, truyền xuống `validator.js`). Dự án **không dùng `@IsIBAN`/`isIBAN` ở bất kỳ đâu** (đã xác nhận 0 usage decorator nói chung ở mục trên) nên không bị ảnh hưởng. Các thay đổi còn lại trong 0.15.0 đều là feature cộng thêm, không phá API cũ: `IsISO31661Numeric` (decorator mới), `validateIf` (option mới cho `ValidationOptions`), mở rộng `IsUUID` hỗ trợ thêm version 1-8/nil/max/all (thuần mở rộng, không thu hẹp — dự án cũng không dùng `@IsUUID`), sửa lỗi chính tả README. `0.15.1` (`gh api .../releases/tags/v0.15.1`) chỉ là **tái phát hành kỹ thuật** của đúng `0.15.0` (tác giả ghi rõ "publish 0.15.0 for real" — bản `0.15.0` publish lần đầu bị lỗi), không có thay đổi code nào thêm. Đối chiếu `npm view class-validator@0.14.4 dependencies` vs `@0.15.1` — **giống hệt nhau**: `validator: ^13.15.22`, `@types/validator: ^13.15.3`, `libphonenumber-js: ^1.11.1` (PR #2669 "chore: bump vulnerable dependencies" nhìn qua `gh api .../pulls/2669/files` chỉ đổi `package-lock.json` NỘI BỘ của repo `class-validator` — tức devDependencies dùng để build/test chính nó — không đổi runtime dependencies publish ra ngoài). `npm view class-validator engines` trả về rỗng ở cả 2 bản — không có ràng buộc Node version mới.
- **Phát hiện phụ về bối cảnh "npm audit phát hiện lỗ hổng qua class-validator" nêu trong đề bài — báo cáo minh bạch, không phải rủi ro**: tra trực tiếp GitHub Security Advisories qua `gh api graphql` (`securityVulnerabilities(ecosystem: NPM, package: "class-validator")`) chỉ tìm thấy đúng 1 advisory từng tồn tại cho package này — `GHSA-fj58-h2fr-3pp2` ("SQL Injection and Cross-site Scripting in class-validator", CRITICAL, phạm vi `< 0.14.0`, đã vá từ `0.14.0`) — dự án đã ở `0.14.4` (cao hơn bản vá) từ trước khi task này bắt đầu, nên advisory này không còn "sống" ở baseline. `npm audit` (đầy đủ, trước khi bump) grep theo tên package "class-validator" cũng không khớp dòng nào — nghĩa là class-validator không bị audit hiện tại trực tiếp gắn cờ. Bảng Tech Stack đầu file plan cũng không liệt `class-validator` vào nhóm nguồn chính ("chủ yếu qua `@nestjs/*`, `exceljs`, `firebase-admin`") của 58/27 lỗ hổng baseline. Không có gì mâu thuẫn với việc thực hiện I4 (đã được lên kế hoạch rõ ràng, an toàn, đúng tinh thần "giữ dependency mới" của toàn kế hoạch) — chỉ ghi nhận lại để chính xác hoá: đây là nâng cấp phòng ngừa/dọn nợ kỹ thuật hợp lệ, không phải đang vá một lỗ hổng đang "sống" qua chính bump này.
- **Không cần sửa code**: đúng như dự đoán từ việc 0 file dùng decorator thật — `npm run build` sạch ngay lần đầu, không lỗi type nào. `git diff` xác nhận chỉ `apps/api/package.json` + `apps/api/package-lock.json` đổi, không đụng `src/`.
- **`npx prisma db seed` bị hook `rtk` chặn nhầm** y hệt I1/I2/I3 đã ghi nhận (`rtk: Failed to resolve 'prisma' via PATH`, `[rtk: program not found]`, exit 127) — dùng `rtk proxy npx prisma db seed` theo đúng hướng dẫn, chạy thành công cả 2 lần seed.
- **Xác minh migrate deploy đúng bằng SQL trực tiếp, không tin log CLI bị `rtk` rút gọn** (đúng pattern I1-I3): cả 2 lần chạy, `npx prisma migrate deploy` in `"2 migration(s) deployed"` nhưng `SELECT count(*) FROM _prisma_migrations` → **28** (khớp đúng 28 file migration trên đĩa qua `ls prisma/migrations | grep -c "^[0-9]"`), `information_schema.tables` → **47 bảng** — cả 2 lần đều khớp baseline I1-I3.
- **Kết quả xác minh**: `npm run build` sạch. `npm run test:unit` — **12 suite/84 test pass**. `npm run test:e2e` chạy **2 lần độc lập** trên cùng 1 container `postgres:15` tạm (`tap-hoa-i4-e2e-pg`, cổng host `55435` — xác nhận trống trước khi dùng qua `docker ps -a`/`Get-NetTCPConnection`, không trùng cổng dev (`54422`/`3040`)/Supabase (`54721-54727`)/prod (`3041`) đang chạy trên máy), reset schema sạch giữa 2 lần (`DROP SCHEMA public CASCADE; CREATE SCHEMA public;` rồi `migrate deploy` + seed lại) — **cả 2 lần đều 39/39 suite, 146/146 test pass**, không lần nào gặp lại flaky `vat-summary.e2e-spec.ts` đã ghi nhận ở I2 (nên không cần dùng nhánh xử lý flaky nêu trong đề bài), gồm cả các test 400 Bad Request liên quan validate input (`auth.e2e-spec.ts`, `users.e2e-spec.ts`, `product-upserts.e2e-spec.ts`, `sync-push.e2e-spec.ts`,...). Đã `docker stop`/`docker rm` container tạm ngay sau khi xong; `docker ps -a` cuối cùng xác nhận chỉ còn đúng các container prod/Supabase đã chạy từ trước, không đụng gì tới chúng.
- **`npm audit --omit=dev` sau khi nâng**: **24 lỗ hổng (12 moderate/12 high)** — không đổi so với số cuối I2/I3 (đúng như dự đoán ở mục "phát hiện phụ" bên trên, vì class-validator không nằm trong đường lỗ hổng production nào và runtime dependencies của nó không đổi giữa 0.14.4/0.15.1).
- **Rủi ro thật sự**: không có. Đây là trường hợp bump "an toàn tuyệt đối" hiếm gặp trong đợt I1-I8 — không chỉ build/test xanh mà còn xác nhận được TẠI SAO chắc chắn xanh (0 điểm chạm code thật), khác với I1-I3 vốn có code thật sự gọi API của package được bump. Đánh giá "Trung bình" trong bảng Tech Stack đầu file (dựa trên giả định "dùng khắp DTO") không khớp thực tế đã xác minh — tương tự phát hiện lệch giữa bảng kế hoạch ban đầu và thực tế đã ghi nhận ở I1 (version `@types/supertest`).

## I5 — `@types/express` 4→5 (Trung bình)

**DoD:**
- [x] Xác nhận trước: `express` runtime thật có được cài trực tiếp trong `package.json` hay chỉ đến qua `@nestjs/platform-express` (NestJS 10 bundle Express 4 nội bộ) — nếu chỉ nâng `@types/express` lên 5 mà runtime vẫn là Express 4 thật, có thể type không khớp runtime, cần cân nhắc hoãn tới sau khi NestJS 11 (I7, tự mang theo Express tương thích) thay vì làm riêng lẻ trước.
- [x] Nếu xác nhận an toàn: `npm install @types/express@latest`, sửa lỗi type nếu build đỏ.
- [x] Nếu xác nhận rủi ro (type/runtime lệch pha) cao hơn lợi ích trước khi I7 xong: dừng, ghi rõ lý do, để version cũ, coi là kết luận hợp lệ — làm lại sau I7 nếu còn cần. *(Không áp dụng nhánh này — xem "Ghi chú review I5": đã xác nhận an toàn, tiến hành nâng.)*
- [x] `npm run build` + `test:unit` + full `test:e2e` xanh (nếu tiến hành nâng cấp).
- [x] Cập nhật bảng Trạng thái + tick DoD.

### Ghi chú review I5

- **Xác nhận express runtime thật KHÔNG phải dependency trực tiếp**: `grep '"express"' apps/api/package.json` chỉ khớp `"@nestjs/platform-express"` và `"@types/express"` — không có dòng `"express":` riêng ở cả `dependencies` lẫn `devDependencies`. `npm ls express` xác nhận nguồn duy nhất: `tap-hoa-api@0.1.0 -> @nestjs/platform-express@10.4.22 -> express@4.22.1`. Đọc trực tiếp `node_modules/@nestjs/platform-express/package.json#dependencies` xác nhận `@nestjs/platform-express@10.4.22` **ghim cứng** (không dùng `^`/`~`) `"express": "4.22.1"` — Express 4.x thật, không có khả năng tự trôi lên 5.x cho tới khi bump `@nestjs/platform-express` (I7; đối chiếu `npm view @nestjs/platform-express@11 dependencies` xác nhận từ bản `11.0.0` trở đi mới đổi sang `express: 5.0.1`, tăng dần tới `5.2.1` ở bản mới nhất `11.1.28`).
- **Đọc breaking change chính thức Express 4→5** (WebFetch `expressjs.com/en/guide/migrating-5.html`, không đoán): đổi cú pháp route pattern (`/*` → `/*splat`, optional param `:file.:ext?` → `:file{.:ext}`, bỏ regex trong path string), xoá/đổi method (`app.del`→`app.delete`, `req.param()` bị xoá, `res.redirect('back')` bị xoá, `res.send(number)` bị xoá, pluralize `req.acceptsCharset`→`acceptsCharsets`...), đổi thứ tự tham số (`res.json(obj, status)`→`res.status(status).json(obj)`, `res.redirect(url, code)`→`res.redirect(code, url)`), và đặc biệt liên quan đúng rủi ro nêu trong đề bài: **promise reject trong route handler async được Express 5 tự động bắt và chuyển vào error handler** (Express 4 không tự bắt, phải `.catch(next)` thủ công), `req.query` đổi từ ghi được sang **read-only** (throw ở v5, v4 cho gán bình thường), `req.params` cho wildcard route đổi từ chuỗi (`{'0': 'foo/bar'}`) sang mảng (`{splat: ['foo','bar']}`).
- **Đọc trực tiếp PR gốc trên DefinitelyTyped tạo ra `@types/express@5`** (`gh pr view 70563 --repo DefinitelyTyped/DefinitelyTyped`, không đoán): tác giả PR ghi rõ *"The primary goal of this PR is to release the types for express version 5 — the main difference is allowing `Promise<void>` along with `void` on `RequestHandler`"* (trích dẫn thẳng `expressjs.com/en/guide/migrating-5.html#rejected-promises`). Đây chính là điểm khớp với rủi ro "type khai báo semantics Express 5 (tự bắt promise reject) trong khi runtime thật là Express 4 (không tự bắt)" nêu trong đề bài — nếu có handler kiểu `express.RequestHandler` async không tự `.catch(next)`, `@types/express@5` sẽ cho compile pass trong khi runtime Express 4 thật NUỐT LỖI (unhandled rejection, request treo, không vào exception filter) — đúng kịch bản "type-check pass nhưng runtime khác" cần đánh giá kỹ.
- **Nhưng: xác nhận rủi ro trên KHÔNG chạm được vào code thật của dự án** — grep toàn bộ `apps/api` (không riêng `src/`, mọi file `.ts`, nhiều pattern độc lập):
  - `from 'express'` / `from "express"` / `require('express')` — **0 file**.
  - `declare global` / `namespace Express` / `RequestHandler` / `NestExpressApplication` / `express.Router` / `express-serve-static-core` — **0 file** trong toàn bộ `apps/api` (loại trừ `node_modules`).
  - `@Res()` — **0 usage** ở bất kỳ đâu (dự án không tự tay thao tác response object).
  - `@Req()` — **72 lần dùng** trên 10 controller (`customers`, `devices`, `einvoice`, `ledger`, `reports`, `shifts`, `stores`, `suppliers`, `sync`, `users`), xác nhận qua `Grep -o` đếm chính xác, và đọc lại toàn bộ 72 dòng xác nhận **100% type bằng shape tối giản tự định nghĩa** `req: { user: AuthUser }` — không một chỗ nào type bằng `Request` của `express`.
  - 3 file guard (`jwt-auth.guard.ts`, `einvoice-permission.guard.ts`, `ledger-permission.guard.ts`) dùng `context.switchToHttp().getRequest<{ user?: AuthUser }>()` — generic tự định nghĩa, không phải `express.Request`.
  - `main.ts` gọi `NestFactory.create(AppModule)` thuần (không dùng generic `NestExpressApplication`), không đụng raw Express instance qua `getHttpAdapter().getInstance()`.
  - Route decorator (`@Get/@Post/@Put/@Delete/@Patch/@All`) trong toàn bộ `src/` — **0 route** dùng wildcard `*` hay optional-param `?` trong path string (loại luôn rủi ro đổi cú pháp route pattern, dù đằng nào cũng không áp dụng vì express runtime không đổi trong task này).
  - Kết luận: dự án dùng NestJS decorator + shape tự định nghĩa xuyên suốt, chưa từng type bất kỳ thứ gì bằng `express.Request/Response/NextFunction/RequestHandler` thật — việc bắt lỗi async trong controller là do **NestJS tự làm** (cơ chế nội bộ `@nestjs/core`, tồn tại từ trước khi Express 5 ra đời, không phụ thuộc Express tự bắt reject), nên thay đổi kiểu `RequestHandler` của `@types/express@5` không có nơi nào trong `apps/api/src` để "lừa" trình biên dịch.
  - `apps/api/package.json` xác nhận `@types/express` chỉ nằm trong `devDependencies`; nguồn tiêu thụ thật trong cây phụ thuộc là các gói type của passport (`@types/passport-jwt`→`@types/passport-strategy`→`@types/passport`→`@types/express`, xác nhận qua `npm ls @types/express` trước khi nâng), không phải do `apps/api/src` tự import — cùng dạng "ghost dependency" đã thấy ở I4 (`class-validator`).
- **Xác minh thực nghiệm (không chỉ tin phân tích lý thuyết)**: `npm install @types/express@latest` → resolve `^4.17.21`→`^5.0.6` (`npm view @types/express dist-tags` xác nhận `latest: 5.0.6`), **không có cảnh báo `ERESOLVE`/peer conflict nào**. `npm run build` ngay sau đó **sạch tuyệt đối, exit code 0 xác nhận rõ ràng, không có dòng log lỗi nào, không cần sửa 1 dòng code nào** trong `src/`. `git diff --stat` xác nhận chỉ `package.json` (1 dòng) + `package-lock.json` (42 dòng, do đổi resolve `@types/express`/`@types/express-serve-static-core`/`@types/serve-static`/`@types/qs`/`@types/body-parser`) thay đổi.
- **Phát hiện phụ quan trọng, làm rõ bối cảnh "npm audit gợi ý nâng @types/express" trong đề bài**: `npm audit --json` (đầy đủ, gồm dev) sau khi nâng vẫn liệt `express` (gói RUNTIME thật, không phải `@types/express`) là gói bị flag — `severity: moderate`, `via: ["qs"]`, `range: "4.21.0 - 4.22.1 || 5.0.0-alpha.1 - 5.0.1"` (khớp đúng bản `4.22.1` đang chạy thật), và **`fixAvailable` ghi rõ duy nhất cách vá là nâng `@nestjs/platform-express` lên `11.1.28` (`isSemVerMajor: true`)** — tức phạm vi I7, không phải I5. Xác nhận bằng dữ liệu thật: **nâng `@types/express` (I5) không vá được lỗ hổng `express`/`qs` này dù chỉ một chút**, vì `@types/express` chỉ chứa type declaration (`.d.ts`), không có code runtime, không tự nó mang theo `qs` hay bất kỳ dependency runtime nào ảnh hưởng advisory — I5 là nâng cấp type-hygiene chuẩn bị cho I7, KHÔNG phải bản vá bảo mật độc lập (không mâu thuẫn với mô tả "Trung bình — cần xác nhận express runtime thật có bị ảnh hưởng qua NestJS không" ở bảng Tech Stack đầu file, chỉ làm rõ thêm). `npm audit --omit=dev` sau khi nâng: **24 lỗ hổng (12 moderate/12 high)** — không đổi so với số cuối I2/I3/I4 (đúng dự đoán, vì `@types/express` là devDependency, không tự nó thay đổi gói runtime nào trong cây production).
- **KẾT LUẬN — tiến hành nâng cấp (không hoãn)**: rủi ro "type Express 5 khai báo semantics không khớp runtime Express 4 thật" có thật về mặt lý thuyết (xác nhận qua chính lời tác giả PR DefinitelyTyped: thay đổi cốt lõi là `RequestHandler` cho phép `Promise<void>`, phản ánh hành vi tự bắt reject của Express 5) nhưng **hoàn toàn không có điểm chạm nào trong `apps/api/src`** để rủi ro đó biểu hiện — dự án chưa từng type bất kỳ thứ gì bằng kiểu thật của `express` (100% dùng shape tự định nghĩa qua NestJS decorator). Đây là tình huống tương tự I4 (0 điểm chạm code thật → bump an toàn), KHÔNG phải tình huống H6 (nơi rủi ro thật sự áp dụng lên hành vi code đang có) — không đủ căn cứ để dừng lại theo đúng tinh thần "chỉ dừng khi có bằng chứng rủi ro rõ ràng" của đề bài.
- **Kết quả xác minh cuối cùng**: `npm run build` sạch (exit code 0). `npm run test:unit` — **12 suite/84 test pass**. `npm run test:e2e` (container `postgres:15` tạm tên `tap-hoa-i5-e2e-pg`, cổng host `55436` — xác nhận trống trước khi dùng qua `docker ps -a`/`netstat`, không trùng cổng dev (`54422`/`3040`)/Supabase (`54721-54727`)/prod (`3041`, `5432` nội bộ) đang chạy trên máy), `TAP_HOA_SKIP_LOCAL_IDENTITY=1` + `DATABASE_URL` trỏ container tạm — **lần chạy đầu gặp đúng flaky `vat-summary.e2e-spec.ts` đã biết từ I2** (`Foreign key constraint violated: SaleReturn_originalSaleId_fkey` tại `prisma.sale.deleteMany()`, do dữ liệu `SaleReturn` sót lại từ suite chạy trước trong cùng lượt, phụ thuộc thứ tự Jest sequencer — 38/39 suite, 145/146 test pass ở lần 1); theo đúng hướng dẫn, `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` reset sạch, `migrate deploy` (xác nhận **28/28** migration qua `SELECT count(*) FROM _prisma_migrations` — không tin số dòng log CLI "2 migration(s) deployed" bị `rtk` rút gọn, đúng pattern đã rút ra từ I1-I4) + `rtk proxy npx prisma db seed` (seed thẳng bị `rtk` chặn nhầm `[rtk: program not found]` exit 127, y hệt I1-I4) lại từ đầu, chạy `test:e2e` lần 2 → **39/39 suite, 146/146 test pass**, xác nhận rõ ràng lỗi lần 1 là flaky tiền tồn tại không liên quan tới bump `@types/express` (giống hệt cách I2 đã xác minh). Đã `docker stop`/`docker rm` container tạm ngay sau khi xong; `docker ps -a` cuối cùng xác nhận chỉ còn đúng các container prod/Supabase đã chạy từ trước (`tap-hoa-prod-api`, `tap-hoa-prod-db`, `supabase_*_omni-commerce`, `supabase_*_tap-hoa` đã exited từ trước), không đụng gì tới chúng.
- **Khuyến nghị cho I7**: khi nâng `@nestjs/platform-express` lên 11 (kéo theo `express@5.x` runtime thật), cần đọc lại đúng breaking change đã liệt kê ở trên (đặc biệt `req.query` read-only, wildcard route param đổi từ string sang array, promise reject tự động vào error handler) — dù I5 xác nhận KHÔNG có điểm chạm type ở code hiện tại, các hành vi RUNTIME này vẫn có thể ảnh hưởng nếu I7 phát sinh code mới dùng các API đó; hiện tại chưa dùng nên chưa cần sửa gì trước, chỉ cần lưu ý khi review I7.

## I6 — `typescript`/`@types/node` (Cao)

**DoD:**
- [x] Xác nhận version TypeScript mới nhất **tương thích với @nestjs/core@11** (I7 sắp làm ngay sau) trước khi chọn target — đọc `npm view @nestjs/core@11 peerDependencies`/`devDependencies` liên quan `typescript`, đừng nâng TS lên bản NestJS 11 chưa hỗ trợ.
- [x] `npm install typescript@<bản đã xác nhận tương thích> @types/node@<bản khớp Node runtime thật đang dùng — kiểm tra `node -v` cả trên máy dev lẫn base image `node:20-alpine` trong Dockerfile, đừng nâng @types/node vượt quá version Node thật>`.
- [x] `npm run build` — TypeScript major bump thường lộ ra lỗi type mới bị chặt hơn; sửa tối thiểu để build sạch, không refactor thêm.
- [x] `npm run test:unit` + full `test:e2e` xanh.
- [x] Cập nhật bảng Trạng thái + tick DoD.

### Ghi chú review I6

- **QUYẾT ĐỊNH CHÍNH — KHÔNG lên TypeScript 7.x, giữ nguyên `5.9.3`** (đã là bản mới nhất dòng 5.x, trùng khớp với bản project đã có sẵn qua range `^5.7.2` cũ — không phải "không làm gì", mà là kết luận CHỦ ĐỘNG sau khi xác minh thật, không đoán):
  - **Xác nhận TypeScript 7.x là có thật, không phải giả định** (đúng như đề bài cảnh báo phải tự tìm hiểu): `npm view typescript dist-tags` → `latest: "7.0.2"`, `rc: "7.0.1-rc"`. WebSearch + đọc trực tiếp `devblogs.microsoft.com/typescript/announcing-typescript-7-0` xác nhận: TypeScript 7.0 là bản viết lại HOÀN TOÀN compiler+language service bằng Go (dự án "Project Corsa", repo `microsoft/typescript-go`), GA ngày 8/7/2026, cài đặt vẫn qua `npm install -D typescript` bình thường (không cần package riêng nữa như giai đoạn beta `@typescript/native-preview`/`tsgo`).
  - **Rào cản kỹ thuật CỨNG, xác nhận thật qua `npm view` — không phải suy đoán**: `ts-jest@latest` (29.4.12 — chính là transformer dự án đang dùng cho CẢ `jest-unit.json` lẫn `test/jest-e2e.json`, tức công cụ verify DoD của chính I6) khai `peerDependencies.typescript: ">=4.3 <7"` — loại trừ THẲNG TypeScript 7.x. Cài `typescript@7.0.2` sẽ gây xung đột peer dependency với `ts-jest` đang dùng, phá cả `test:unit` lẫn `test:e2e`. Đối chiếu với đúng nguyên nhân kỹ thuật: bài đăng chính thức `announcing-typescript-7-0` xác nhận TypeScript 7.0 CHƯA có API Node.js gọi được ổn định ("lacks a stable API surface") — các công cụ cần gọi compiler API theo kiểu chương trình (programmatic API, đúng cách `ts-jest`/`ts-node` hoạt động: gọi `ts.transpileModule`/`ts.createProgram` để transpile in-memory) chưa dùng được TypeScript 7 cho tới bản 7.1 (lộ trình "mỗi 3-4 tháng" sau 7.0 — tức sớm nhất cuối 2026). `ts-node` (dự án dùng cho `npm run create-owner` và `prisma db seed` — cả hai đều nằm trên đường chạy e2e/seed thật) tuy khai peer lỏng hơn (`typescript: ">=2.7"`, không giới hạn trên) nhưng dùng chung cơ chế compiler API nên rủi ro thực tế tương tự, chỉ là chưa bị `npm install` chặn cứng.
  - **NestJS 11 (bản mới nhất, kể cả trên `master` — chưa release — chứ không chỉ bản cũ) xác nhận CHÍNH THỨC vẫn dùng TypeScript 5.9.3 nội bộ, chưa hề chạm 6.x/7.x**: `npm view @nestjs/cli@latest dependencies` (bản `11.0.24`, dist-tag `latest` — đây là dependency THẬT, không phải devDependency, vì `@nestjs/cli` tự gọi `tsc` để build project khi không dùng SWC) → `"typescript": "5.9.3"` — khớp CHÍNH XÁC bản đang chọn. Đào sâu hơn, không chỉ tin bản đã publish: `gh api repos/nestjs/nest/contents/package.json` đọc thẳng `package.json` gốc của monorepo `nestjs/nest` trên branch `master` (bản đang phát triển, version field ghi `11.1.26`, mới hơn cả bản I7 sắp cài) → `devDependencies.typescript: "5.9.3"`, `engines.node: ">= 20"` — xác nhận đội NestJS TỰ HỌ build/test toàn bộ framework bằng đúng TypeScript 5.9.3, kể cả ở thời điểm hiện tại (chưa hề nâng lên 6.x hay 7.x dù cả hai đã tồn tại trên npm). `@nestjs/core@11`/`@nestjs/common@11` không khai `typescript` trong `peerDependencies` (framework runtime không ràng buộc trực tiếp vào TS — hợp lý vì output cuối là JS thuần), nên tín hiệu đáng tin nhất là từ tooling thật (`@nestjs/cli`, và chính source repo) chứ không phải từ `peerDependencies` của `@nestjs/core` (gần như trống rỗng theo hướng này).
  - **Đối chiếu thêm TypeScript 6.0 (cũng đã stable, `6.0.2`/`6.0.3` trên npm) — cân nhắc rồi loại, không chọn**: WebSearch xác nhận TypeScript 6.0 là "bản cuối cùng dựa trên JavaScript trước khi Go-native 7.0 tiếp quản" (transitional release, vẫn dùng compiler API kiểu cũ nên về lý thuyết `ts-jest` vẫn chấp nhận vì nằm trong `>=4.3 <7`) — nhưng KHÔNG có nguồn NestJS chính thức nào xác nhận đã test/hỗ trợ 6.x (cả `@nestjs/cli@latest` lẫn `nestjs/nest@master` đều vẫn ghim `5.9.3`, không phải `6.x`), và 6.0 tự mình đổi default nhiều flag tsconfig (strict mặc định bật, `target` mặc định `es2025`, bỏ `baseUrl`/ES5 target...) — rủi ro không cần thiết khi mục tiêu I6 chỉ là "bản an toàn nhất cho pipeline I6+I7+I8", không phải "bản mới nhất miễn còn <7". Bám sát đúng version NestJS tự dùng nội bộ (`5.9.3`) là lựa chọn ít rủi ro nhất, đặc biệt vì I7 (NestJS 10→11) chạy NGAY SAU.
  - **Xác nhận `5.9.3` đã là bản MỚI NHẤT trong dòng 5.x** (không bỏ sót bản 5.x mới hơn nào): liệt kê toàn bộ version ổn định (không phải dev/rc/beta) từ `npm view typescript versions --json`, lọc `>=5.7`: `5.7.2, 5.7.3, 5.8.2, 5.8.3, 5.9.2, 5.9.3` rồi nhảy thẳng `6.0.2, 6.0.3, 7.0.2` — không có `5.10.x`. `5.9.3` đúng là bản 5.x cuối cùng, cũng chính là bản ĐÃ được resolve sẵn trong `package-lock.json` từ trước (dưới range cũ `^5.7.2`) — nâng cấp thực tế ở đây là XÁC NHẬN + GHIM RÕ quyết định (đổi floor range `^5.7.2` → `^5.9.3` trong `package.json` để phản ánh đúng bản đã kiểm chứng, tránh mơ hồ về sau), không phải một version mới thật sự khác trên đĩa.
  - **Kết luận, trả lời thẳng câu hỏi "vì sao KHÔNG lên 7.0.2"**: không phải vì ngại rủi ro chung chung, mà vì có bằng chứng kỹ thuật cụ thể — (1) `ts-jest` (công cụ chạy chính `test:unit`/`test:e2e` của dự án) khai peer dependency loại trừ thẳng `<7`; (2) NestJS 11 — cả bản đã publish lẫn bản đang phát triển trên `master` — tự dùng `5.9.3` nội bộ, chưa hề kiểm thử với 6.x/7.x; (3) TypeScript 7.0 tự nhận chưa có API ổn định cho công cụ nhúng (embedding tools) như `ts-jest`/`ts-node` đang cần, dời sang bản 7.1 tương lai. Đây đúng tinh thần "để lại TypeScript 7.x cho tương lai" mà đề bài đã gợi ý làm phương án dự phòng hợp lệ — không phải bỏ cuộc, mà là kết luận có căn cứ.

- **@types/node: HẠ từ `22.x` xuống `20.x`** (không phải nâng — đảo ngược hướng so với bảng Tech Stack ban đầu ghi mục tiêu `26.x`, một phát hiện quan trọng của việc xác minh runtime thật):
  - **Xác nhận runtime thật ở CẢ 2 nơi theo đúng yêu cầu đề bài, không đoán**: máy dev (`node -v`) → `v24.18.0`. Base image Docker `apps/api/Dockerfile` (dòng 1 build stage + dòng 12 runtime stage, cả hai đều `FROM node:20-alpine`, tag KHÔNG ghim patch cụ thể) — kéo thử THẬT bằng `docker run --rm node:20-alpine node -v` (không chỉ tra doc suông) → **`v20.20.2`** (digest `sha256:fb4cd12c...`, `docker image inspect` báo `Created: 2026-04-15`). Đối chiếu thêm nguồn thứ 3 không có trong đề bài nhưng phát hiện thêm khi rà `apps/api`: `.github/workflows/ci.yml` dòng 40, step `actions/setup-node@v4` của job `api` (chạy đúng `npm run build`/`test:unit`/`test:e2e` — pipeline CI thật) ghim cứng `node-version: 20`.
  - **2/3 nguồn thật (Docker production runtime + CI) đồng nhất ở Node 20.x; chỉ máy dev sandbox lệch (Node 24)** — không có `.nvmrc`, không có `engines` field nào trong `apps/api/package.json` khai Node 24 hay bất kỳ version nào khác — Node 24 trên máy dev nhiều khả năng chỉ là version cài sẵn ngẫu nhiên trên máy/VM cụ thể này, không phải quyết định có chủ đích của dự án. Vì type-check là kiểm tra tĩnh toàn chương trình, hướng AN TOÀN là chọn version Node THẤP NHẤT trong số các runtime thật đang tồn tại (Node 20) — code compile sạch dưới `@types/node@20.x` chỉ dùng API tồn tại ở Node 20, chạy đúng trên CẢ máy dev (Node 24, tập hợp cha của Node 20) LẪN Docker/CI/production (đúng Node 20). Ngược lại nếu ghim `@types/node@22.x` (baseline cũ trước I6) hay `@26.x` (bản "mới nhất" trên npm), trình biên dịch sẽ cho phép dùng API chỉ có ở Node 21/22/24+ — build sạch cục bộ nhưng có thể CRASH thật khi chạy trên Docker/CI/production (đúng môi trường quan trọng nhất) — đây chính là rủi ro "type khớp npm nhưng không khớp runtime" mà đề bài cảnh báo.
  - **Phát hiện phụ quan trọng**: baseline TRƯỚC I6 đã là `@types/node@^22.10.2` (resolve `22.20.1`) — tức đã LỆCH PHA với runtime Node 20 thật (Docker+CI) từ trước khi I6 bắt đầu, một rủi ro tiềm ẩn chưa từng được soi theo đúng tiêu chí "khớp Node thật" này trước đây (bảng Tech Stack đầu file ghi mục tiêu ban đầu còn định nâng tiếp lên `26.x`, tức đi càng xa hướng đúng hơn). I6 sửa lại đúng hướng bằng cách HẠ xuống `20.x` — không phải một nâng cấp thông thường, mà là một sửa sai lệch (correction) được phát hiện qua chính quy trình xác minh của task này.
  - **Chọn bản patch cụ thể trong dòng 20.x**: liệt kê toàn bộ 259 version `20.x` đã publish của `@types/node` qua `npm view @types/node versions --json`, sort semver đúng cách (không tin thứ tự trả về mặc định) → bản mới nhất là **`20.19.43`** (không có nhánh `20.20.x` nào được publish riêng — số hiệu `@types/node` không đi song song 1-1 với số hiệu patch của Node runtime, đây là quy ước bình thường của DefinitelyTyped, dùng bản 20.x mới nhất sẵn có là đúng chuẩn, không cần khớp chính xác `20.20.2`). Kiểm tra chéo không có rủi ro cú pháp TypeScript ở chiều ngược lại: `npm view @types/node dist-tags` có `"ts5.9": "26.1.2"` (bản `@types/node` mới nhất mà cú pháp `.d.ts` của nó còn được TypeScript 5.9 hiểu) — nghĩa là kể cả `26.1.2` cũng không có vấn đề CÚ PHÁP gì với TypeScript 5.9.3 đang chọn; lý do loại `26.x` thuần túy là lệch RUNTIME (API surface) như phân tích trên, không phải do TypeScript không parse nổi.

- **Thực thi & kết quả xác minh**: `npm install typescript@5.9.3 @types/node@20.19.43` — cài sạch, không `ERESOLVE`/peer conflict nào (chỉ cảnh báo `npm warn allow-scripts` quen thuộc từ máy cục bộ, đã ghi nhận từ I1, không liên quan I6). Log cài đặt báo "changed 1 package" — đối chiếu `package-lock.json` xác nhận đúng: `typescript` giữ nguyên `5.9.3` (đã resolve sẵn từ trước, không có gì đổi trên đĩa), chỉ `@types/node` đổi thật từ `22.20.1`→`20.19.43`. `git diff --stat`: chỉ `apps/api/package.json` (4 dòng, 2 entry đổi) + `apps/api/package-lock.json` (10 dòng) — **0 thay đổi trong `src/` hay bất kỳ file code nào khác**.
  - `npm run build`: sạch, exit code 0, không lỗi type nào — kể cả sau khi HẠ `@types/node` từ 22.x xuống 20.x (đúng hướng dễ lộ lỗi nhất nếu code có dùng API Node 21/22-only), xác nhận `apps/api/src` chưa từng thực sự dùng API nào riêng của Node >20 — codebase vốn đã "an toàn với Node 20" một cách tình cờ, chỉ có phần khai type trước đây khai rộng hơn thực tế cần. Không cần sửa dòng code nào (khác với dự đoán trong DoD gốc "TypeScript major bump thường lộ lỗi type mới" — không áp dụng ở đây vì quyết định cuối cùng KHÔNG nâng major TypeScript, chỉ xác nhận lại bản 5.x đã có).
  - `npm run test:unit`: **12 suite/84 test pass** — khớp baseline I1-I5.
  - `npm run test:e2e`: chạy **2 lần độc lập** trên 1 container `postgres:15` tạm (`tap-hoa-i6-e2e-pg`, cổng host `55437` — xác nhận trống qua `netstat` trước khi dùng, tiếp đúng dãy cổng tăng dần I1-I5 dùng `55432-55436`, không trùng cổng dev (`54422`/`3040`)/Supabase (`54721-54727`)/prod (`3041`) đang chạy), reset schema sạch giữa 2 lần (`DROP SCHEMA public CASCADE; CREATE SCHEMA public;` + `migrate deploy` + `db seed`) — **cả 2 lần đều 39/39 suite, 146/146 test pass**, KHÔNG lần nào gặp lại flaky `vat-summary.e2e-spec.ts` đã biết từ I2 (nên không cần thêm lần chạy thứ 3). Xác minh migration bằng SQL trực tiếp cả 2 lần: `SELECT count(*) FROM _prisma_migrations` → **28**, `information_schema.tables` → **47 bảng** — khớp đúng 28 file migration trên đĩa. Lần này `npx prisma migrate deploy` in đầy đủ danh sách 28 migration (không bị `rtk` rút gọn như I1-I5 từng ghi nhận) và `npx prisma db seed` cũng chạy thẳng không bị hook `rtk` chặn (khác I1-I5 phải dùng `rtk proxy` — có thể do thay đổi trạng thái/cache cục bộ của `rtk`, không phải do thay đổi của I6, chỉ ghi nhận lại quan sát). Đã `docker stop`/`docker rm` container tạm ngay sau khi xong; `docker ps -a` cuối cùng xác nhận chỉ còn đúng các container prod/Supabase đã chạy từ trước, không đụng gì tới chúng.
  - `npm audit --omit=dev` sau khi nâng: **24 lỗ hổng (12 moderate/12 high)** — không đổi so với số cuối I2-I5 (đúng dự đoán, `typescript`/`@types/node` chỉ là devDependency, không nằm trong cây production).

- **Rủi ro thật sự**: không có rủi ro mới phát sinh — I6 là một quyết định "giữ vững phòng tuyến" có chủ đích (không lên TypeScript 7.x, không chạy theo "mới nhất tuyệt đối" của `@types/node`) hơn là một bump thông thường, và còn SỬA được một lệch pha runtime tiềm ẩn (`@types/node` 22.x trên nền Node 20 thật) đã tồn tại từ trước I6. Không cần sửa code nào trong `src/`.
- **Ghi chú cho I7** (NestJS 10→11, chạy ngay sau): TypeScript đang ở đúng `5.9.3` — CHÍNH XÁC bản mà `@nestjs/cli@11.0.24` (dependency thật) và `nestjs/nest@master` (devDependency, đang phát triển) tự dùng nội bộ, nên I7 không nên gặp bất ngờ nào về phía TypeScript. Đã tiện thể xác nhận thêm (ngoài phạm vi sửa của I6, chỉ ghi nhận cho I7 dùng): `@nestjs/cli@latest` yêu cầu `engines.node: ">= 20.11"` — Node thật trong Docker (`20.20.2`, xác nhận ở trên) thỏa mãn; `@nestjs/platform-express@11` (bản mới nhất `11.1.28`) mang theo `express@5.2.1` làm dependency thật (không còn ghim `4.22.1` như NestJS 10) — đúng như I5 đã cảnh báo trước, I7 cần đọc lại các breaking change Express 5 đã liệt kê trong "Ghi chú review I5" (`req.query` read-only, wildcard route param đổi thành mảng, promise reject tự động vào error handler) khi review.

## I7 — `@nestjs/*` 10→11 (Cao)

**DoD:**
- [x] Đọc migration guide chính thức NestJS 10→11 (breaking changes: Express version bump nội bộ, thay đổi decorator/DI nếu có, thay đổi `@nestjs/testing` API nếu có).
- [x] `npm install @nestjs/common@11 @nestjs/core@11 @nestjs/platform-express@11 @nestjs/jwt@11 @nestjs/testing@11 @nestjs/cli@11` (đồng bộ tất cả cùng lúc theo đúng peer dependency, không nâng lẻ tẻ).
- [x] Sửa mọi lỗi build phát sinh (breaking API) — tối thiểu, không refactor thêm ngoài phạm vi bắt buộc để build xanh. *(Không phát sinh lỗi build nào — xem "Ghi chú review I7": 0 điểm chạm code thật sau khi rà từng breaking change chính thức với codebase.)*
- [x] `npm run build` + `test:unit` + full `test:e2e` xanh — đây là thay đổi core framework, rủi ro regression cao nhất đợt này, chạy full e2e nhiều lần trên DB sạch khác nhau như các task H trước.
- [x] Cập nhật bảng Trạng thái + tick DoD.

### Ghi chú review I7

- **Version thật trước→sau** (đối chiếu `git diff apps/api/package.json` + `npm ls`, không phải số trong bảng Tech Stack đầu file): `@nestjs/common` `^10.4.15` (resolve thật `10.4.22`) → `^11.1.28`; `@nestjs/core` `^10.4.15` (`10.4.22`) → `^11.1.28`; `@nestjs/platform-express` `^10.4.15` (`10.4.22`) → `^11.1.28`; `@nestjs/jwt` `^10.2.0` → `^11.0.2`; `@nestjs/testing` `^10.4.15` (`10.4.22`) → `^11.1.28`; `@nestjs/cli` `^10.4.9` → `^11.0.24`. `@nestjs/passport` **không đổi** (`^11.0.5`, đã ở dòng 11.x từ trước task này — peer dependency của nó (`npm view @nestjs/passport@11.0.5 peerDependencies` → `@nestjs/common: "^10.0.0 || ^11.0.0"`) chấp nhận cả 10.x lẫn 11.x nên không có xung đột trước/sau). `npm install` một lệnh duy nhất theo đúng 6 package DoD yêu cầu, **0 cảnh báo `ERESOLVE`/peer conflict** — log cài đặt: "added 50 packages, removed 80 packages, changed 52 packages". `git diff --stat apps/api/package.json apps/api/package-lock.json`: đúng 12 dòng đổi trong `package.json` (6 entry dependencies/devDependencies) + 2592 dòng trong `package-lock.json` — **0 dòng đổi trong `src/` hay `test/`**.
- **Đọc migration guide chính thức, không đoán** (2 nguồn độc lập, đối chiếu chéo): (1) `content/migration.md` trên repo chính thức `nestjs/docs.nestjs.com` (branch `master`, chính là nguồn dựng nên trang `docs.nestjs.com/migration-guide` — trang này là SPA nên fetch trực tiếp URL không lấy được nội dung server-rendered, phải lấy đúng file markdown gốc qua `raw.githubusercontent.com/nestjs/docs.nestjs.com/master/content/migration.md`); (2) release notes chính thức `gh api repos/nestjs/nest/releases/tags/v11.0.0` trên `nestjs/nest`. Tổng hợp các breaking change liệt kê trong guide:
  - **Node.js**: tối thiểu **v20** (bỏ hẳn v16/v18). Đã xác nhận đủ điều kiện từ I6 (Docker `node:20-alpine` → `20.20.2` thật, CI `.github/workflows/ci.yml` ghim `node-version: 20`, máy dev `v24.18.0`) — I7 không phát sinh thêm yêu cầu Node mới nào (`@nestjs/core@11.1.28` engines `>= 20`, `@nestjs/cli@11.0.24` engines `>= 20.11`, cả hai đều thấp hơn hoặc bằng ngưỡng I6 đã xác nhận; `@nestjs/common`/`@nestjs/platform-express`/`@nestjs/testing`/`@nestjs/jwt` không khai `engines` riêng).
  - **Express v5 trở thành adapter mặc định**: wildcard route `/*` → phải đặt tên `/*splat`; optional segment `?` trong path string bị bỏ, phải dùng `{}` (`/:file{.:ext}`); ký tự dành riêng `(()[]?+!)` phải escape; không còn hỗ trợ regex trong route pattern string. Query parser mặc định đổi từ `qs` (extended, hỗ trợ nested object/array qua `?filter[where][name]=`) sang `simple` (không tự parse nested) — muốn giữ hành vi cũ phải gọi `app.set('query parser', 'extended')`.
  - **Module resolution algorithm**: không còn sinh hash dự đoán được (predictable hash) cho dynamic module — chuyển sang so sánh theo object reference. Ảnh hưởng: nếu MỘT testing module import CÙNG MỘT dynamic module (`.register()`/`.forRoot()`) qua nhiều đường không được dedupe thủ công, có thể tạo ra nhiều instance khác instance mong đợi trong test — cách xử lý được guide gợi ý: dedupe thủ công, `module.select(ParentModule).get(Target)`, `module.get(Target, { each: true })`, hoặc trả về thuật toán cũ qua `Test.createTestingModule({}, { moduleIdGeneratorAlgorithm: 'deep-hash' })`.
  - **`Reflector`**: `getAllAndMerge` trả về object (thay vì mảng) khi chỉ có 1 metadata entry kiểu `object`; `getAllAndOverride` đổi kiểu trả về thành `T | undefined` (trước là `T`) — có thể lộ lỗi TypeScript `strictNullChecks` mới ở nơi đang giả định luôn có giá trị.
  - **Thứ tự lifecycle hook**: hook kết thúc (`onModuleDestroy`/`onApplicationShutdown`) chạy theo thứ tự NGƯỢC với hook khởi tạo (`onModuleInit`) trong chuỗi dependency — module global khởi tạo trước, kết thúc sau cùng.
  - **Thứ tự đăng ký middleware**: middleware khai báo trong global module giờ chạy trước, bất kể vị trí trong dependency graph.
  - **`@nestjs/cache-manager`, `@nestjs/config`, `@nestjs/terminus`**: đều có breaking change riêng (đổi sang Keyv, đổi thứ tự ưu tiên `ConfigService#get()`, đổi API `HealthIndicator`) — không liên quan nếu dự án không dùng các package này.
  - Guide **không đề cập** thay đổi nào cho Guards/Interceptors nói chung, hành vi injection `@Req()`/`@Res()`, hay `@nestjs/cli` — 3 mục này xác nhận qua release notes riêng (bên dưới) thay vì guide.
  - Từ release notes `v11.0.0` (không nằm trong guide, đọc trực tiếp changelog PR): `feat(common)!: type narrowing allowed injection tokens for @Inject()` (#12735, breaking theo conventional-commit `!`) — thắt chặt kiểu type nào được chấp nhận làm injection token; `fix(common)!: drop broken support for promises on exports của modules` (#14114) — bỏ một hành vi vốn dĩ bị lỗi (`exports: [aPromise]`), không phải use-case hợp lệ trước đó; sửa `Reflector` types (#13388, trùng với mục Reflector trong guide); `ClassSerializerInterceptor` áp dụng `options` đúng hơn khi gọi `plainToClass` (#12764).
  - `@nestjs/jwt`: tra riêng `gh api repos/nestjs/jwt/releases` — bản `11.0.0` ghi đúng 1 dòng "chore: update nest monorepo to v11" (chỉ là bump peer dependency theo NestJS 11, không đổi API); `11.0.1` thêm generic cho `sign`/`signAsync` (tính năng cộng thêm, suy luận kiểu tự động, không phá chữ ký cũ); `11.0.2` chỉ bump `jsonwebtoken` lên `9.0.3`. Không có breaking change API thật nào giữa `10.2.0` và `11.0.2`.
- **Đối chiếu từng breaking change ở trên với `apps/api/src` thật, không suy đoán chung chung** (Glob + Grep toàn bộ, nhiều pattern độc lập cho mỗi mục):
  - **Wildcard/optional route pattern**: grep `@(Get|Post|Put|Delete|Patch|All)\([^)]*[*?]` trên toàn bộ `src/` — **0 match**. Không có route nào dùng `*` hay `?` trong path string (khớp lại đúng phát hiện đã có từ I5).
  - **Query parser nested/array**: grep toàn bộ `@Query(` trên `src/` — **58 lần dùng, 100% đều là `@Query('tênKey') x?: string`** (khoá đơn, giá trị scalar) trên `customers`, `ledger`, `sync`, `suppliers`, `reports` controller — không chỗ nào cần parse `filter[where][name]` hay mảng qua bracket notation. Đổi default query parser (`qs`→`simple`) của Express 5 **không có điểm chạm nào** để ảnh hưởng hành vi hiện tại; không cần gọi `app.set('query parser', 'extended')`.
  - **Module resolution / dynamic module dedupe**: chỉ đúng 1 dynamic module trong toàn bộ `apps/api/src` — `JwtModule.register({...})` trong `src/auth/auth.module.ts`, được import đúng 1 lần (qua `AuthModule`, chính nó cũng chỉ được `AppModule` import 1 lần) — không có tình huống "cùng 1 dynamic module bị import qua nhiều đường trong cùng 1 module graph" mà breaking change này nhắm tới. Mỗi file `*.e2e-spec.ts` (39 file) tự `Test.createTestingModule({ imports: [AppModule] }).compile()` độc lập trong tiến trình Jest riêng — không chia sẻ module graph giữa các file, nên không có rủi ro duplicate-instance giữa các suite.
  - **`Reflector`**: grep `Reflector` toàn bộ `apps/api/src` — **0 file**. Không guard/interceptor nào trong dự án dùng `Reflector`/`SetMetadata`/`@nestjs/core`'s reflection API — cả 3 guard hiện có (`jwt-auth.guard.ts` chỉ `extends AuthGuard('jwt')` từ `@nestjs/passport`; `einvoice-permission.guard.ts`, `ledger-permission.guard.ts` tự implement `CanActivate` thuần, đọc trực tiếp `request.user` qua `context.switchToHttp().getRequest<{ user?: AuthUser }>()`) không đọc metadata qua decorator tuỳ biến. Thay đổi kiểu trả về `getAllAndOverride`/`getAllAndMerge` không có chỗ để chạm.
  - **Thứ tự lifecycle hook**: grep `onModuleDestroy|onApplicationShutdown|onModuleInit` toàn bộ `src/` — chỉ đúng 1 file, `src/prisma/prisma.service.ts` (`onModuleInit` gọi `$connect()`, `onModuleDestroy` gọi `$disconnect()`). Đây là SERVICE DUY NHẤT trong toàn bộ app implement lifecycle hook — không có chuỗi dependency nhiều hook để thứ tự "đảo ngược" ảnh hưởng tới.
  - **Thứ tự middleware**: grep `MiddlewareConsumer|NestModule|app\.use\(` toàn bộ `src/` — **0 file**. Dự án không dùng middleware kiểu NestJS (`configure(consumer: MiddlewareConsumer)`) hay raw Express middleware (`app.use`) ở bất kỳ đâu — thay đổi thứ tự đăng ký middleware không áp dụng.
  - **`@nestjs/config`/`@nestjs/cache-manager`/`@nestjs/terminus`**: cả 3 package đều **không có trong `package.json`** (dự án đọc thẳng `process.env` qua các hàm tự viết trong `src/config/`, không dùng `ConfigModule`) — 3 mục breaking change này không áp dụng.
  - **`@Inject()` type narrowing**: grep `@Inject\(` toàn bộ `src/` — đúng 1 lần, `src/einvoice/einvoice.service.ts:21` (`@Inject(EINVOICE_ADAPTER) private readonly adapter: EInvoiceAdapter`). `EINVOICE_ADAPTER` là injection token kiểu `string` thuần (`export const EINVOICE_ADAPTER = 'EINVOICE_ADAPTER'` trong `src/einvoice/einvoice.tokens.ts`) — dạng token chuẩn, hợp lệ với mọi phiên bản kiểu injection token trước/sau khi narrowing, không bị ảnh hưởng (build sạch xác nhận thêm).
  - **`@Req()` injection**: grep `@Req\(` toàn bộ `src/` — **72 lần dùng** trên 10 controller, **100%** type bằng shape tối giản tự định nghĩa `req: { user: AuthUser }` (không phải `express.Request` thật) — đúng như I5 đã xác nhận, hành vi này do NestJS tự triển khai nội bộ (đọc trực tiếp property `user` được `passport`/`AuthGuard` gắn vào request object), không phụ thuộc Express 4 hay 5. `@Res()` — vẫn **0 usage** trong toàn bộ dự án. `main.ts` vẫn gọi `NestFactory.create(AppModule)` thuần, không dùng generic `NestExpressApplication`, không thao tác raw Express instance. Xác nhận bằng thực nghiệm: toàn bộ 39 e2e suite (bao gồm 72 điểm gọi `@Req()` qua các route yêu cầu auth) chạy qua HTTP thật (`supertest` gọi `app.getHttpServer()` — chính là instance Express 5 thật, không phải mock) — pass 100% cả 3 lần chạy, xác nhận hành vi injection không đổi ở tầng runtime, không chỉ tầng type.
  - **`ClassSerializerInterceptor`/Interceptor nói chung**: grep `Interceptor` toàn bộ `src/` — **0 file**. Dự án không dùng interceptor nào (không serialize response qua class-transformer, trả JSON thuần qua object literal) — thay đổi hành vi `ClassSerializerInterceptor` không áp dụng.
- **Express runtime nội bộ — xác nhận thật, không chỉ tin release note**: `npm ls express` sau khi cài → `tap-hoa-api@0.1.0 -> @nestjs/platform-express@11.1.28 -> express@5.2.1` (trước đó, theo I5: `express@4.22.1` qua `@nestjs/platform-express@10.4.22`) — đúng như I5 đã cảnh báo trước, Express nội bộ đổi thật 4.x→5.x kèm theo I7, không phải giả định. Kiểm tra transitive quan trọng khác đi kèm: `multer@2.2.0` (từ `1.x` — dòng bundle theo `@nestjs/platform-express`), `body-parser@2.3.0` (dòng 2.x, bundle trong `express@5.2.1`), `path-to-regexp@8.4.2` (đổi hẳn dòng major so với `path-to-regexp@0.1.x` mà Express 4/`@nestjs/core@10` dùng — đây chính là thư viện chi phối cú pháp route pattern, xác nhận tại sao wildcard/optional-param cú pháp đổi, nhưng dự án không có route nào dùng 2 cú pháp đó nên không bị ảnh hưởng), `cors@2.8.6`, `router@2.2.0` (Express 5 tách routing thành package riêng).
- **`@types/express@5.0.6` (đã nâng từ I5) giờ mới thật sự khớp pha với runtime `express@5.2.1`** — I5 đã nâng type trước khi runtime đổi (chấp nhận rủi ro lý thuyết vì 0 điểm chạm code), I7 hoàn tất vế còn lại: type và runtime Express giờ cùng ở dòng 5.x, không còn lệch pha tiềm ẩn nào giữa 2 task.
- **`npm audit --omit=dev` trước/sau I7 — cải thiện bảo mật thật, không chỉ nâng version suông**: trước I7 **24 lỗ hổng (12 moderate/12 high)**, sau I7 **16 lỗ hổng (6 moderate/10 high)**. Đối chiếu trực tiếp qua `npm audit --json` (lưu file trước/sau, diff bằng tên package) xác nhận các package/advisory sau biến mất hoàn toàn khỏi audit sau I7 — tất cả đều nằm trong `fixAvailable` trỏ đúng tới `@nestjs/core@11.1.28`/`@nestjs/platform-express@11.1.28` **trước khi I7 chạy** (nghĩa là bản `10.4.22`/`11.1.17`-trở-xuống trước đó ĐÃ bị flag, không phải lỗ hổng mới xuất hiện do bump):
  - `@nestjs/core` (moderate) — **GHSA-36xv-jgw5-4q75** "Improperly Neutralizes Special Elements in Output Used by a Downstream Component ('Injection')", phạm vi `<=11.1.17` → đã vá ở `11.1.28`.
  - `@nestjs/platform-express` (high, qua `@nestjs/core`+`body-parser`+`express`+`multer`), `@nestjs/common` (moderate, qua `file-type`) — cascade từ 2 advisory dưới.
  - `express`/`qs` (moderate) — **GHSA-q8mj-m7cp-5q26** "qs has a remotely triggerable DoS... crashes with TypeError" phạm vi `qs 6.11.1-6.15.1`; `express` bản `4.21.0-4.22.1` bị flag qua chuỗi này.
  - `multer` (high) — 5 advisory riêng: **GHSA-xf7r-hgr6-v32p**, **GHSA-v52c-386h-88mc**, **GHSA-5528-5vmv-3xc2** (DoS qua cleanup/resource-exhaustion/recursion, phạm vi `<2.1.0`/`<2.1.1`), **GHSA-72gw-mp4g-v24j** (DoS qua nested field name, phạm vi `>=1.0.0 <2.2.0`), **GHSA-3p4h-7m6x-2hcm** (moderate, phạm vi `2.0.0-alpha.1 - <2.2.0`) — tất cả vá ở `multer@2.2.0` mà `@nestjs/platform-express@11.1.28` mang theo.
  - `body-parser` (qua `qs`) — **GHSA-v422-hmwv-36x6** (low, DoS khi limit value không hợp lệ), phạm vi `<1.20.6` — `body-parser@2.3.0` bundle trong `express@5.2.1` đã vá.
  - `file-type` (moderate, qua `@nestjs/common`) — **GHSA-5v7r-6r5c-r473** (infinite loop ASF parser) + **GHSA-j47w-4g3g-c36v** (ZIP decompression bomb), cả 2 phạm vi chạm bản đang dùng trước I7 — biến mất sau khi cây phụ thuộc `@nestjs/common@11.1.28` kéo bản `file-type` mới hơn.
  - 16 lỗ hổng còn lại sau I7 (6 moderate/10 high): toàn bộ đi qua `exceljs`→`archiver`/`@google-cloud/storage`→`teeny-request`/`gaxios`/`retry-request`→`uuid`/`glob`/`minimatch`/`brace-expansion`/`rimraf`/`readdir-glob`/`zip-stream`/`google-gax` — đúng nhóm đã ghi nhận từ I2/I3 là "không có bản vá lẻ khả thi, ngoài phạm vi I7 (không đụng `exceljs`/`firebase-admin` trong task này)", không phát sinh gì mới.
- **`npm run build`**: sạch tuyệt đối, exit code 0, **0 dòng log lỗi/cảnh báo nào** — chạy 2 lần độc lập (`nest-cli.json` có `deleteOutDir: true` nên mỗi lần `nest build` tự xoá `dist/` từ đầu, đảm bảo không phải build cache "ăn gian" che lỗi) đều cho kết quả giống hệt nhau. Đúng như dự đoán từ việc rà 0 điểm chạm code ở trên — **không cần sửa một dòng code nào** trong toàn bộ quá trình I7, khác hẳn dự đoán rủi ro ban đầu trong DoD gốc ("Sửa mọi lỗi build phát sinh").
- **`npm run test:unit`**: **12 suite/84 test pass** — khớp baseline I1-I6 tuyệt đối. Toàn bộ 12 suite unit test đều là pure-function/service test **không dùng `@nestjs/testing`'s `Test.createTestingModule`** (xác nhận qua grep `@nestjs/testing|createTestingModule` trên `src/` — 0 match) nên không có bề mặt rủi ro nào từ thay đổi module resolution algorithm ở lớp unit test.
- **`npm run test:e2e` — chạy đúng 3 lần theo yêu cầu, CẢ 3 LẦN đều dựng container Postgres 15 hoàn toàn mới từ đầu (vượt yêu cầu tối thiểu "ít nhất 1 lần"), không tái sử dụng container nào giữa các lần**:
  - **Lần 1**: container `tap-hoa-i7-e2e-pg-1`, cổng host `55438` (xác nhận trống trước khi dùng qua `docker ps -a` — tiếp đúng dãy cổng tăng dần I1-I6 đã dùng `55432-55437`, không trùng cổng dev `54422`/`3040`, Supabase `54721-54727`, prod `3041`). `docker run postgres:15` → `pg_isready` xanh sau 1 lần thử → `TAP_HOA_SKIP_LOCAL_IDENTITY=1` + `DATABASE_URL` trỏ container → `npx prisma migrate deploy` (log CLI in đầy đủ tên cả 28 migration, không bị `rtk` rút gọn lần này) → xác minh lại bằng SQL trực tiếp (không tin log CLI, đúng pattern I1-I6): `SELECT count(*) FROM _prisma_migrations` → **28**, `information_schema.tables` → **47 bảng** — khớp đúng baseline. `npx prisma db seed` chạy thẳng, **không bị `rtk` chặn** (giống quan sát cuối I6, khác I1-I5) → seed owner `0900000001` + store `CH1`/`CH2` + 5 cash category + accounts. `npm run test:e2e` → **39/39 suite, 146/146 test pass**, 42.7s — không gặp flaky `vat-summary`.
  - **Lần 2**: `docker stop`/`docker rm` container lần 1 trước khi tạo container mới (không tái sử dụng). Container `tap-hoa-i7-e2e-pg-2`, cổng host `55439` (trống, tiếp dãy cổng). `pg_isready` xanh sau 2 lần thử. `migrate deploy` → SQL xác nhận lại **28** migration / **47** bảng. `db seed` thành công (không bị `rtk` chặn). `npm run test:e2e` → **39/39 suite, 146/146 test pass**, 43.6s — không gặp flaky.
  - **Lần 3**: `docker stop`/`docker rm` container lần 2. Container `tap-hoa-i7-e2e-pg-3`, cổng host `55440`. `pg_isready` xanh sau 2 lần thử. `migrate deploy` → SQL xác nhận **28** migration / **47** bảng. `db seed` thành công. `npm run test:e2e` → **39/39 suite, 146/146 test pass**, 47.6s — không gặp flaky.
  - **Tổng kết 3 lần**: **117/117 suite pass, 438/438 test pass** (39×3, 146×3), **0 lần flaky** — kể cả `vat-summary.e2e-spec.ts` (đã biết flaky do thứ tự chạy phụ thuộc dữ liệu `SaleReturn` sót lại, ghi nhận từ I2, gặp lại ở I5) chưa gặp lại lần nào trong cả 3 lần chạy I7. Vì mỗi lần đều dùng container HOÀN TOÀN MỚI (không phải chỉ reset schema như một số task trước), thứ tự Jest sequencer + dữ liệu test hoàn toàn độc lập giữa 3 lần — mức độ tin cậy verify cao hơn yêu cầu tối thiểu của DoD.
  - `docker stop`/`docker rm` cả 3 container tạm ngay sau mỗi lần dùng xong (không đợi tới cuối mới dọn hàng loạt). `docker ps -a` cuối cùng xác nhận đúng 26 container (14 running + 12 stopped) — khớp chính xác danh sách trước khi bắt đầu I7, không đụng bất kỳ container prod/Supabase nào (`tap-hoa-prod-api`, `tap-hoa-prod-db`, `supabase_*_omni-commerce`, `supabase_*_tap-hoa`).
- **Rủi ro thật sự**: không có. Đây là trường hợp bump "core framework" nhưng **0 điểm chạm code thật** — tương tự phát hiện đã có ở I4/I5 (dependency được đánh giá rủi ro "Trung bình/Cao" ở bảng Tech Stack ban đầu nhưng thực tế xác minh ra 0 usage thật của phần API bị breaking) — khác biệt là ở I7, mức độ rà soát phải sâu hơn nhiều lần (7 nhóm breaking change chính thức, mỗi nhóm đều grep+đọc code xác nhận riêng) đúng với tính chất "core framework, mọi controller/service/module/guard/decorator đều phụ thuộc". Điểm cộng thêm ngoài dự kiến: I7 tự động vá luôn 8 lỗ hổng bảo mật (1 moderate injection trong `@nestjs/core`, 5 advisory `multer`, 1 `qs`/`express`, 1 `body-parser`, 2 `file-type` — tổng hợp lại thành 24→16 trong audit) mà chưa có task nào trước đó chạm tới, vì toàn bộ nằm trong chính cây `@nestjs/*` là phạm vi của I7.

## I8 — `@prisma/client`/`prisma` 6→7 (Cao)

**DoD:**
- [x] Đọc migration guide chính thức Prisma 6→7 (breaking changes: schema syntax, client API, generator config).
- [x] Xác nhận patch version thật của `node:20-alpine` trong `apps/api/Dockerfile` đủ `>=20.19` (yêu cầu engine Prisma 7) — nếu base image pin ở patch cũ hơn, cần bump tag trước.
- [x] `npm install prisma@latest @prisma/client@latest`, chạy `npx prisma generate` lại, sửa lỗi phát sinh (tối thiểu).
- [x] `npm run build` + `test:unit` + full `test:e2e` xanh (bao gồm chạy `npx prisma migrate deploy` thật trên container Postgres tạm — xác nhận toàn bộ 28 migration cũ vẫn áp dụng đúng dưới Prisma 7).
- [x] Cập nhật bảng Trạng thái + tick DoD.

### Ghi chú review I8

- **Version thật trước→sau** (đối chiếu `git diff apps/api/package.json` + `npm ls`): `prisma` `^6.1.0` (resolve thật `6.19.3`) → `^7.9.1` (bản mới nhất hiện có, `npm view prisma dist-tags` xác nhận `latest: 7.9.1`, phát hành 2026 sau GA `7.0.0` ngày 19/11/2025); `@prisma/client` cùng `^6.1.0`(`6.19.3`)→`^7.9.1`. Thêm **dependency mới bắt buộc**: `@prisma/adapter-pg@^7.9.1` (kéo theo `pg@8.22.0` + `pg-pool@3.14.0` + `@prisma/driver-adapter-utils@7.9.1` — tất cả cùng lockstep version với `prisma`/`@prisma/client`, xác nhận qua `npm ls @prisma/adapter-pg pg`). `npm install` 2 lệnh (`prisma@latest @prisma/client@latest` rồi `@prisma/adapter-pg@latest`) đều sạch, không `ERESOLVE`/peer conflict — chỉ cảnh báo `npm warn allow-scripts` quen thuộc từ máy cục bộ (đã ghi nhận từ I1, không liên quan I8) cho `prisma@7.9.1`/`@prisma/engines@7.9.1` (2 gói mới có install script bị chặn bởi cơ chế `allow-scripts` cục bộ — không ảnh hưởng vì WASM engine không cần biên dịch native).
- **Đọc migration guide chính thức, 2 nguồn độc lập, không đoán** (đúng yêu cầu đề bài "đừng đoán"): (1) `gh api repos/prisma/prisma/releases/tags/7.0.0` — release notes gốc trên GitHub; (2) WebFetch `prisma.io/docs/orm/v6/more/upgrades/to-v7` — trang migration guide chính thức. Đây là breaking-change lớn nhất trong toàn bộ đợt I1-I8: Prisma 7 bỏ hẳn Rust query engine (binary/library engine), chuyển engine mặc định sang TS+WASM chạy qua **driver adapter bắt buộc** cho MỌI database (kể cả Postgres) — không còn đường nào để `PrismaClient` tự kết nối DB trực tiếp từ `url` khai trong `schema.prisma` như trước. Danh sách breaking change liên quan trực tiếp tới dự án, đọc thẳng từ 2 nguồn trên:
  - `datasource.url`/`shadowDatabaseUrl` bị cấm khai trong `schema.prisma` — phải chuyển sang `prisma.config.ts` (file cấu hình CLI mới, thay thế cơ chế cũ).
  - `new PrismaClient()` không tham số, `new PrismaClient({ datasources: ... })`, `new PrismaClient({ datasourceUrl: ... })` — cả 3 cách khởi tạo cũ đều bị "removed" (nguyên văn release note), thay bằng bắt buộc truyền `{ adapter }` (hoặc `{ accelerateUrl }` cho Prisma Accelerate, không áp dụng ở đây).
  - Generator `prisma-client-js` (cũ) được đánh dấu "sẽ bị gỡ ở bản tương lai" nhưng **CHƯA bị gỡ ở 7.9.1** — vẫn dùng được nếu không đổi sang generator mới `prisma-client` (ESM-only, yêu cầu `output` bắt buộc + `package.json#type=module` + đổi toàn bộ import path).
  - Block `"prisma": { "seed": ... }` trong `package.json` bị gỡ hoàn toàn — phải khai `migrations.seed` trong `prisma.config.ts`.
  - CLI không còn tự động load `.env` — phải tự `dotenv` hoặc đảm bảo biến môi trường có sẵn thật trong process.
  - Không có thay đổi nào được tài liệu chính thức nhắc tới cho `$transaction` API, `Prisma.Decimal`, hay cú pháp tagged-template của `$queryRaw`/`$executeRaw` — 3 điểm này về mặt API bề mặt giữ nguyên (đã tự kiểm chứng thêm bằng test thật, xem mục riêng bên dưới vì đây là yêu cầu đặc biệt của đề bài).
- **Xác nhận lại Node runtime thật trên CẢ 2 môi trường (không tin báo cáo I6 cũ, tự kiểm tra lại thật theo đúng yêu cầu đề bài)**: máy dev `node -v` → **v24.18.0** (thỏa `>=24.0`). `docker run --rm node:20-alpine node -v` (kéo lại thật, không đoán) → **v20.20.2** (thỏa `^20.19`) — khớp đúng số I6 đã báo cáo, xác nhận base image chưa bị đổi giữa các lần. `npm view prisma@7 engines` xác nhận lại `{ node: '^20.19 || ^22.12 || >=24.0' }` (từ bản `7.0.1` trở đi; riêng `7.0.0` ghi `^24.0` thay vì `>=24.0`, không ảnh hưởng vì đang cài `7.9.1`) — cả 2 môi trường đều đạt, không cần đổi base image `Dockerfile` (vẫn `node:20-alpine`, không ghim tag phụ).
- **Sự cố thật gặp phải theo đúng trình tự, tự sửa theo migration guide (không đoán mò)** — đây là phần cốt lõi nhất của I8:
  1. Cài xong, chạy `npx prisma generate` lần đầu (chưa sửa gì) → lỗi thật `P1012`: `"The datasource property url is no longer supported in schema files..."`, trỏ đúng dòng `url = env("DATABASE_URL")` trong `schema.prisma`. Sửa: xoá dòng `url` khỏi block `datasource db` (giữ `provider = "postgresql"`).
  2. Tạo mới `apps/api/prisma.config.ts` theo đúng shape API thật (đọc trực tiếp `node_modules/@prisma/config/dist/index.d.ts` để lấy đúng chữ ký `defineConfig`/`env`/`Datasource`/`MigrationsConfigShape`, không đoán tham số): `schema: 'prisma/schema.prisma'`, `migrations: { seed: 'ts-node prisma/seed.ts' }` (khớp đúng script cũ từng nằm trong `package.json#prisma.seed`, đã xoá khỏi `package.json`), `datasource: { url: env('DATABASE_URL') }`. **Không** thêm `dotenv/config` như ví dụ mẫu trong doc — xác nhận trước bằng grep: dự án chưa từng có file `.env` nào (gitignore chỉ liệt `.env`/`.env.production`, không file nào thật tồn tại trong repo) và không có `dotenv` ở bất kỳ đâu trong `src/`/`prisma/` — `DATABASE_URL` luôn được bơm thẳng qua process env thật (Docker `ENV`/`-e`, CI `env:` trong `.github/workflows/ci.yml`, hoặc shell export khi test cục bộ) — thêm `dotenv` sẽ là dependency thừa không dùng tới.
  3. `npx prisma generate` chạy lại (có set `DATABASE_URL` giả để test) → **thành công**, log xác nhận `Generated Prisma Client (v7.9.1) to ./node_modules/@prisma/client` — vẫn generate vào `node_modules` như cũ (đúng vì giữ nguyên provider `prisma-client-js`, không đổi `output`), import path `@prisma/client` trong toàn bộ `src/` **không cần đổi gì**.
  4. `npm run build` (`tsc`, chưa sửa `PrismaService`) → lỗi type thật (đúng dự đoán từ release note): `new PrismaClient` (trong `PrismaService extends PrismaClient` với constructor ngầm, và 2 script `prisma/seed.ts`/`prisma/create-owner.ts` gọi `new PrismaClient()` trực tiếp) đòi tham số bắt buộc. Sửa cả 3 nơi, thêm `@prisma/adapter-pg`: `src/prisma/prisma.service.ts` thêm `constructor() { super({ adapter: new PrismaPg({ connectionString: process.env.DATABASE_URL }) }); }`; `prisma/seed.ts` và `prisma/create-owner.ts` đổi `new PrismaClient()` → `new PrismaClient({ adapter: new PrismaPg({ connectionString: process.env.DATABASE_URL }) })`. Đọc trực tiếp `node_modules/@prisma/adapter-pg/dist/index.d.ts` xác nhận đúng chữ ký `constructor(poolOrConfig: pg.Pool | pg.PoolConfig | string, options?)` trước khi viết, không đoán.
  5. `npm run build` chạy lại → **sạch, 0 lỗi** (cả `tsc --noEmit` riêng lẫn `nest build` đều xác nhận `TypeScript: No errors found`) — đúng nghĩa "sửa tối thiểu": toàn bộ 55+ điểm dùng `Prisma.Decimal`/`Prisma.TransactionClient`/`Prisma.PrismaClientKnownRequestError`/`Prisma.JournalEntryWhereInput`/... trong `src/sync/`, `src/products/`, `src/suppliers/`, `src/ledger/`, `src/shifts/` **không cần sửa gì** — các type này không đổi shape giữa Prisma 6 và 7 khi vẫn dùng provider `prisma-client-js`.
  6. Phát hiện thêm khi build thử trong `Dockerfile` (không phải lỗi `npm run build` trên host, mà lỗi build-stage của image): `prisma.config.ts` gọi `env('DATABASE_URL')` **ngay cả cho lệnh `prisma generate`** (vốn không cần kết nối DB thật, chỉ cần đọc schema) — throw `PrismaConfigEnvError` nếu biến hoàn toàn chưa set (tự kiểm chứng bằng `env -u DATABASE_URL npx prisma generate` → lỗi thật). Build-stage cũ của `Dockerfile` (`COPY . . && RUN npx prisma generate && npm run build`) chưa từng cần `DATABASE_URL` dưới Prisma 6 (vì `url` nằm trong schema, chỉ lazy-eval lúc kết nối thật) — nay cần một giá trị placeholder. Sửa: thêm `ENV DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder?schema=public"` **chỉ ở build stage** ngay trước dòng `RUN npx prisma generate && npm run build` (không phải giá trị thật, không kết nối gì — `generate` chỉ đọc schema; runtime stage vẫn nhận `DATABASE_URL` thật từ `docker run -e` như cũ, không đổi).
  7. Phát hiện thêm thứ hai ở `Dockerfile`: runtime stage (image thứ 2) chỉ `COPY --from=build /app/prisma ./prisma` (thư mục con) — **không** copy `prisma.config.ts` (file nằm ở root `apps/api/`, ngang hàng `package.json`, không nằm trong thư mục `prisma/`) — nếu không sửa, container production thật sẽ thiếu file này lúc `CMD` chạy `npx prisma migrate deploy` (bước đầu của `CMD`). Sửa: thêm dòng `COPY --from=build /app/prisma.config.ts ./prisma.config.ts` vào runtime stage. Đã xác nhận `.dockerignore` không loại trừ `prisma.config.ts` (chỉ loại `node_modules`/`dist`/`coverage`/`.env*`/log/`.tsbuildinfo`).
- **Xác nhận thật bằng `docker build` thật, không chỉ tin `npm run build` trên host** (bước xác minh thêm, vượt yêu cầu tối thiểu DoD, hợp lý vì đã tự sửa `Dockerfile`): `docker build --target build -t tap-hoa-api-i8-buildtest .` chạy trọn vẹn trong `node:20-alpine` thật — `npm ci` (982 packages, có cảnh báo `EBADENGINE` cho `firebase-admin@14.2.0` và `@prisma/streams-local@0.1.11` đòi Node `>=22` trong khi image là Node 20 — CHỈ LÀ CẢNH BÁO không chặn cài đặt/chạy, `firebase-admin` đã ghi nhận vấn đề này từ I2, `@prisma/streams-local` là gói mới xuất hiện theo cây `prisma@7.9.1` — transitive, không phải dependency trực tiếp, phục vụ tính năng streaming của Prisma Postgres/Accelerate mà dự án không dùng, không có tác động thật) → `npx prisma generate` thành công trong container (log: `Loaded Prisma config from prisma.config.ts.` + `Generated Prisma Client (v7.9.1)`) → `npm run build` thành công → image export thành công. Dọn `docker rmi tap-hoa-api-i8-buildtest` ngay sau khi xác minh xong, không để lại image thừa.
- **Phát hiện 1 bug tiền tồn tại (pre-existing), KHÔNG do I8 gây ra, để ngoài phạm vi sửa** — phát hiện khi soát kỹ `Dockerfile` do phải tự sửa nó: `CMD` chạy `node dist/main.js`, nhưng `docker run --rm tap-hoa-api-i8-buildtest sh -c "ls /app/dist/src/main.js"` xác nhận file thật nằm ở **`dist/src/main.js`**, không phải `dist/main.js` — vì `prisma/seed.ts`+`prisma/create-owner.ts` (2 file **đã tồn tại từ trước I8**, không phải do I8 tạo ra, nằm ngang hàng `src/` chứ không nằm trong nó) khiến TypeScript tự suy luận `rootDir` chung là thư mục gốc `apps/api/` thay vì `src/`, làm `tsc`/`nest build` xuất toàn bộ cấu trúc thư mục (`dist/src/`, `dist/prisma/`) thay vì gộp phẳng `dist/`. **Xác minh đây là lỗi có từ trước I8, không phải do thay đổi của mình**: tạm di chuyển `prisma.config.ts` (file DUY NHẤT mình mới thêm) ra ngoài, chạy lại `npx tsc -p tsconfig.build.json --outDir dist_test` — kết quả **vẫn** ra `dist_test/src/main.js` (không phải `dist_test/main.js`), chứng minh nguyên nhân đến từ 2 file `prisma/*.ts` cũ, không liên quan `prisma.config.ts` mới. Nghĩa là `Dockerfile` hiện tại (kể cả trước I8, trên nhánh `main`) nếu build+run thật sẽ báo lỗi `Cannot find module '/app/dist/main.js'` — một bug production có thật nhưng **không ai từng phát hiện** vì I1-I7 (và cả I8 theo đúng phạm vi DoD) chỉ chạy `npm run build`/`test:e2e` qua Jest trực tiếp trên source, chưa từng thật sự `docker build` trọn cả 2 stage rồi `docker run` xem container có khởi động được không. Đây LÀ NGOÀI PHẠM VI I8 (không phải breaking change của Prisma 7, thuần là bug đặt sai đường dẫn `CMD` từ trước) — không tự ý sửa (đúng tinh thần "chỉ đụng phạm vi I8"), đã tạo task riêng để theo dõi (xem báo cáo cuối).
- **Kiểm tra kỹ các chỗ dùng Prisma nhạy cảm theo đúng yêu cầu đề bài** (grep toàn bộ `apps/api/src`, không đoán):
  - **`Prisma.Decimal`**: 55 lần dùng trên 6 file (`sync/sync.service.ts`, `sync/stock-ops.service.ts`, `sync/sale-returns.service.ts`, `products/products.service.ts`, `suppliers/suppliers.service.ts`, dùng cho `qty`/`qtyBase`/`packSize`/`systemQty`/`countedQty`/`varianceQty`...) — API bề mặt (`new Prisma.Decimal(x)`, `.mul()`, `.greaterThan()`, `.toString()`) không đổi theo migration guide (không nhắc tới); xác nhận qua build sạch (0 lỗi type) + toàn bộ e2e liên quan số lượng/giá vốn (`cogs-wac.e2e-spec.ts`, `inventory-movement.e2e-spec.ts`, `inventory-stock-ops.e2e-spec.ts`, `product-upserts.e2e-spec.ts`) pass cả 3 lần chạy.
  - **`$transaction`**: 20 điểm gọi (dạng interactive callback `async (tx) => {...}`) trên 9 service (`users`, `sync.service`, `sync/sale-returns.service`, `sync/stock-ops.service`, `products`, `shifts`, `suppliers`, `customers`, `einvoice`) — đây chính là cơ chế double-entry bookkeeping (qua `LedgerService` được gọi lồng bên trong các `$transaction` này). Không đổi API bề mặt, hoạt động đúng qua driver adapter (đã có transaction API riêng `startTransaction`/nested query trong `@prisma/adapter-pg`, xác nhận qua `node_modules/@prisma/adapter-pg/dist/index.d.ts`) — xác nhận bằng toàn bộ e2e liên quan sổ cái pass cả 3 lần (`account-ledger`, `ledger`, `vat-journals`, `transfer-journal`, `wastage-journal`, `ledger-returns-stocktake`, `phase3-hardening-accounting`).
  - **Raw SQL `$queryRaw`/`$executeRaw`**: đúng 3 điểm dùng, cả 3 đều ở `sync/stock-ops.service.ts` — 2 điểm `UPDATE "ProductStoreStock" SET "qty" = "qty" - CAST(${...} AS DECIMAL)...` (trừ tồn kho combo/thường, tagged-template interpolation tham số hoá, không nối chuỗi thô) và 1 điểm `SELECT "id" FROM "PurchaseOrder" WHERE "id" = ${...} FOR UPDATE` (row lock khi nhận hàng theo đơn đặt). Cú pháp tagged-template không đổi giữa Prisma 6/7 (migration guide không nhắc breaking change nào cho `$queryRaw`/`$executeRaw`) — xác nhận thật qua e2e `inventory-stock-ops.e2e-spec.ts`/`supplier-return.e2e-spec.ts` pass cả 3 lần (bao gồm đường combo product và đường nhận hàng theo PO).
- **Kết quả xác minh đầy đủ**:
  - `npm run build`: sạch, 0 lỗi (xác nhận cả trên host Windows lẫn trong `node:20-alpine` thật qua `docker build --target build`, xem mục trên).
  - `npm run test:unit`: **12 suite/84 test pass**.
  - `npm run test:e2e` chạy **3 lần, mỗi lần container `postgres:15` HOÀN TOÀN MỚI** (đúng yêu cầu "mỗi lần dựng DB Postgres 15 hoàn toàn mới", vượt tối thiểu 3 lần của I7):
    - **Lần 1**: container `tap-hoa-i8-e2e-pg-1`, cổng host `55441` (tiếp đúng dãy cổng tăng dần I1-I7 dùng `55432-55440`, xác nhận trống qua `docker ps -a` trước khi dùng). `migrate deploy` → log CLI "2 migration(s) deployed" (bị `rtk` rút gọn, y hệt I1-I7 đã ghi nhận) — xác minh THẬT bằng SQL trực tiếp: `SELECT count(*) FROM _prisma_migrations` → **28**, `information_schema.tables` → **47 bảng**, `WHERE finished_at IS NULL OR rolled_back_at IS NOT NULL` → **0** (không migration nào lỗi/rollback). `npx prisma db seed` bị `rtk` chặn nhầm (`[rtk: program not found]`, exit 127, y hệt I1-I5) → `rtk proxy npx prisma db seed` chạy thành công. `npm run test:e2e` → **39/39 suite, 146/146 test pass**, 55.3s.
    - **Lần 2**: `docker stop`/`docker rm` container lần 1 trước khi tạo mới (không tái sử dụng). Container `tap-hoa-i8-e2e-pg-2`, cổng host `55442`. `migrate deploy` → SQL xác nhận lại **28**/**47**/**0 lỗi**. `db seed` thành công qua `rtk proxy`. `npm run test:e2e` → **39/39 suite, 146/146 test pass**, 48.2s.
    - **Lần 3**: `docker stop`/`docker rm` container lần 2. Container `tap-hoa-i8-e2e-pg-3`, cổng host `55443`. `migrate deploy` → SQL xác nhận **28**/**47**/**0 lỗi**. `db seed` thành công. `npm run test:e2e` lần đầu → **38/39 suite, 145/146 test — 1 FAIL**: `vat-summary.e2e-spec.ts`, lỗi `PrismaClientKnownRequestError: Foreign key constraint violated: SaleReturn_originalSaleId_fkey` tại `prisma.sale.deleteMany()` dòng 61 — **đúng y hệt** chữ ký lỗi đã biết từ I2/I5 (dữ liệu `SaleReturn` sót từ suite chạy trước trong cùng lượt Jest do file test thiếu bước `saleReturn.deleteMany()` trước khi xoá `sale`, phụ thuộc thứ tự Jest sequencer, KHÔNG liên quan Prisma). Xử lý đúng theo hướng dẫn trong đề bài: `DROP SCHEMA public CASCADE; CREATE SCHEMA public;` reset sạch, `migrate deploy` lại (SQL xác nhận lại **28** migration) + `db seed` lại, chạy `npm run test:e2e` lần 2 trên cùng container → **39/39 suite, 146/146 test pass**, 48.3s — xác nhận rõ ràng đây là flake tiền tồn tại phụ thuộc thứ tự chạy, không phải regression thật của Prisma 7 (giống hệt cách I2 đã xác minh cho chính file test này).
    - **Tổng kết**: 116/117 lượt suite sạch ngay lần đầu (chỉ 1 lần dính flake đã biết, không phải lần đầu tiên hiện tượng này xuất hiện trong cả kế hoạch — đã gặp ở I2, I5, nay là I8), retry xác nhận 100% xanh. `docker stop`/`docker rm` cả 3 container tạm ngay sau mỗi lần dùng xong; `docker ps -a` cuối cùng xác nhận không đụng bất kỳ container nào khác (`tap-hoa-prod-api`, `tap-hoa-prod-db`, `supabase_*_omni-commerce`) — riêng `supabase_vector_omni-commerce` quan sát thấy đang tự "Restarting" ở cuối phiên làm việc, xác nhận đây là hành vi có sẵn của chính container đó (health-check riêng của Supabase Vector, không có lệnh `docker` nào của mình nhắm vào container này).
  - `npm audit --omit=dev` sau khi nâng: **16 lỗ hổng (6 moderate/10 high)** — **không đổi** so với số cuối I7 (cũng 16: 6 moderate/10 high). Xác nhận qua `npm audit --json`: không có package nào trong họ `prisma`/`@prisma/client`/`@prisma/adapter-pg`/`pg` xuất hiện trong danh sách bị flag — toàn bộ 16 lỗ hổng vẫn đi qua đúng chuỗi cũ `exceljs`→`archiver`/`uuid` và `firebase-admin`→`@google-cloud/storage`→`teeny-request`/`gaxios`/`retry-request`→`uuid` đã ghi nhận từ I2/I7, không có gì mới. `npm audit` đầy đủ (gồm dev): 35 lỗ hổng (6 moderate/29 high) — toàn bộ 35 package bị flag đối chiếu từng tên đều thuộc các chuỗi cũ đã biết (`jest`/`@jest/*`/`ts-jest`/`babel-jest`→`glob`/`minimatch`/`brace-expansion` từ I3, `@nestjs/cli`→`fork-ts-checker-webpack-plugin`→`minimatch`, cộng 2 chuỗi production ở trên) — **0 package mới từ Prisma xuất hiện trong audit**, bump Prisma 7 không tự thêm cũng không tự bớt lỗ hổng nào.
- **Rủi ro còn lại, không phải lỗi nhưng đáng ghi chú cho tương lai** (không hành động trong I8, đúng tinh thần "sửa tối thiểu"):
  - Driver adapter (`pg`) dùng connection-pool mặc định của chính `pg`, khác mặc định cũ của Rust engine (vd timeout connection — `pg` mặc định KHÔNG có timeout trong khi Prisma 6 mặc định 5s) — không quan sát thấy vấn đề gì qua toàn bộ test (container Postgres tạm, kết nối cực nhanh trong cùng máy), nhưng nếu production gặp DB chậm/mạng chập chờn trong tương lai, có thể cần cấu hình thêm option cho `PrismaPg`/`pg.Pool` (vd `connectionTimeoutMillis`) — hiện tại KHÔNG cấu hình gì thêm vì chưa có bằng chứng cần thiết, đúng nguyên tắc không sửa những gì chưa chứng minh hỏng.
  - SSL: connection string dùng trong toàn bộ e2e/CI đều dạng `postgresql://...@localhost:PORT/db?schema=public` không SSL — khớp đúng cách production hiện tại của repo triển khai (`tap-hoa-prod-db` là container Postgres chạy cùng host với `tap-hoa-prod-api` qua Docker network nội bộ, không phải DB ngoại vi qua Internet, theo quan sát `docker ps -a` trên máy) nên không cần cấu hình `ssl` cho `PrismaPg` — nếu sau này đổi sang DB managed (RDS/Supabase qua Internet) cần SSL, sẽ cần thêm `ssl: {...}` vào `PrismaPgOptions` lúc đó.
  - `@prisma/streams-local@0.1.11` (transitive mới, cảnh báo `EBADENGINE` đòi Node `>=22`) không phải dependency trực tiếp, không ảnh hưởng build/runtime hiện tại (chỉ cảnh báo, không chặn) — không hành động gì, chỉ ghi nhận.
  - Đã cân nhắc và CHỦ ĐỘNG KHÔNG đổi sang generator mới `prisma-client` (ESM, ~90% bundle nhỏ hơn, nhanh hơn tới 3 lần theo release note) — lý do: cần `output` bắt buộc (đổi mọi import `@prisma/client`→đường dẫn generated tuỳ ý), cần `package.json#type=module` (đổi toàn bộ hệ CJS hiện tại của `nest build`/`ts-jest`/`ts-node`/`jest` — rủi ro cao hơn lợi ích trong phạm vi 1 task, đúng tinh thần I6 đã áp dụng khi từ chối TypeScript 7.x). Có thể cân nhắc lại ở một task riêng trong tương lai nếu cần tối ưu hiệu năng/bundle size.

---

## Cách chạy (SDD)

| Bước | Cách |
|------|------|
| Mỗi task | **1 subagent** — implement + test, không đụng file ngoài scope |
| Review | Orchestrator đọc diff + tự chạy lại build/test/e2e độc lập trước khi tick DoD |
| Commit | 1 commit/task, message tiếng Việt mô tả rõ, cập nhật bảng Trạng thái + tick DoD trong cùng commit |
| Thứ tự | I1 → I2 → I3 → I4 → I5 → I6 → I7 → I8 (rủi ro thấp trước, cao sau) |
